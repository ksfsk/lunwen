# 评价协议

更新：2026-09-16。

本协议服务于当前工作题目（待导师确认）**《评分证据约束下的数学归纳法证明关键缺步诊断与分项评分研究》**。它规定未来实验的可比性和报告口径，不表示仓库已经实现这些实验。任务与代码差距见 [tasks.md](tasks.md) 和 [code_map.md](code_map.md)。

## 1. 评价目标

主实验回答：

1. 原文证据约束能否减少关键缺步误给分？
2. 在证据约束基础上加入步骤依赖，能否进一步减少前置步骤未建立时的后续误给分？
3. 上述机制分别增加多少合理省略误扣分、拒答和人工复核成本？
4. 可选局部 Lean 验证在相同解析输出下是否提供独立真实检错增益，其误报、覆盖和成本是多少？

不预设 M2、M3 或任何完整方法一定优于强 LLM 基线。无显著增益、负增益或仅在局部子集有效，均应按实际结果报告。

## 2. 数据范围与版本审计

当前首选基础数据是 Student Proof by Induction Data Set。正式实验前必须记录：

- 数据发布版本、许可、下载日期和原始文件校验值；
- 四道题的题面、grading instructions、字段和原始行数；
- 空答案、重复项、异常字符和清洗规则；
- 约 3586 份非空答案这一公开规模经本地核验后的实际可用数量；
- 原始 0/1/2 标签与任何二值化或新部分分口径的映射版本。

公开原标签不是当前所有评价任务的 gold。原文跨度、候选步骤、合理省略、关键缺步、依赖、首错/首缺步和复核需求须通过二次标注获得。

数据子集建议：

- 先导阶段约 80–120 份，用于完善标注手册、状态定义和失败分类；
- 正式阶段视标注资源和先导效应扩展至约 300–500 份；
- 数量只是资源规划，不是统计充分性、显著性或毕业成果保证。

真实学生答案与人工构造的“完整—合理压缩—关键删除—错误理由—依赖受阻”成对变体必须分别标识、分别报告，不将构造变体描述为新的真实课堂作答。

## 3. Gold 标注与质量控制

人工标注者只依据原题、学生原文、冻结 rubric 和冻结标注手册判断，不参考系统输出生成 gold。建议顺序为：

1. 独立判断七项 rubric 状态及分值；
2. 标注原文跨度和显式学生步骤；
3. 标注合理省略、部分完成、关键缺步和错误；
4. 标注可观察引用、公式/推理结构体现的依赖、允许隐含知识、评分所需路径及受阻关系；
5. 标注首错/首缺步和是否需要复核。

至少在预先确定的核心 test 子集上进行双人独立标注，并报告：

- 各字段的标注样本数和缺失数；
- 分类标签的一致率及 Cohen's kappa 或 Krippendorff's alpha；
- span 的严格匹配与重叠指标；
- 在各自可标注子集上报告显式引用边、结构性有效依赖和依赖状态的一致性；
- 分歧类型、裁决流程和最终 gold 版本。

若“合理省略/关键缺步”等核心标签的一致性不足，应先修订手册和限定任务，不应通过合并标签或删去难例制造高一致性。

## 4. 数据划分与泄漏控制

划分单位不是单条文本，而是同源组。以下内容必须在同一 split：

- 同一学生原始作答及其所有人工或模型变体；
- 近重复文本；
- 可识别的同一模板家族；
- 由同一基础证明经过不同删除/压缩得到的样本。

`few-shot`、`train`（若使用）、`dev` 和 `test` 严格分离：

- few-shot 只提供允许的示例；
- dev 用于 prompt、规则、阈值、允许省略和依赖政策开发；
- test 在上述内容冻结后只用于最终评价；
- test 的 StepIR、分数、错误类型、标注理由和同源变体不得进入模型请求、检索库或规则开发。

由于基础数据只有四道题，必须明确区分：

- **同题独立答案测试**：评价同一道题内未见学生表达，不能声称跨题泛化；
- **按题留出测试**：可作为探索性跨题实验，但四题规模不足以支持通用归纳法结论；
- **同结构换表述测试**：只评价表述稳健性，不能声称未见证明结构泛化。

若 B2 使用 RAG，检索库只能来自允许的训练/few-shot 数据，并按同源组去重。若某组允许题面、参考解、rubric 或 grading instructions，所有可比组应获得预先声明的同等信息；差异必须作为实验变量记录。

## 5. 运行类型与输出完整性

以下运行类型必须分开保存和报告：

- `oracle`：输入人工 StepIR，用于隔离评价下游依赖和评分；不能作为解析或端到端性能。
- `mock`：人为修改预测做受控故障分析；不能冒充真实模型结果。
- `interface_example`：接口和规范化演示。
- `real_prediction`：真实模型输出，保存原始响应、配置、重试和规范化记录。

每个 test 样本必须有一条最终运行记录。缺失输出标记 `missing_prediction`，不得回退 gold；重复 ID、错题 ID、无法对齐或额外 ID 在评价前显式报错并单列。失败、超时、歧义、不支持和复核样本保留在总体分母中，不得只报告可处理样本。

原始输出不可被修复后的 JSON 覆盖。格式修复、模型重试、人工修订和语义裁决分别留痕，禁止利用 gold 修复预测。

每条下游决定保存 `decision_provenance`（至少为 `automatic`、`human_reviewed`、`gold_annotation`）、`review_required` 和 `review_reason_codes`。人工确认只能进入 human-assisted 或 gold 结果，不能混入全自动主表。

## 6. 对照组与消融组

| 组 | 方法 | 评价目的 |
| --- | --- | --- |
| B0 | 原归纳法研究的嵌入/分类方法，或在代码不可得时明确标注的可复现替代基线 | 对齐公开数据上的传统 NLP 任务，不冒充原论文精确复现 |
| B1 | LLM 直接依据冻结 rubric 评分 | 直接评分基线 |
| B2 | LLM + rubric + few-shot/RAG | 强提示与检索评分基线 |
| M1 | 候选 StepIR + 原文证据约束 + 规则分项评分 | 测量证据约束的作用与代价 |
| M2 | M1 + 评分步骤依赖和状态传播 | 当前完整核心方法，测量依赖增量 |
| M3（可选） | M2 + 局部 Lean 验证 | 在同一解析输出下测量形式验证的独立增量与代价 |

必要的补充实验：

- `M2 - evidence`：去除可执行证据约束，检验证据机制作用；
- `M2 - dependency`：等同或对齐 M1，检验依赖机制作用；
- oracle StepIR 下比较规则评分与依赖评分，隔离解析误差；
- predicted StepIR 下做端到端比较，报告解析误差传播；
- M3 必须复用 M2 的同一批 StepIR、rubric 和依赖输入，只增加 Lean 记录，避免把更好解析误算为 Lean 增益。

对 B1/B2/M1/M2/M3 固定题目、答案、rubric 版本、参考信息范围、模型版本和可比预算；无法固定的差异必须披露。删除模块时明确替代行为，不能暗中使用该模块结果。自然真实输出与受控故障注入分开报告。

B0 原论文把原始 0/1/2 合并为二分类。若复现或替代 B0 不能输出部分分、证据或诊断，则只在预先定义的共同二值派生标签上与其他方法比较分类指标；总分 MAE、关键缺步、证据跨度和依赖指标标记为“不适用”，不得为凑齐主表而反推不存在的输出。

## 7. 核心指标

### 7.1 分项与总分

| 指标 | 口径 |
| --- | --- |
| 七项 rubric Macro-F1 | 对七个评分项分别计算后宏平均；同时报告各项 precision、recall、F1 和支持数 |
| 部分分一致性 | 若保留 0/1/2 或新有序状态，报告加权 kappa 或有序分类指标，不只二值化 |
| 总分 MAE | 仅对系统给出有效自动分数的样本计算，并同时报告其覆盖率 |
| 总分完全一致率 | 系统总分与人工 gold 完全相同的样本数 / 全部 test 样本；待复核不算自动匹配 |
| 总分 ±1 一致率 | 与 gold 相差不超过 1 分的自动结果 / 全部 test 样本；同时单列已评分子集结果 |

### 7.2 关键风险指标

设人工标注为 `missing_critical` 的 rubric 实例集合为 \(C\)，系统仍授予该项关键步骤分的实例为 \(A\)，则：

\[
\text{关键缺步误给分率}=\frac{|A\cap C|}{|C|}.
\]

设人工标注为 `omitted_allowed` 的实例集合为 \(O\)，系统因该省略扣除本应保留分数的实例为 \(D\)，则：

\[
\text{合理省略误扣分率}=\frac{|D\cap O|}{|O|}.
\]

两项须共同报告。不能通过对所有省略一律扣分来降低误给分，也不能通过大量送审来隐藏错误；需同时报告相同自动覆盖率或相同复核预算下的结果。

### 7.3 证据、步骤与依赖

| 指标 | 口径 |
| --- | --- |
| span 严格匹配 | 预测起止位置与 gold 完全相同的比例 |
| span 重叠质量 | 字符级 precision/recall/F1 或 token-level F1；明确分词与规范化规则 |
| 无依据补步率 | 被写入学生步骤层但没有有效原文证据的预测数 / 全部预测学生步骤；另报受影响样本率 |
| 显式步骤召回率 | 被正确提取的 gold 显式步骤 / 全部 gold 显式步骤；缺失预测计漏检 |
| 非空白字符处置覆盖率 | `source_spans` 与 `unparsed_spans` 并集覆盖的原答案非空白字符 / 原答案全部非空白字符；另由 gold 标注判断其中哪些是真正证明内容，防止只解析容易片段仍报告 `parsed` |
| 步骤角色 Macro-F1 | 对七项角色及预先定义辅助角色报告 |
| 可观察引用边 precision/recall/F1 | 在预先定义的步骤对齐后，只在人工可裁定子集评价 StudentStepIR 的 `premise_refs`/`step_refs`；`ambiguous`、`not_observed` 与未标注必须分开，不能强造 gold 边 |
| 有效步骤依赖 precision/recall/F1 | 评价下游 `effective_step_dependencies`，并按 `explicit_reference`、`implicit_structural`、`implicit_allowed_knowledge` 分层报告；与 rubric ID 路径分开 |
| Rubric 路径与状态 | 评价 `satisfied_path_id` 准确率及评分项级 `dependency_status` Macro-F1；冻结的 `required_dependency_paths` 是共享政策，不作为预测边计算 F1 |
| 受阻状态 F1 | 对人工标注 `blocked_dependency` 的评分项评价 |
| 首错/首缺步定位 | 在可裁定子集中正确定位数量 / 该子集总数；缺失输出计未定位 |

证据跨度正确不等于 claim 语义忠实。应在预先抽样或全部核心 test 上增加人工语义忠实性判断，单列“原文被系统修正、强化或补全”的错误。

依赖指标也必须与评分结果分开解释。`premise_refs`/`step_refs` 为空不自动构成错误；只要学生公式或推理结构直接体现合法使用，或冻结政策允许隐含使用背景知识，有效依赖仍可成立。相反，只有规则名称而没有实际使用，不应计为有效依赖。核心报告优先关注有效依赖、路径状态和受阻诊断；可观察引用边作为辅助解析指标。

### 7.4 覆盖、复核与成本

| 指标 | 口径 |
| --- | --- |
| 自动覆盖率 | 无需人工复核即可给出全部目标分项的样本数 / 全部 test 样本 |
| 分项覆盖率 | 系统可自动裁定的 rubric 实例数 / 全部 rubric 实例 |
| 人工复核率 | 至少一个目标分项为 `needs_review` 的样本数 / 全部 test 样本 |
| 复核工作量 | 需复核的分项数、证据字符数及人工分钟数（若实际测量） |
| 时间 | 每份答案端到端延迟及各模块耗时分布 |
| 费用 | 模型 token/调用费用及可选 Lean 执行资源，注明计价日期和假设 |

报告风险—覆盖曲线或在固定复核预算下比较关键错误率。`needs_review` 不视为学生零分，也不视为自动预测正确。

## 8. 可选 Lean 增量评价

M3 除上述指标外，至少报告：

- 支持生成的局部任务数及占候选 R2/R6/R7 步骤的比例；
- `generated` 任务数，以及 `verified`、`invalid_inference`、`blocked_dependency`、`unsupported`、`formalization_error`、`timeout`、`system_error`、`needs_review` 各状态数量；
- 经人工确认的新增真实错误数；
- 经人工确认的新增误报数；
- 自动形式化忠实率；
- 因加入 Lean 增加的复核、延迟和资源成本；
- 在相同 StepIR 和自动覆盖率下，M3 相对 M2 的配对差异。

Lean 编译失败不能自动记为学生错误。新增检错只有在排除错误翻译、缺少库支持、作用域错误和系统异常后，才能计为 Lean 的真实收益。即使 Lean 只在局部子集有效，也必须报告全体覆盖和适用范围。

## 9. 统计比较与误差分析

- 对同一 test 样本上的方法使用配对比较；为主要比例和 MAE 报告 bootstrap 置信区间。
- 同一原答案及其变体是同源观测，bootstrap 或显著性检验应按组重采样，不能当作完全独立样本。
- 对关键缺步误给分、合理省略误扣分等配对二元结果，可补充 McNemar 检验；小样本时优先报告效应量和区间。
- 按题目、rubric 项、完整/省略/缺步/错误/歧义、真实/构造来源分层报告。
- 建立错误分类：证据对齐失败、语义漂移、角色误判、替代路径漏识别、依赖错误、规则错误、模型拒答，以及可选形式化错误。

统计显著不等于教学价值。应结合误给分下降、误扣分上升、自动覆盖和人工成本判断实际意义。

## 10. 可复现性

每次正式实验记录：

- 数据版本、清洗脚本、split 清单及校验值；
- gold、Schema、rubric、prompt、few-shot、RAG 库、省略和依赖规则版本；
- 代码提交或源码校验值；
- 模型完整标识、日期、温度、seed（若可用）、token 预算、重试和并发策略；
- 运行次数、原始响应、规范化日志、失败原因、时间与费用；
- 若启用 Lean，记录 Lean/Mathlib 版本、生成模板、允许依赖、超时和硬件环境。

主表来自冻结独立 test。开发集试验、接口示例、oracle、mock、人工修复后结果和 human-assisted 结果必须分表，不得混作全自动主结果。

## 11. 阶段决策标准

先导阶段首先判断标注和任务是否成立，而不是追求高分：

- 核心标签能否达到可解释的人工一致性；
- 证据约束是否确实减少系统补步；
- 依赖机制是否在不过度误伤替代路径时减少错误传播；
- 自动覆盖和复核工作量是否可接受；
- 失败是否形成稳定、可研究的类型。

M2 是否保留为核心方法，由独立 test 上相对 B2/M1 的风险—覆盖—成本证据决定。M3 是否进入正式论文标题和主要贡献，由其相对 M2 的新增真实检错、误报、覆盖和成本决定。任何组没有胜出都应如实记录，并分析是解析、标注、规则、依赖还是形式化瓶颈。

## 12. 实施与验收要求

当前课题的主结果只能来自本协议定义的冻结数据、独立 test 和完整输出清单。接口样例、人工构造输出、oracle 运行或其他题型结果均不得计入主指标。

新增代码必须建立当前课题的数据、Schema、评分和评价测试；若复用已有公共组件，还需运行对应回归以确认工程行为没有被意外破坏。仅修改文档时检查链接、术语、版本和“已实现/待实现”表述即可。
