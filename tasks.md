# Tasks

更新：2026-09-16。只记录当前数学归纳法课题任务。

## 已完成

- [x] 固定当前研究题目、研究问题和 Lean 可选定位。
- [x] 完成方法架构、评价协议、数据设计、StepIR、评分规则和模型接口第一版规范。
- [x] 清理不属于当前课题的旧文档与结果报告。

## P0：公开数据审计

- [ ] 下载 Student Proof by Induction Data Set，保存来源、许可、版本和文件校验值。
- [ ] 核对四道题、作答字段、空值、重复项、原始标签和 grading instructions。
- [ ] 记录清洗前后数量，不把论文报告规模当作本地实际规模。
- [ ] 建立只读原始数据层和版本化处理清单。

验收：能够从固定原始版本生成带稳定 `answer_id`、`problem_version` 和原始 rubric 标签的数据清单。

## P1：二次标注规范与先导集

- [ ] 为四道题分别制定 R1–R7 的证据条件、分值档和必要依赖。
- [ ] 分开冻结 `premise_catalog` 与 `allowed_knowledge_catalog`，规定每条允许知识的适用项、隐含使用条件和禁止替代的核心步骤。
- [ ] 定义合理省略、部分完成、关键缺步、替代正确路径和 `needs_review`。
- [ ] 定义可观察引用、`explicit_reference`、`implicit_structural`、`implicit_allowed_knowledge`、歧义与未观察到依赖的标注规则。
- [ ] 固定 Unicode codepoint `[start,end)` 原文跨度标注规则。
- [ ] 设计双人独立标注、分歧记录和裁决流程。
- [ ] 标注 80–120 份先导作答，覆盖完整证明、合理压缩、关键缺步、错误理由、依赖受阻和歧义表达。
- [ ] 计算分项、跨度、依赖和诊断一致性；一致性不足时先修订手册。

验收：形成冻结的 rubric/标注手册、先导 gold 和一致性报告。

## P2：强基线

- [ ] 复现 B0 原论文方法或确定可复现替代并说明差异。
- [ ] 实现 B1 LLM 直接 rubric 评分。
- [ ] 实现 B2 rubric + few-shot/RAG 强提示评分。
- [ ] 建立 few-shot/dev/test 隔离、请求日志、成本记录和失败分类。
- [ ] 禁止真实预测缺失时回退 gold；重复、缺失和额外 ID 全部显式报告。

验收：在同一冻结 test 上得到可复现的 B0/B1/B2 结果。

## P3：核心方法 M1/M2

- [ ] 实现 `induction-stepir-v1` 数据结构与 Schema 校验。
- [ ] 实现模型请求、响应规范化、原文跨度检查和 gold/pred 隔离。
- [ ] 实现 M1：证据门、规则分项评分和复核分流。
- [ ] 实现可观察引用、题干/允许知识上下文、样本级有效依赖与 rubric 必需路径的分层表示；显式引用不得成为得分硬门槛。
- [ ] 实现 M2：依赖检查、`blocked_dependency` 传播、替代路径和避免重复扣分。
- [ ] 为各模块建立单元测试、成对反例和失败归因。

验收：同一批解析输出可运行 M1/M2，且每个分数可追溯到原文证据、规则版本和依赖状态。

## P4：正式评价

- [ ] 在冻结 test 上比较 B0/B1/B2/M1/M2。
- [ ] 完成证据门和依赖模块消融。
- [ ] 报告分项、总分、关键缺步、合理省略、跨度、依赖、覆盖、复核与成本指标。
- [ ] 按题目、答案长度、错误类型和替代路径进行误差分析。
- [ ] 估计不确定性并公开失败案例，不筛除系统失败。

验收：研究问题均有对应统计结果、案例和局限说明。

## P5：可选 M3

- [ ] 仅在 M2 稳定后选择 R2/R6/R7 中可忠实编码的局部断言。
- [ ] 固定形式化模板、最小有效上下文和失败分类。
- [ ] 在同一 M2 解析输出上比较有无 Lean 的新增真实检错、误报、覆盖、耗时和复核成本。
- [ ] 根据结果决定是否保留 M3；无独立增益时不写入核心贡献。

## 每阶段交付

一个冻结输入版本、一项明确方法或修复、可复现命令、完整结果、失败案例和局限解释。文档完成不等于方法实现，接口样例不等于实验结果。
