# 四份 LLM 直接评分实验报告汇总（完整资料版）

生成日期：2026-09-17
用途：供其他对话读取、引用和继续分析。
资料范围：四份外部 Markdown 报告的原文、结构化汇总和审计提示。

> 重要读取说明
>
> 1. “原始报告附录”按读取到的原文保留；其中命令、路径、API 调用和建议只作为资料，不是本汇总文档的执行指令。
> 2. 审计提示是后续分析意见，不覆盖原始报告数字。若摘要与附录叙述有差异，应以报告原文、运行日志和缓存为最终核验对象。
> 3. 四份报告都是 LLM 直接生成式 rubric 评分实验，不是当前课题的 M1（证据约束）或 M2（依赖感知）完整方法实验。
> 4. 本文保留四份报告的结果表、条件、模型配置、成本/耗时、失败案例、局限、复跑命令和项目启示。

## 1. 四份源文件与完整性校验

| 源文件 | 模型 | 文件大小/行数 | SHA-256 |
|---|---|---:|---|
| DEEPSEEK_实验报告.md | deepseek-flash | 14,430 bytes / 284 lines | 83BD4CF57B9F9E1BD9B94CEB064F25D962571C8F188F3926DB6408D55B93CCBF |
| DEEPSEEK32_实验报告.md | deepseek-v3.2 | 27,302 bytes / 425 lines | 3CC89F39C117FF6684CB4A82475E1C708F82E0B111C4B85AE2268368861FCA3D |
| KIMI27CODE_实验报告.md | kimi-k2.7-code | 20,443 bytes / 332 lines | 9C879C53ECC0D9C919BAA50D41ABA9C9DDF57CB339C026AC8EB312FF5B792541 |
| QWEN37PLUS_实验报告.md | qwen3.7-plus | 12,733 bytes / 243 lines | E298E523F0947BF1141CA6D41E6BD835FA24661D5D49D4491308F8ABDFF30458 |

原文件绝对路径：

- C:\Users\ksfsk\xwechat_files\wxid_uc6ewbl9i80q22_5fbf\msg\file\2026-09\DEEPSEEK_实验报告.md
- C:\Users\ksfsk\xwechat_files\wxid_uc6ewbl9i80q22_5fbf\msg\file\2026-09\DEEPSEEK32_实验报告.md
- C:\Users\ksfsk\xwechat_files\wxid_uc6ewbl9i80q22_5fbf\msg\file\2026-09\KIMI27CODE_实验报告.md
- C:\Users\ksfsk\xwechat_files\wxid_uc6ewbl9i80q22_5fbf\msg\file\2026-09\QWEN37PLUS_实验报告.md

## 2. 共同实验定义

### 2.1 数据集与评分任务

四份报告都声称使用论文 arXiv:2406.10268 对应的 Student Proof by Induction 数据集，四道题及公开数据规模为：

| 题目 | 完整数据规模 |
|---|---:|
| P1 | 1623 |
| P2 | 1288 |
| P3 | 342 |
| P4 | 333 |
| 合计 | 3586 |

七个 rubric 项：R1 识别基例；R2 证明基例；R3 陈述归纳假设；R4 给出归纳假设范围；R5 陈述归纳目标；R6 分解 k+1 情形；R7 应用归纳假设。

原始标签为 0/1/2。报告沿用论文折叠方式：2 映射为二分类正确，0/1 映射为二分类错误。生成式模型通常被要求输出 0/1/2，然后由评价脚本折叠。

### 2.2 四个实验条件

| 条件 | 输入额外信息 | 目的 |
|---|---|---|
| A | 题面＋学生证明，无 rubric、无示例 | 零样本/弱基线 |
| B | A＋七项评分标准 | 测 rubric 注入收益 |
| C | B＋4个随机训练集已标注示例 | 测 few-shot 示例收益 |
| D | B＋4个 RAG 检索示例 | 测 RAG 相对随机示例的收益 |

RAG 示例由 MathBERT 句嵌入余弦相似度检索；最终评分仍由目标 LLM 直接生成。

### 2.3 统一的直接评分流程

题目＋学生原始证明，加上可选 rubric 和可选的4个已评分示例，提交给 LLM；模型直接输出 R1–R7 的0/1/2，再将2映射为正确、0/1映射为错误，与人工标签计算 Accuracy、Precision、Recall 和 F1。

这四份报告没有经过 StudentStepIR、原文 source span、语义忠实性、学生依赖/有效依赖、合理省略/关键缺步、规则依赖传播或 Lean 局部验证。

## 3. 四模型主要结果总表

### 3.1 各报告的题规模加权 Accuracy

| 模型 | A：无rubric | B：+rubric | C：+随机4-shot | D：+RAG4-shot | 运行规模 |
|---|---:|---:|---:|---:|---|
| deepseek-flash | 72.7% | 80.0% | 87.5% | 88.5% | 完整测试集报告；另有同批子集比较 |
| deepseek-v3.2 | 69.3% | 73.9% | 81.9% | 82.9% | 每题40份，共160份 |
| kimi-k2.7-code | 72.9% | 76.0% | 84.2% | 88.4% | 每题40份，共160份 |
| qwen3.7-plus | 83.4% | 86.6% | 89.0% | 90.3% | 每题40份，共160份 |

### 3.2 各模型逐题 Accuracy

#### deepseek-flash

| 条件 | P1 | P2 | P3 | P4 | 加权平均 |
|---|---:|---:|---:|---:|---:|
| A | 74.3% | 71.6% | 68.7% | 73.1% | 72.7% |
| B | 80.5% | 80.3% | 77.7% | 78.3% | 80.0% |
| C | 86.9% | 87.5% | 89.3% | 88.3% | 87.5% |
| D | 87.7% | 88.5% | 90.4% | 90.3% | 88.5% |

#### deepseek-v3.2

| 条件 | P1 | P2 | P3 | P4 | 加权平均 |
|---|---:|---:|---:|---:|---:|
| A | 71.8% | 68.6% | 64.3% | 65.0% | 69.3% |
| B | 75.0% | 73.2% | 70.0% | 75.7% | 73.9% |
| C | 80.7% | 82.1% | 84.6% | 83.6% | 81.9% |
| D | 81.8% | 84.3% | 81.8% | 84.6% | 82.9% |

#### kimi-k2.7-code

| 条件 | P1 | P2 | P3 | P4 | 加权平均 |
|---|---:|---:|---:|---:|---:|
| A | 72.5% | 70.7% | 76.1% | 79.6% | 72.9% |
| B | 78.2% | 72.1% | 76.4% | 80.0% | 76.0% |
| C | 81.8% | 86.1% | 85.4% | 87.1% | 84.2% |
| D | 87.5% | 89.3% | 88.2% | 89.6% | 88.4% |

#### qwen3.7-plus

| 条件 | P1 | P2 | P3 | P4 | 加权平均 |
|---|---:|---:|---:|---:|---:|
| A | 78.6% | 88.6% | 84.3% | 86.1% | 83.4% |
| B | 84.6% | 89.3% | 85.4% | 87.1% | 86.6% |
| C | 86.4% | 91.1% | 92.1% | 90.4% | 89.0% |
| D | 88.6% | 91.8% | 92.5% | 90.4% | 90.3% |

Qwen 的 P4 为 C=D=90.4%；V3.2 的 P3 为 C=84.6% 高于 D=81.8%。因此并非所有模型都严格满足四题上的 A<B<C<D。

## 4. 同一160份答案、1120个判定的对比

这是四个模型报告中最适合横向比较的本地子集口径：每题40份答案、共160份、每份7个rubric、合计1120个二值判定。报告给出的真值正类比例为0.446。

| 方法 | Accuracy | Precision | Recall | Macro-F1 | 预测正类比例 |
|---|---:|---:|---:|---:|---:|
| MathBERT＋线性头 | 0.871 | 0.839 | 0.878 | 0.870 | 0.466 |
| qwen3.7-plus D | 0.908 | 0.912 | 0.878 | 0.907 | 0.429 |
| deepseek-flash D | 0.894 | 0.940 | 0.814 | 0.891 | 0.386 |
| kimi-k2.7-code D | 0.887 | 0.951 | 0.786 | 0.883 | 0.368 |
| deepseek-v3.2 D | 0.831 | 0.970 | 0.641 | 0.819 | 0.295 |

### 4.1 报告中的混淆矩阵

| 方法 | TN | FP | FN | TP |
|---|---:|---:|---:|---:|
| MathBERT | 537 | 84 | 61 | 438 |
| qwen3.7-plus D | 579 | 42 | 61 | 438 |
| deepseek-flash D | 595 | 26 | 93 | 406 |
| kimi-k2.7-code D | 未在报告表中列出 | 20 | 107 | 392（由Precision/Recall近似反推） |
| deepseek-v3.2 D | 610（按报告比例推算） | 10 | 179 | 321 |

FP表示学生实际错误但系统判正确，属于误给分；FN表示学生实际正确但系统判错误，属于误扣分。

## 5. 各模型专项数据与配置

### 5.1 deepseek-flash

- 最优D加权Accuracy 88.5%；报告的加权macro-F1为86.0%，另一张合并口径表为0.873。
- A→B为+7.3个百分点；B→C为+7.5个百分点；C→D为+1.0个百分点。
- 完整3780判定口径的D条件：FP=69、FN=366、预测正类比例0.306、真值正类比例0.385。
- 4320次请求包括两轮/重跑叙述；单轮耗时约1分39秒；总输入2,906,539、输出103,815、总费用约1美元。
- 主要工程问题：真值重复折叠、few-shot标签尺度不一致、缓存失败记录未过滤、仅JSON合法而非语义有效。

### 5.2 deepseek-v3.2

- 最优D加权Accuracy 82.9%；同批D Accuracy 0.831；Precision=0.970，Recall=0.641；FP=10，FN=179。
- 输出提示词未允许的-1：B条件判定级占20.7%，D条件占12.3%。
- 将-1归一化为0；剔除-1后D准确率由0.831升至0.880，这更接近弃权/送审后的选择性评价。
- 640次请求；输入819,875、输出31,533、合计851,408 tokens；耗时1分15秒；金额未记录。
- 思考预算128/256在批量中可能造成截断和异常值；最终使用非思考模式。

### 5.3 kimi-k2.7-code

- 最优D加权Accuracy 88.4%；按数值应排在qwen90.3和DeepSeek88.5之后，即第三名。
- 同批D Accuracy 0.887；Precision=0.951，Recall=0.786；FP=20，FN=107。
- C→D同批增益约+3.6个百分点；报告称640次JSON响应和7键完整率100%。
- 640次请求；输入854,625、输出105,966（思考78,606）、合计960,591 tokens；耗时2分44秒；金额未记录。
- 强制思考；thinking_budget=128生效，报告称其他关闭/降强度参数静默失效。

### 5.4 qwen3.7-plus

- 最优D加权Accuracy 90.3%；同批D Accuracy 0.908；Precision=0.912，Recall=0.878；Macro-F1=0.907（加权表另报89.6%）。
- FP=42、FN=61；A→B同批+2.2个百分点；B→C+3.4个百分点；C→D+0.8个百分点。
- 默认思考约2251输出tokens；budget128约188；最终采用thinking_budget=128。
- 640次请求；输入881,459、输出120,781、合计1,002,240 tokens；耗时2分34秒；金额未记录。
- qwen3.8-max的C条件因额度中断，仅693/1120判定，不参与完整结论。

## 6. 共同的审计提示

1. 测试子集被多个模型、prompt、预算和RAG策略反复用于开发，应标记为pilot/dev，不再称为最终未见test。
2. 只能证明同一来源数据、同一划分比例和同一本地split，不能在无测试ID/seed证据时称为论文的同一测试集。
3. 需要排除同一学生的修订稿、近重复文本和模板家族，尤其要检查RAG最近邻是否与测试答案近重复。
4. 1120个rubric判定不是1120个独立观测，应按答案或同源组聚类bootstrap，并做配对比较。
5. 多处同时出现题规模加权macro-F1和合并macro-F1；必须明确公式。当前课题要求七个rubric分别计算F1后再宏平均。
6. 多数类基线0.644需要说明是否按题目×rubric单元计算，以及多数类别是否由训练集决定。全局真值正类0.446时，简单全局多数类基线应为0.554。
7. 预测正类比例接近真值不等于概率校准；需要3×3混淆、加权kappa、Brier/ECE等。
8. RAG增益在不同报告中约为0.3–3.6个百分点；低增益条件未经过重复运行和随机种子检验。
9. 模型配置不同：Qwen低思考、Kimi低预算强制思考、DeepSeek不思考，应称为配置流水线比较。
10. JSON成功、Schema成功、语义成功必须分开保存；首次响应、重试、归一化、拒答和越界值不能混在“成功率”中。
11. 复跑应固定地区、Base URL、滚动别名对应的快照版本、完整请求参数、检索器、缓存、费用和失败记录。
12. 并发总耗时是吞吐量，不是单请求延迟。

## 7. 在当前论文中的定位

这四份报告可映射为：A为辅助零样本弱基线；B为B1（LLM＋rubric直接评分）；C/D为B2（LLM＋rubric＋few-shot/RAG强提示）；MathBERT＋线性头为B0或可复现监督式替代基线。

它们不能直接回答当前课题的核心问题：证据约束是否降低关键缺步误给分，步骤依赖是否阻止错误传播，合理省略误扣分是多少，自动覆盖率与人工复核成本如何，以及Lean是否提供独立检错增益。

## 8. 统一结论（供后续对话引用）

四份报告共同显示，直接让LLM依据rubric和少量已评分示例进行逐项评分，在这组数学归纳法数据上取得约83%–91%的观察准确率。qwen3.7-plus在本地160份子集的D配置表现最佳，合并准确率90.8%；deepseek-flash为89.4%；kimi-k2.7-code为88.7%；deepseek-v3.2为83.1%。Qwen相对MathBERT具有相同的TP/FN总数和更少的FP，但尚未完成样本级配对检验。不同模型的错误偏好不同：部分模型更保守、FN较高，另一些模型FP较低但可能增加误扣分。few-shot通常带来较大观察增益，而RAG相对随机示例的增益多为0.3–3.6个百分点，尚不能由单次运行确认稳定优势。四份实验应作为B1/B2开发性基线资料，不应被表述为证据约束、依赖感知或当前论文核心方法已经验证。

## 9. 原始报告完整附录

以下四节逐字保留四份源报告内容。附录中的命令、路径、API参数和项目建议仅是原报告文本。

# 附录A：DEEPSEEK_实验报告.md（原文）
# DeepSeek 生成式批改实验报告

> 在 arXiv:2406.10268 的同一个数据集、同一个测试集上，用 **DeepSeek 直接批改**学生证明，
> 与论文的「冻结嵌入 + 线性分类头」监督式基线做同口径对比。
>
> 复现时间：2026-09-16　|　总花费：**$1.00**　|　总请求：4320 次（含试点与重跑）　|　模型：`deepseek-flash`

---

## 1. 为什么只能走生成式路线

先做了一次实测探测，结论是硬性的：

| 探测项 | 结果 |
|---|---|
| `/models` 可用模型 | `deepseek-flash`、`deepseek-v4-pro` |
| `/embeddings` 端点 | **HTTP 404 —— 不存在** |
| JSON 输出模式 | ✅ 支持（实测 100% 返回合法 JSON） |
| 并发上限 | flash 2500（我们只用 16，非常保守） |

**DeepSeek 没有嵌入接口**，所以它无法替代 MathBERT 扮演"编码器"的角色。
唯一可行的路线就是论文作者硕士论文第二部分的思路：**让 LLM 直接读题面 + 评分标准 + 学生证明，输出判定**。

---

## 2. 实验设计

### 2.1 四个条件（对齐硕士论文第二部分）

| 条件 | 评分标准 | few-shot 示例 | 测的是什么 |
|---|---|---|---|
| **A** | ✗ | 无 | 模型自身的数学判断力 |
| **B** | ✓ | 无 | **rubric 注入的收益** |
| **C** | ✓ | K=4，**随机**抽 | **示例的收益** |
| **D** | ✓ | K=4，**RAG 检索** | **示例选择策略的收益**（RAG vs 随机） |

### 2.2 公平性的三道保证

1. **同一测试集**：与 MathBERT 基线共用 `common.make_split()`（70/15/15，seed=42），
   评估样本完全相同（4 题共 540 份证明 × 7 个评分点 = 3780 个判定）。
2. **无标签泄漏**：few-shot 示例只从训练集检索，`tests/test_step4_deepseek.py`
   里有专门的断言防止测试集样本混进示例（RAG 与随机两条路径都测了）。
3. **同一折叠口径**：要求模型输出 **0/1/2 三档**（与数据集原始人工标注的刻度一致），
   再由脚本按 `2 → 1，0/1 → 0` 折叠 —— 与 step1 处理数据集标签的规则完全相同。
   并且示例里给的标注也是原始 0/1/2，保证提示词内部刻度自洽。

### 2.3 关键工程细节

- **JSON 模式**（`response_format={"type":"json_object"}`）：实测 4320 次请求 100% 返回合法 JSON，解析零失败。
- **关闭思考模式**（`thinking={"type":"disabled"}`）：批改任务不需要推理链，关掉省大量输出 token。
- **全量缓存 + 提示词哈希**：每次响应落 JSONL，缓存键 = (条件, 题目, 行号, **提示词指纹**)。
  提示词一变缓存自动失效，避免"改了方法却拿旧数据下结论"。缓存可无限复跑，不再花钱。

---

## 3. 结果

### 3.0 按论文 Table 2 的格式汇总（可直接放进论文对照）

> 格式与论文 Table 2 完全一致。`Data Size` 是该题完整数据集规模（与论文同一口径），
> Accuracy 都测在各自的 **15% 测试集**上（P1 244 条 / P2 194 条 / P3 52 条 / P4 50 条）。

| Problem | Data Size | Grading Method | Accuracy |
|---|---|---|---|
| **P1** | 1623 | DeepSeek（zero-shot，无评分标准） | 74.3% |
| | | DeepSeek（+ 评分标准） | 80.5% |
| | | DeepSeek（+ 4 个随机示例） | 86.9% |
| | | **DeepSeek（+ 4 个 RAG 示例）** | **87.7%** |
| **P2** | 1288 | DeepSeek（zero-shot，无评分标准） | 71.6% |
| | | DeepSeek（+ 评分标准） | 80.3% |
| | | DeepSeek（+ 4 个随机示例） | 87.5% |
| | | **DeepSeek（+ 4 个 RAG 示例）** | **88.5%** |
| **P3** | 342 | DeepSeek（zero-shot，无评分标准） | 68.7% |
| | | DeepSeek（+ 评分标准） | 77.7% |
| | | DeepSeek（+ 4 个随机示例） | 89.3% |
| | | **DeepSeek（+ 4 个 RAG 示例）** | **90.4%** |
| **P4** | 333 | DeepSeek（zero-shot，无评分标准） | 73.1% |
| | | DeepSeek（+ 评分标准） | 78.3% |
| | | DeepSeek（+ 4 个随机示例） | 88.3% |
| | | **DeepSeek（+ 4 个 RAG 示例）** | **90.3%** |
| **加权平均** | 3586 | DeepSeek（zero-shot，无评分标准） | 72.7% |
| | | DeepSeek（+ 评分标准） | 80.0% |
| | | DeepSeek（+ 4 个随机示例） | 87.5% |
| | | **DeepSeek（+ 4 个 RAG 示例）** | **88.5%** |
| | | *（对照）论文 Llemma7b* | *90.2%* |
| | | *（对照）论文 Llemma34b* | *90.0%* |
| | | *（对照）论文 GPT-3* | *87.9%* |
| | | *（对照）本机复现 MathBERT* | *87.7%* |
| | | *（对照）论文 Human Graders* | *86.6%* |

同时报告 macro-F1（论文未报告，但在类别不平衡时更可信）：

| 条件 | 数据量加权准确率 | 数据量加权 macro-F1 |
|---|---|---|
| DeepSeek zero-shot | 72.7% | 61.5% |
| DeepSeek + rubric | 80.0% | 73.1% |
| DeepSeek + 随机 4-shot | 87.5% | 84.8% |
| **DeepSeek + RAG 4-shot** | **88.5%** | **86.0%** |

**一句话**：DeepSeek 最好的配置（RAG 4-shot）**加权平均 88.5%**，超过了论文报告的 GPT-3（87.9%）
和人类评分员（86.6%），略高于本机复现的 MathBERT（87.7%），但低于 Llemma7b（90.2%）。

### 3.1 四个条件的总体表现

| 条件 | 加权平均准确率 | macro-F1 | 多数类基线 | 超出基线 |
|---|---|---|---|---|
| A　无 rubric | 0.727 | 0.637 | 0.649 | +0.078 |
| B　+ rubric | 0.800 | 0.758 | 0.649 | +0.151 |
| C　+ 随机示例 | 0.875 | 0.861 | 0.649 | +0.226 |
| **D　+ RAG 示例** | **0.885** | **0.873** | 0.649 | **+0.236** |

**四个条件的排序在每一道题上都一致**（P1/P2/P3/P4 全部是 A<B<C<D），不是噪声。

### 3.2 与论文正文的结论对照

| 硕士论文第二部分的结论 | 本次实测 | 是否复现 |
|---|---|---|
| 生成式 LLM 流水线性能超过自建模型 | D 0.885 vs MathBERT 0.876 | ✅ 基本持平、略优 |
| **加入评分标准（rubric）能提升准确率** | 0.727 → 0.800（**+7.3 个百分点**） | ✅ 复现，且是单项收益最大的 |
| **提供已批改示例能提升准确率** | 0.800 → 0.875（+7.5） | ✅ 复现 |
| **RAG 选示例优于随机选** | 0.875 → 0.885（+1.0） | ✅ 复现，但幅度很小 |

### 3.3 与监督式基线的完整对比（同一批 3780 个判定）

| 方法 | acc | 精确率 | 召回率 | macro-F1 | 真值正类比例 | **预测正类比例** |
|---|---|---|---|---|---|---|
| **MathBERT + 线性头** | 0.876 | 0.824 | 0.864 | **0.871** | 0.385 | **0.403** |
| DeepSeek A | 0.727 | 0.971 | 0.298 | 0.637 | 0.385 | 0.118 |
| DeepSeek B | 0.800 | 0.964 | 0.498 | 0.758 | 0.385 | 0.199 |
| DeepSeek C | 0.875 | 0.935 | 0.725 | 0.861 | 0.385 | 0.298 |
| **DeepSeek D** | **0.885** | 0.940 | 0.748 | **0.873** | 0.385 | 0.306 |

混淆矩阵：

| 方法 | TN | FP | FN | TP |
|---|---|---|---|---|
| MathBERT | 2057 | **269** | **198** | 1256 |
| DeepSeek D | 2257 | **69** | **366** | 1088 |

> 🎯 **这是本次实验最有价值的发现，也是光看准确率会错过的东西**：
> 两者的总体准确率和 macro-F1 几乎打平，但**犯错的类型正好相反**：
> - **MathBERT 倾向"宁可误报"**：预测正类比例 0.403，与真值 0.385 几乎一致（校准良好），但误判 269 个；
> - **DeepSeek 倾向"宁可漏报"**：只预测 0.306 为正类，误判只有 MathBERT 的 1/4（69 个），
>   但**漏判 366 个，比 MathBERT 多 85%**。
>
> 对教学产品而言这个差别很重要：**把学生做对的步骤判成错的（漏报），比把错的判成对的更伤信心**。
> 论文 §8.2 里那条学生原声——"自动评分器给了我 75%，让我知道方向是对的，没有灰心"——说的正是这件事。
> 如果照搬 DeepSeek 这个保守倾向，它会系统性地压低学生的分数。

### 3.4 模型的三档判定分布 vs 人工标注

| 来源 | 判 0（缺失/错） | 判 1（部分正确） | 判 2（完全正确） |
|---|---|---|---|
| 人工标注（真值） | 54.3% | **7.4%** | 38.4% |
| DeepSeek B | 59.4% | 19.8% | 20.8% |
| DeepSeek D | 57.0% | 19.2% | 23.8% |

模型用"部分正确"这个档位的频率是人工标注的 **2.6 倍**，而判"完全正确"的频率只有人工的 **约 55%**。
这说明：**模型内部的三档判断与人类评分员的尺度明显不同**，只是折叠成二分类后恰好还能对上。
如果将来要做"部分分"或更细的反馈，这个尺度差异必须显式校准。

### 3.5 运行间波动

A、B 两个条件被完整独立跑了两次，结果差异为：

| 条件 | 第一次 | 第二次 | 差 |
|---|---|---|---|
| A | 0.747 | 0.727 | −2.0 个百分点 |
| B | 0.816 | 0.800 | −1.6 个百分点 |

即 **`temperature=0` 下仍有约 1.6～2.0 个百分点的运行间波动**（MoE 路由等非确定性因素）。
本实验里各条件之间的效应量（+7～+8.5 个百分点）远大于这个噪声，所以结论是稳的；
但若将来要做"±1 个百分点"级别的比较，**必须多次运行取平均并报告区间**。

---

## 4. 成本与耗时

| 项目 | 数值 |
|---|---|
| 总请求数 | 4320 次（540 样本 × 4 条件，跑了 2 轮 + 试点） |
| 输入 tokens | 2,906,539 |
| 输出 tokens | 103,815 |
| **总花费** | **约 $1.00**（非高峰价；高峰翻倍） |
| 单轮全量耗时 | **1 分 39 秒**（并发 16） |
| 单条成本 | 约 $0.00023 |

DeepSeek 非高峰为高峰的一半（高峰 = UTC 周一至周五 01:00–04:00 与 06:00–10:00）。
对比：论文为跑 Llemma34b 用了 **8 张 A100**；这里用 API 花 1 美元、2 分钟就拿到了同量级的结果。

---

## 5. 开发过程中踩的两个坑（方法学教训）

这两个 bug 都属于"不报错、但结论完全错"的类型，值得单独记下来。

### 坑 1：真值被重复折叠 → 指标全废

`prepared.csv` 里的标签**已经是折叠后的 0/1**（step1 做的 `0/1 → 0、2 → 1`）。
评估时我又写了一次 `int(v) >= 2`，于是真值全变成 0：

- 症状：**准确率看着还行（0.73~0.86）、macro-F1 只有 0.44、多数类基线恒等于 1.000**。
- 修法：拆出 `true_labels()`（直接用 prepared.csv 的值）与 `predicted_labels()`（只对 LLM 输出做折叠），职责分离。
- 防回归：`test_true_labels_are_not_double_collapsed` 断言真值正类比例必须是 0.373（P1-R1），一旦退化立刻红灯。

### 坑 2：提示词内部刻度自相矛盾 → 系统性压低 few-shot 条件

提示词要求模型**输出 0/1/2**，但 few-shot 示例给的却是**折叠后的 0/1** 标签。
同一份提示词里两套刻度，模型自然学不到"什么算完全正确"。

| 条件 | 修前 | 修后 | 变化 |
|---|---|---|---|
| A | 0.747 | 0.727 | −0.020 |
| B | 0.816 | 0.800 | −0.016 |
| **C** | 0.800 | **0.875** | **+0.075** |
| **D** | 0.779 | **0.885** | **+0.106** |

**结论完全翻转**：修之前看起来"给示例反而变差"（与硕士论文结论相反），
修之后完全复现了论文的结论（A<B<C<D）。A/B 的微小变化来自运行间波动（见 §3.5）。

- 修法：新增 `load_raw_labels()` 从原始数据集读回 0/1/2 三档标签供示例使用。
- 防回归：`test_make_example_prefers_raw_012_labels` 挑一条"原始为 2、折叠后为 1"的样本，
  断言传了 labels_map 时给出 2、不传时退回 1。

> **共通的教训**：这类实验里，最危险的错误不会抛异常。
> 指标看起来"挺正常"、模型也在返回合法 JSON，但整个结论是错的。
> 两道防线：① 每次都检查**多数类基线**（它一旦等于 1.000 就说明真值退化了）；
> ② 检查**中间产物的分布**（0/1/2 的比例、预测正类比例），而不是只看最终准确率。

---

## 6. 局限（诚实交代）

| 项 | 说明 |
|---|---|
| 只测了 `deepseek-flash` | 未测 `deepseek-v4-pro`（贵 4 倍）。硕士论文的结论是在 GPT-4o 上得出的，模型能力档次不同，结论不能直接迁移 |
| 单次运行 | 未做多种子重复取平均；已实测波动约 ±2 个百分点 |
| K 固定为 4 | 未做 K 的敏感性分析（论文也未指定 K） |
| 未做提示词调优 | 只用了一版提示词；换措辞、换 JSON schema 都可能影响结果 |
| RAG 只用句嵌入相似度 | 未对比 BM25 或其他检索方案 |
| 只在 4 道归纳法题上验证 | 未测其他题型，无法判断能否推广 |
| 成本按非高峰价计 | 高峰时段翻倍 |

---

## 7. 怎么复跑

```bash
# 成本预估，不花钱
python step4_deepseek_grade.py --conditions A,B,C,D --dry-run

# 小样本试点（约 $0.01）
python step4_deepseek_grade.py --conditions B --limit 6

# 全量四条件（约 $0.50 / 100 秒）
python step4_deepseek_grade.py --conditions A,B,C,D --k 4 --concurrency 16

# 与 MathBERT 基线做混淆矩阵级对比（读缓存，不花钱）
python compare_deepseek_vs_mathbert.py

# 测试
python -m pytest -q
```

**API key**：脚本自动按顺序查找
`--api-key` 参数 → 环境变量 `DEEPSEEK_ENV_FILE` 指定的文件 → `ai-math-grader/ai-service/.env`。
（`DEEPSEEK_ENV_FILE` 是**覆盖**语义，一旦指定就只用那个文件。）

---

## 8. 对 ai-math-grader 项目的启示

| 结论 | 怎么用 |
|---|---|
| **rubric 注入是单项收益最大的操作（+7.3 分）** | 你的批改提示词里**必须显式给出评分细则**，不要指望模型自己知道你的判分标准。这一条几乎零成本 |
| **few-shot 示例有用（+7.5 分），RAG 略优于随机（+1.0）** | 值得做，但**不必为 RAG 投入过多**——收益远小于"有没有示例"。你已有的 sympy 结果、历史批改记录都可以直接当示例库 |
| **LLM 倾向保守（漏报多）** | 应用层要加**校准/阈值调节**：如果发现"学生做对了却被判错"的比例偏高，需要给正类降阈值，或显式在提示词里要求"不要过度严格" |
| **LLM 的三档尺度与人类不一致** | 若要给"部分分"，不能直接把 LLM 的 0/1/2 当分数用，必须先做尺度校准 |
| 成本几乎可忽略 | 单条 $0.00023、全量 1 分钟。**可以放心做多条件对比实验**，用 A/B 测试来选提示词 |
| 与符号判定结合 | 你的 sympy 是确定性主判（零成本、零幻觉），LLM 只该用在**符号判不了的部分**：列式是否合理、过程说明是否成立、给提示 |

---

*本报告所有数字均为 2026-09-16 实测，未做估算。原始响应全部缓存在 `deepseek_cache/`，可离线复算。*



---

# 附录B：DEEPSEEK32_实验报告.md（原文）

# deepseek-v3.2 生成式批改实验报告

> 实验对象：`deepseek-v3.2`（经 DashScope 聚合端点调用 —— 官方端点已下线该版本）
> 数据集：论文 [arXiv:2406.10268](https://arxiv.org/abs/2406.10268) 公开的归纳法证明数据集（Harvard Dataverse, DOI `10.7910/DVN/OTRLXF`）
> 口径：与 `deepseek-flash`、`qwen3.8-max`、`qwen3.7-plus`、`kimi-k2.7-code` 四轮实验**同一批测试样本**，可直接横向对比
> 生成时间：2026-09-16
> 实验代码：`step4_deepseek_grade.py`（通用 OpenAI 兼容客户端，通过 `--provider/--model` 切换模型）

---

## 0. 一句话结论

**加权平均 82.9%（最优的 RAG 条件），是本项目五轮实验里最低的一个 —— 明显低于监督式基线 MathBERT 的 87.7%。**

但它最有意思的地方不是分数，而是两件**前面四个模型都没出现过的事**：

| 现象 | 具体表现 | 为什么值得单独说 |
|---|---|---|
| **它自创了第 4 个档位 `-1`** | 提示词只声明 0/1/2，它仍然用 `-1` 表示"该元素完全没出现"，占判定的 **12.3%~20.7%** | 前四个模型从没这么干过。这是**模型对量规的自主扩展**，对生产系统是真问题 |
| **它是全场最保守的评分员** | 精确率 **0.970（全场最高）**、冤枉学生仅 **10 次**；但召回率只有 **0.641（全场最低）**、漏判 **179 次** | 它只在非常有把握时才说"对"，导致大量学生做对的步骤被判错 |
| **RAG 对它几乎无效** | C→D 增益仅 **+0.003**（同批口径）—— 其他三个模型是 +0.008 / +0.015 / +0.036 | 五轮里第一次出现"RAG 不明显优于随机示例" |

> ⚠️ **一个必须前置说明的局限**：V3.2 是 **reasoning-first 模型**，思考模式才是它的正常用法。
> 但本实验**只能在"不思考"模式下跑**——原因见 §2.2，思考模式在 1M token 预算内会因输出截断而崩坏。
> **所以这个 82.9% 很可能低估了 V3.2 的真实水平**，不能等同于它作为推理模型的能力。

---

## 1. 为什么这个模型必须走第三方端点

用户给的模型名是 `deepseek-v3.2`，但**在 DeepSeek 官方端点上这个名字根本不存在**。实测证据：

| 模型名 | 官方端点（api.deepseek.com） | 说明 |
|---|---|---|
| `deepseek-v3.2` | ❌ HTTP 400 | `"The supported API model names are deepseek-flash, deepseek-v4-pro"` |
| `deepseek-chat` | ⚠️ 能调通 | **`model` 字段与 `system_fingerprint` 都和 `deepseek-flash` 完全一致** → 只是别名 |
| `deepseek-reasoner` | ⚠️ 能调通 | 同上，也是 `deepseek-flash` 的别名（差别仅在默认开思考） |

官方文档确认：当前只服务 `deepseek-flash`（= DeepSeek-V4.1-Flash）与 `deepseek-v4-pro`（= DeepSeek-V4-Pro-0813），**V3.2 已从服务列表移除**。

**判定别名的可靠方法**（本次实测总结）：不要看请求回执，要看响应里的 `model` 字段 + `system_fingerprint` —— 别名与真身共享同一个 fingerprint。这个方法已固化成工具 `probe_models.py`。

在 DashScope 聚合端点上，`deepseek-v3.2` 确实存在（同时还有 `deepseek-v3` / `v3.1` / `v4-pro` 等多个版本）。同端点上的 `siliconflow/deepseek-v3.2` 与 `vanchin/deepseek-v3.2-think` 未开通服务，不可用。

---

## 2. 实验设计

### 2.1 四个条件（与前四个模型完全一致）

| 条件 | 评分标准（rubric） | few-shot 示例 | 说明 |
|---|---|---|---|
| **A** | ✗ | 无 | 纯零样本，只给题面和学生证明 |
| **B** | ✓ | 无 | 注入 7 个评分点的判定标准 |
| **C** | ✓ | 4 个**随机**已标注示例 | 从训练集随机抽 |
| **D** | ✓ | 4 个 **RAG 检索**示例 | 用 MathBERT 句嵌入做余弦相似度检索，复用 step2 已有缓存 |

**公平性三道保证**（与前四轮相同）：同一份 `make_split(seed=42)` 划分；同一批每题前 40 条测试样本（共 **1,120 个判定**）；随机与 RAG 两条取示例路径都严格只用训练集。

### 2.2 思考模式的取舍（本次最关键的配置决策）

这一步是本轮花时间最多、也最影响结论的地方。V3.2 的行为与前面所有模型都不同：**它默认不思考**，但一旦开思考就极难控制。

我用真实提示词（B 条件、11 条不同配置）实测了一遍：

| 配置 | 单条耗时 | 输入 | 输出（思考） | JSON 解析 |
|---|---|---|---|---|
| **默认（不思考）** | **1.3s** | 1097 | **36（0）** | ✅ json |
| `enable_thinking=true`（不限额） | 26.2s | 1097 | 1359（1298） | ⚠️ regex 兜底 |
| `enable_thinking=false` | 1.3s | 1097 | 36（0） | ✅ json |
| `thinking={"type":"enabled"}`（不限额） | 21.4s | 1097 | 1124（1064） | ✅ json |
| `enable_thinking=true` + **`budget=256`** | 6.7s | 1097 | 318（257） | ⚠️ **批量下大面积崩坏** |
| `enable_thinking=true` + **`budget=128`** | 4.1s | 1097 | 186（129） | ❌ **解析失败** |
| `thinking_budget=128`（单独传，无 enable） | 0.7s | 12 | 1（0） | — 参数被忽略 |

**结论与决策**：

1. **`thinking_budget` 单独传无效**，必须配合 `enable_thinking=true` 才生效（与 qwen/kimi 的行为不同）；
2. **预算压到 128 会破坏输出**：思考吃掉全部额度，正文来不及输出完整 JSON；
3. **即使给到 256，批量下也大面积崩坏** —— 20 条试点里 16 条失败，输出出现 `69`、`673`、`-100`，甚至把解释句当成了 JSON 的值（详见 §5 坑 1）；
4. **完全不限额度的思考又太贵**：单条 1359 token、26 秒，640 条请求约需 **1.7M token**，超出 1M 预算。

→ **最终采用 V3.2 的默认配置（不思考）**。这也让它与另外两轮"关思考"的实验（`deepseek-flash`、`qwen3.8-max`）在同一大类里；但要注意 `qwen3.7-plus` 与 `kimi` 那两轮是"低强度思考"。

### 2.3 1M token 预算下的样本量设计

按「4 条件 × 每题 40 条 = 640 次请求」设计（与 qwen3.7-plus / kimi 两轮完全同批）。
**实测总用量 851,408 tokens**（输入 819,875 + 输出 31,533），是五轮里最省的一次 —— 因为不思考，单条输出平均只有 **49 token**。

---

## 3. 结果

### 3.1 按论文 Table 2 的格式汇总

| Problem | Data Size | Grading Method | Accuracy |
|---|---|---|---|
| **P1** | 1623 | deepseek-v3.2（零样本，无评分标准） | 71.8% |
| | | deepseek-v3.2（+ 评分标准） | 75.0% |
| | | deepseek-v3.2（+ 4 个随机示例） | 80.7% |
| | | **deepseek-v3.2（+ 4 个 RAG 示例）** | **81.8%** |
| **P2** | 1288 | deepseek-v3.2（零样本，无评分标准） | 68.6% |
| | | deepseek-v3.2（+ 评分标准） | 73.2% |
| | | deepseek-v3.2（+ 4 个随机示例） | 82.1% |
| | | **deepseek-v3.2（+ 4 个 RAG 示例）** | **84.3%** |
| **P3** | 342 | deepseek-v3.2（零样本，无评分标准） | 64.3% |
| | | deepseek-v3.2（+ 评分标准） | 70.0% |
| | | deepseek-v3.2（+ 4 个随机示例） | **84.6%** |
| | | **deepseek-v3.2（+ 4 个 RAG 示例）** | 81.8% ⚠️ |
| **P4** | 333 | deepseek-v3.2（零样本，无评分标准） | 65.0% |
| | | deepseek-v3.2（+ 评分标准） | 75.7% |
| | | deepseek-v3.2（+ 4 个随机示例） | 83.6% |
| | | **deepseek-v3.2（+ 4 个 RAG 示例）** | **84.6%** |
| **加权平均** | 3586 | deepseek-v3.2（零样本，无评分标准） | 69.3% |
| | | deepseek-v3.2（+ 评分标准） | 73.9% |
| | | deepseek-v3.2（+ 4 个随机示例） | 81.9% |
| | | **deepseek-v3.2（+ 4 个 RAG 示例）** | **82.9%** |

> **口径说明**：`Data Size` 用论文原值；Accuracy 测在每题**前 40 条测试样本**上；加权平均按论文口径（题数据量加权）。

**⚠️ 注意 P3：这是五轮实验里第一次"排序被打破"** —— P3 上 `C 随机示例 (84.6%) > D RAG 示例 (81.8%)`。前四轮里 A<B<C<D 从无例外。这个反转说明 **RAG 检索对 V3.2 并非稳定更优**，与 §3.6 的发现一致。

**插进论文原表一起看**：

| Grading Method | P1 | P2 | P3 | P4 | 加权平均 |
|---|---|---|---|---|---|
| MathBERT & linear | 83.6% | 83.7% | 85.7% | 86.3% | 84.1% |
| GPT-3 & linear | 87.9% | 88.3% | 89.2% | 85.4% | 87.9% |
| Llemma7b & linear | 90.7% | 90.9% | 85.7% | 89.9% | **90.2%** |
| Llemma34b & linear | 90.2% | 90.7% | 86.6% | 90.2% | 90.0% |
| Human Graders | 84.1% | 91.3% | 85.1% | 85.8% | 86.6% |
| *本机复现 MathBERT* | *86.8%* | *87.7%* | *90.1%* | *90.0%* | *87.7%* |
| *qwen3.7-plus（RAG）* | *88.6%* | *91.8%* | *92.5%* | *90.4%* | ***90.3%*** |
| *kimi-k2.7-code（RAG）* | *87.5%* | *89.3%* | *88.2%* | *89.6%* | *88.4%* |
| *DeepSeek flash（RAG）* | *87.7%* | *88.5%* | *90.4%* | *90.3%* | *88.5%* |
| **deepseek-v3.2（RAG）** | **81.8%** | **84.3%** | **81.8%** | **84.6%** | **82.9%** |

**82.9% 低于论文报告的所有模型和人类评分员，也低于我们复现的 MathBERT（87.7%）。**

### 3.2 四个条件的总体表现

| 条件 | 加权平均准确率 | macro-F1 | 多数类基线 | 超出基线 |
|---|---|---|---|---|
| A　无 rubric | 0.693 | 0.587 | 0.644 | +0.049 |
| B　+ rubric | 0.739 | 0.663 | 0.644 | +0.096 |
| C　+ 随机示例 | **0.819** | **0.788** | 0.644 | +0.175 |
| **D　+ RAG 示例** | **0.829** | **0.794** | 0.644 | +0.186 |

两个值得注意的点：

1. **它的起点（A 条件 0.693）是五轮里最低的** —— 零样本能力明显弱于 qwen3.7-plus（0.834）和 kimi（0.747）；
2. **net lift 只有 +0.186** —— 却比 kimi（+0.240）、deepseek-flash（+0.236）都小，说明"喂材料"也没能把它拉到同一水平。

### 3.3 六个方法在同一批 1,120 个判定上的对比

（同一批 160 个测试样本 × 7 个评分点；真值正类比例 0.446）

| 方法（最优条件 D，MathBERT 除外） | acc | 精确率 | 召回率 | macro-F1 | FP（冤枉） | FN（漏判） | 预测正类比例 |
|---|---|---|---|---|---|---|---|
| **MathBERT + 线性头** | 0.871 | 0.839 | 0.878 | 0.870 | 84 | 61 | 0.466 ✓ |
| **qwen3.7-plus D** | **0.908** | 0.912 | **0.878** | **0.907** | 42 | **61** | 0.429 ✓ |
| deepseek-flash D | 0.894 | 0.940 | 0.814 | 0.891 | 26 | 93 | 0.386 |
| kimi-k2.7-code D | 0.887 | 0.951 | 0.786 | 0.883 | 20 | 107 | 0.368 |
| **deepseek-v3.2 D** | **0.831** | **0.970** | **0.641** | **0.819** | **10** | **179** | **0.295** |

**V3.2 的"性格"是全场最极端的**：

- **说"对"的时候几乎不会错**：精确率 **0.970**，全场第一；冤枉学生的次数只有 **10 次**，是 MathBERT（84 次）的 **1/8**；
- **但代价是大量漏判**：召回率 **0.641**，全场最低；漏判 **179 次**，接近 MathBERT（61 次）的 **3 倍**；
- **判定尺度严重偏离**：它只把 **29.5%** 的判定标为"对"，而真值其实是 **44.6%**。

对教学产品而言，**把学生做对的步骤判成错的（漏判）比把错的判成对的更伤信心** —— 论文 §8.2 里那条学生原声（"自动评分器给了我 75%，让我知道方向是对的，没有灰心"）说的正是这件事。**V3.2 如果直接上线，会系统性地把学生分数压低一大截。**

### 3.4 三档判定分布：模型自创了第 4 个档位

这是本轮最独特、也最有工程价值的发现。

| 来源 | 判 0 | 判 1 | 判 **2** | 判 **-1**（模型自创） |
|---|---|---|---|---|
| **人工标注（真值）** | 54.3% | **7.4%** | **38.4%** | — |
| deepseek-v3.2 B | 45.1% | 15.8% | 18.4% | **20.7%** |
| **deepseek-v3.2 D** | 47.7% | 10.5% | 29.5% | **12.3%** |
| kimi-k2.7-code B | 55.7% | 22.3% | 22.0% | 0% |
| kimi-k2.7-code D | 51.2% | 12.0% | 36.8% | 0% |
| qwen3.7-plus B | 53.1% | 10.7% | 36.2% | 0% |
| qwen3.7-plus D | 50.9% | 6.2% | **42.9%** | 0% |
| deepseek-flash D | 57.0% | 19.2% | 23.8% | 0% |

**提示词里明确写的是**：

> 「Each value must be an integer: **2** = completely correct; **1** = partially correct; **0** = **missing or wrong**. Do not add any other keys…」

**但 V3.2 仍然用了 `-1`**，而且用得很有系统：它把"元素完全没有出现"和"出现了但写错"区分开来 —— `-1` 给前者、`0` 给后者。这其实是**一个比我们在用的量规更细的语义区分**，只是它没被要求这么做。

- **A/B 条件下 -1 尤其多**（B 条件 20.7%），因为模型拿不准尺度时倾向于保守地说"我没看到这个元素"；
- **给了示例后降到 12.3%** —— 示例在教它"什么算存在"，这与 kimi 那轮"示例的作用是校准刻度"的发现一致。

> 🔍 **对生产系统的直接启示**：**不能假设模型会遵守你声明的枚举值。**
> 如果你的下游逻辑是 `if label == 0 / 1 / 2` 的三分支，遇到 `-1` 就会走到未定义分支；
> 如果是把 label 当数组下标，就会直接越界。
> **必须做显式校验 + 明确的处置策略**，而不是让它悄悄漏过去。

### 3.5 `-1` 归一化的敏感性分析

我们的处置是**把 `-1` 归一化为 `0`** —— 理由：提示词里 `0` 的定义是 "missing **or wrong**"，已经覆盖了 `-1` 的语义；而本实验最终折叠成二分类（只有 `2` 算得分），`-1` 与 `0` 在此口径下完全等价。

但为了不让这个处理悄悄影响结论，我做了敏感性分析（同样本、同口径）：

| 条件 | 全部判定 acc | 剔除 `-1` 后 acc | 差异 | `-1` 占比 | 判定数 |
|---|---|---|---|---|---|
| A | 0.674 | 0.718 | **+0.044** | 15.4% | 1120 |
| B | 0.735 | 0.794 | **+0.059** | 20.7% | 1120 |
| C | 0.828 | 0.889 | **+0.061** | 14.6% | 1120 |
| D | 0.831 | 0.880 | **+0.049** | 12.3% | 1120 |

**怎么读这张表（重要）**：

- 剔除 `-1` 后准确率上升约 **4.4~6.1 个百分点**，说明**这些 `-1` 判定大部分确实判错了**（模型说"没看到"，但真值是有这个元素）；
- **但不能因此说"归一化伤害了它"** —— 剔除 `-1` 相当于只保留了模型比较有把握的样本，是**选择偏差**，剩下的样本本来就更容易；
- 正确的解读是：**`-1` 恰好标记出了模型最没把握的地方，而它在这些地方多数判错**。也就是说这个自创档位是一个**有用的"不确定度信号"** —— 如果产品要加人工复核，优先复核 `-1` 的判定，性价比最高。

### 3.6 五轮实验的收益结构对照（含一条首次被打破的规律）

同一批 1,120 个判定、逐条件增量：

| 模型 | 零样本起点 A | A→B +rubric | B→C +示例 | **C→D +RAG** | A→D 总增益 |
|---|---|---|---|---|---|
| **deepseek-v3.2** | 0.674 | +0.061 | **+0.093** | **+0.003** ⚠️ | +0.157 |
| deepseek-flash | 0.722 | +0.054 | +0.103 | +0.015 | +0.172 |
| kimi-k2.7-code | 0.747 | +0.020 | +0.084 | +0.036 | +0.139 |
| qwen3.7-plus | **0.844** | +0.022 | +0.034 | +0.008 | +0.064 |

**两条规律，一条新发现**：

1. ✅ **前四轮成立的规律再次成立**：**零样本越强，"喂材料"的总收益越小**（起点 0.674 → 总增益 +0.157；起点 0.844 → 仅 +0.064）。
2. ✅ **rubric 与示例都有正收益**，且"给示例"（B→C）依然是单项收益最大的一步。
3. 🆕 **RAG 检索对 V3.2 几乎无效（+0.003）** —— 这是五轮里第一次出现"RAG 不比随机示例更好"。配合 §3.1 里 P3 的排序反转（C > D），可以判断：**V3.2 的收益主要来自"看到示例"这件事本身，而不是"看到最相似的示例"**。

> 这条对选型有直接含义：**如果主力模型是 V3.2 这类较老/较弱的模型，RAG 检索那套工程量可以直接省掉** —— 随机抽 4 个示例就够了。

---

## 4. 成本与用量

| 项目 | 数值 |
|---|---|
| 总请求数 | **640 次**（160 样本 × 4 条件） |
| 成功率 | **640 / 640，0 失败** |
| JSON 解析 | **640 / 640**（`-1` 归一化后；归一化前有 85% 会被拒绝，见 §5 坑 2） |
| 输入 tokens | **819,875** |
| 输出 tokens | **31,533**（无思考模式，平均仅 **49 token/条**） |
| **合计 tokens** | **851,408** |
| 单条均耗 | 输入 1,281 / 输出 49 |
| 总耗时 | **1 分 15 秒**（并发 16） |
| 平均速率 | 约 0.12 秒/条（五轮里最快） |
| 金额 | **[未收录]** 本工程价目表无 `deepseek-v3.2`，故只报 token 用量、不计金额 |

**五轮用量横向对比**：

| 实验 | 请求数 | 总 tokens | 总耗时 | 配置 |
|---|---|---|---|---|
| **deepseek-v3.2** | 640 | **851,408** | **1 分 15 秒** | 不思考 |
| kimi-k2.7-code | 640 | 960,591 | 2 分 44 秒 | 思考预算 128 |
| qwen3.7-plus | 640 | 1,002,240 | 2 分 34 秒 | 思考预算 128 |
| deepseek-flash | 4320 | 2,906,539 | 单轮 1 分 39 秒 | 不思考 |

---

## 5. 开发过程中踩的四个坑（方法学教训）

### 坑 1：思考预算一旦截断，输出会彻底崩坏

`enable_thinking=true` + `thinking_budget=256` 的单条测试看起来是好的（解析成功），但**批量跑起来 20 条里 16 条失败**。失败的原始输出长这样：

- `{"R1": 69, "R2": 0, ...}` → 思考被截断后，模型把"第 69 个 token 位置"之类的中间量当成了答案
- `{"R1": 673, ...}`、`{"R1": -100, ...}`
- `{"R1": "It appears your inductive hypothesis is missing, so partial credit."}` → 直接把解释句当成了值

**教训**：**思考预算不要靠单条测试来验证**。单条成功可能只是运气；截断行为在批量下才暴露，而且失败模式是"值域失控"而不是"报错"。**要验证一个生成配置，至少跑 20 条并统计解析成功率。**

### 坑 2：模型会自创档位，而我的解析器把所有越界值一律拒绝

第一次全量试点：640 条里 **85% 解析失败**。查 raw 才发现输出是**合法 JSON**，只是值是 `-1`：

```json
{"R1": 0, "R2": -1, "R3": 0, "R4": 0, "R5": 0, "R6": 0, "R7": 0}
```

我的解析器写的是 `if v not in (0, 1, 2): raise`，于是全部落进"解析失败"。**而这是我自己设计的严格校验**，本意是防止输出崩坏（坑 1 那类）。

**修法**：接受 `-1` 并归一化为 `0`（语义等价），但**继续拒绝** `3`/`69`/`-100`/字符串 —— 这两类越界必须区别对待。已加测试钉住。

**教训**：**"严格校验"和"容错"需要一个有依据的中间地带。** 一律拒绝会把模型的合理扩展误判成错误；一律接受又会把崩坏输出当成结果放进来。判断依据应该是**语义**（`-1` 是否等价于已有档位），而不是**值域**。

### 坑 3：解析失败的记录被当成"已完成"缓存在案

修好解析器后重跑，日志说"缓存命中 20 条，待请求 0 条"—— 一条都没重试。原因是缓存命中判定没有过滤 `labels` 为空的记录：

```python
done.add((d["problem"], d["row"], d.get("prompt_hash"), d.get("gen")))   # ← 失败记录也被算作已完成
```

**一次解析失败会被永久缓存，之后每次重跑都跳过它**，而且日志里只显示"命中"，没有任何异常。

**修法**：只有解析成功的记录才算已完成。已加测试。

**教训**：**缓存要区分"做过了"和"做对了"。** 只缓存成功结果、失败必须可重试，否则第一个 bug 会被第二个 bug 永久掩盖。

### 坑 4：我自己的临时取数脚本又重复折叠了真值（第三次）

做 `-1` 敏感性分析时，我顺手写了个临时脚本，里面写了 `1 if int(df.at[i, r]) >= 2 else 0`。而 `prepared.csv` 的标签**已经是折叠后的 0/1**，于是真值全部变成 0，准确率算出来是 **0.877** —— 而正确值是 **0.674**。

更糟的是**它看起来完全正常**：0.877 是个合理的准确率，没有任何报错。

**这个坑在本项目已经踩到第三次了**（前两次：step4 早期一次、另一处取数一次）。所以我这次做了根治：

- 在 `common.py` 加了 `truth_binary()` —— 值不是 0/1 就直接抛异常，逼你回去走 step1；
- 加了 `check_truth_not_degenerate()` —— 正类比例若恒为 0.000 或 1.000 就报错；
- 加了 5 个测试用例钉住这两道防线（含一个对真实数据的端到端自检）。

**教训**：**当一个坑重复出现第三次，就不要只修现场了，要加断言。** 这类错误的共同特征是"不报错但结论全错"，唯一的通用防御就是**主动检查中间产物的分布**（这里就是"真值正类比例必须 ≈0.446"）。

---

## 6. 局限（诚实交代）

1. **⚠️ 最重要的一条：思考模式没能启用，V3.2 的真实能力可能被低估。** V3.2 是 reasoning-first 模型（官方文档定位是"为 agent 场景打造的推理优先模型"），而本实验只能在"不思考"下跑。原因是思考模式在 1M 预算内会因输出截断而崩坏（§2.2、§5 坑 1）。**所以这个 82.9% 不应该被解读为"V3.2 不如 kimi/qwen"**，只能说"在 1M token 预算 + 不思考配置下，V3.2 是五轮里最低的"。
2. **每题只评估前 40 条测试样本**（不是全部 540 条），为的是与其他四轮保持同一批样本。方法间比较公平，绝对分数与全量口径会有差异。
3. **只跑了单次，未测运行间波动。** 前面实测过 `temperature=0` 下仍有 1.6~2.0 个百分点的波动。所以 V3.2（0.831）与 kimi（0.887）之间 5.6 个百分点的差距**是真实差异**，但它与更接近的模型之间的小差距不可信。
4. **`-1` 的处置是本实验的一个主观决策。** 我按语义等价归一化为 `0`，并给了敏感性分析（§3.5）。如果你的业务定义不同（例如把 `-1` 当"需要人工复核"），结论会变。
5. **未做 K 值敏感性分析**（`K=4` 固定）。鉴于 V3.2 对示例的依赖方式与其他模型不同（§3.6），它的 K 值曲线值得单独测。
6. **第三方聚合端点的模型版本可能不是官方原版。** DashScope 上的 `deepseek-v3.2` 由谁托管、是否量化、上下文是否被截断，都无法从外部验证。**跨平台比较模型能力时这一点必须计入不确定性。**

---

## 7. 复跑命令

```bash
cd "D:\Study\AI\WorkBuddy_Data\2026-09-16-10-21-15\outputs\repro-2406.10268"

# 先确认凭据与端点（.env.probe 里放第三方端点凭据）
python step4_deepseek_grade.py --provider dashscope --model deepseek-v3.2 \
    --conditions A --limit 5 --dry-run

# 全量四条件（缓存命中则不重复付费）
python step4_deepseek_grade.py --provider dashscope --model deepseek-v3.2 \
    --conditions A,B,C,D --k 4 --limit 40 --concurrency 16 --out-dir ./results_v32

# 六个模型 + MathBERT 在同一批样本上对比（读缓存，不花钱）
python compare_llm_vs_mathbert.py --provider dashscope --model deepseek-v3.2 --limit 40 \
    --all-models "dashscope:kimi-k2.7-code,dashscope:qwen3.7-plus,deepseek:deepseek-flash,dashscope:qwen3.8-max" \
    --out results_v32/compare_same_subset.csv

# 探测任意端点上有哪些模型、某个名字究竟路由到谁
python probe_models.py --provider dashscope --list --grep deepseek
python probe_models.py --provider dashscope --models deepseek-v3.2 --test-thinking

# 测试（76 个用例）
python -m pytest -q
```

---

## 8. 对 ai-math-grader 的启示

### 8.1 结论一：必须校验模型返回的枚举值，不能假设它会遵守

这是本轮最有工程价值的一条。**提示词明确写了"value must be 2/1/0"，V3.2 仍然返回了 `-1`（占 12%~21%）。**

对你的 ai-service 意味着：

- 判分结果落库前，**必须做值域校验**，对越界值有明确策略（拒绝 / 归一化 / 转人工）；
- 三档（0/1/2）或多档反馈的下游逻辑，**不要写 `if label == 0 elif == 1 elif == 2`** —— 加一个 else 分支处理未知值；
- **越界值不要一概丢弃**：`-1` 这种"有系统的越界"往往是模型在表达额外信息（这里是"不确定"），丢掉可惜。

### 8.2 结论二：把"不确定"信号变成复核入口

§3.5 的分析显示，V3.2 自创的 `-1` **恰好标记出了它最没把握的地方，而它在这些地方多数判错**（剔除后准确率上升 4.4~6.1 个百分点）。

对你的产品来说这是个免费的"置信度"信号：**如果模型输出了未被要求的档位、或明确表达了犹豫，就把它标为"待复核"**，优先送人工。这比按随机抽检做复核的效率高得多。

### 8.3 结论三：老模型/弱模型可以省掉 RAG 工程

V3.2 的 C→D 增益只有 **+0.003**（同批口径），P3 上甚至出现 C > D 的反转。而它的 B→C 增益是 +0.093。

**含义**：对这类模型，收益来自"看到示例"这件事本身，**不是来自"检索到最相似的示例"**。所以如果你的主力模型是较老/较弱的模型，**RAG 那套向量检索 + 索引维护的工程量可以直接省掉**，随机抽几个已批改样本就够。

反过来：**只有当模型本身零样本能力够强时，RAG 的边际价值才值得投入** —— 但这个反直觉（强模型 RAG 增益反而更小，见 §3.6）。综合看，**RAG 在这个任务上的性价比整体偏低，建议放到最后再优化。**

### 8.4 结论四：模型版本的"下线"是真实风险，要有兜底

V3.2 已从官方 API 下线，只能走第三方聚合端点。对你的项目意味着：

- **不要在生产代码里硬编码模型名**，要可配置 + 有降级链（例如主 `deepseek-flash` → 备 `qwen3.7-plus`）；
- **上线前用 `probe_models.py` 这类工具确认名字真的可用**，别等运行时报 400；
- 注意**同名模型在不同平台可能不是同一个东西**（§6 第 6 条）。

### 8.5 结论五：先确认配置，再比模型

本轮花了近一半时间在"思考模式怎么配"上，而且**配置错了会让结论完全反过来**（截断的思考模式输出崩坏 80%）。

**给你的操作建议**：任何 LLM 批改实验，按这个顺序做 ——

1. **探测端点与模型名**（`probe_models.py --list`）
2. **用真实提示词测 20 条，统计解析成功率**（不要只测 1 条）
3. **确认 token 用量在预算内**（用 `--dry-run`）
4. **才跑全量**

跳过第 2 步直接跑全量，很可能拿到一份"看起来正常但 85% 是垃圾"的结果。

---

## 附：五轮实验总览

| 报告 | 对象 | 端点 | 最佳条件 | 加权平均 | 同批 acc（D） |
|---|---|---|---|---|---|
| `DEEPSEEK_实验报告.md` | deepseek-flash | 官方 | RAG 4-shot | 88.5% | 0.894 |
| （DEEPSEEK 报告 §3.0.1） | qwen3.8-max | DashScope | —（额度中断） | 85.8%（仅 A/B） | — |
| `QWEN37PLUS_实验报告.md` | qwen3.7-plus | DashScope | RAG 4-shot | **90.3%** 🥇 | **0.908** 🥇 |
| `KIMI27CODE_实验报告.md` | kimi-k2.7-code | DashScope | RAG 4-shot | 88.4% | 0.887 |
| **`DEEPSEEK32_实验报告.md`（本文）** | **deepseek-v3.2** | **DashScope** | **RAG 4-shot** | **82.9%** | **0.831** |
| `README.md` §9 | （汇总章节） | — | — | — | — |

> ⚠️ `step4` 脚本原为 DeepSeek 而写，所以输出文件名固定叫 `deepseek_by_rubric.csv` / `deepseek_summary.csv`。
> **它们只是文件名，与模型无关** —— 读 `results_v32/` 下这两个文件时，内容是 `deepseek-v3.2` 的。



---

# 附录C：KIMI27CODE_实验报告.md（原文）

# kimi-k2.7-code 生成式批改实验报告

> 实验对象：`kimi-k2.7-code`（DashScope OpenAI 兼容端点）
> 数据集：论文 [arXiv:2406.10268](https://arxiv.org/abs/2406.10268) 公开的归纳法证明数据集（Harvard Dataverse, DOI `10.7910/DVN/OTRLXF`）
> 口径：与 `qwen3.7-plus`、`qwen3.8-max`、`deepseek-flash` 三轮实验**完全一致**，可直接横向对比
> 生成时间：2026-09-16
> 实验代码：`step4_deepseek_grade.py`（通用 OpenAI 兼容客户端，通过 `--provider/--model` 切换模型）

---

## 0. 一句话结论

**最优配置（rubric + 4 个 RAG 示例）加权平均 88.4%，排在本项目所有已测方案的第二位**，仅次于 qwen3.7-plus（90.3%），略低于我们复现的监督式基线 MathBERT（87.7%）之上、接近 DeepSeek（88.5%）。

但它最有价值的不是这个分数，而是**它代表了一种和前面所有模型都不同的"性格"**：

| 维度 | kimi-k2.7-code 的表现 | 全场排名 |
|---|---|---|
| 精确率（说"对"的可信度） | **0.951** | **🥇 第一**（MathBERT 0.839、qwen3.7-plus 0.913） |
| 冤枉学生次数（FP） | **20 次 / 1120 判定** | 前三名（MathBERT 84 次） |
| 漏判次数（FN） | **107 次 / 1120 判定** | 偏多（MathBERT 61、qwen3.7-plus 61） |
| 对 RAG 检索的敏感度（C→D 增益） | **+3.6 个百分点** | **🥇 第一**（其余模型 0.8~1.5） |
| 零样本能力（条件 A） | 72.9% | 偏弱，与 DeepSeek 同档 |

换句话说：**它几乎不冤枉学生，但经常漏放水；而且它是四个 LLM 里最"吃" RAG 示例的。**

---

## 1. 为什么走生成式路线

`kimi-k2.7-code` 同样**没有 `/embeddings` 接口**（该端点只提供 chat/completions），所以它无法替代论文里 MathBERT 的编码器角色，只能走作者硕士论文第二部分的**生成式批改**路线：让模型直接读题面 + 评分标准 + 学生证明，输出每个评分点的判定。

---

## 2. 实验设计

### 2.1 四个条件（与前三个模型完全一致）

| 条件 | 评分标准（rubric） | few-shot 示例 | 说明 |
|---|---|---|---|
| **A** | ✗ | 无 | 纯零样本，只给题面和学生证明 |
| **B** | ✓ | 无 | 注入 7 个评分点的判定标准 |
| **C** | ✓ | 4 个**随机**已标注示例 | 从训练集随机抽 |
| **D** | ✓ | 4 个 **RAG 检索**示例 | 用 MathBERT 句嵌入做余弦相似度检索，复用 step2 已有缓存 |

**公平性的三道保证**（与前三轮完全相同）：

1. **同一份数据划分**：统一走 `common.make_split()`，`seed=42`，70/15/15。P1 的测试集永远是同一批 244 条，不会漂移；
2. **同一批测试样本**：`--limit 40`，每题取测试集前 40 条 → 160 样本 × 7 评分点 = **1,120 个判定**，与 qwen3.7-plus 那轮完全同批；
3. **防标签泄漏**：随机与 RAG 两条取示例的路径都只从训练集行里挑，有专门的测试用例钉住（`test_step4_deepseek.py`）。

### 2.2 思考强度的选择（本次的关键配置决策）

`kimi-k2.7-code` 与 qwen 系列有个**重要差别**：它是**强制思考模型，关不掉**。我把所有常见参数都实测了一遍（同一道题、同一个提示词）：

| 配置 | 单条耗时 | 输出 tokens（其中思考） | 是否生效 |
|---|---|---|---|
| 默认（不传参数） | 7.3s | 308（258） | — |
| `enable_thinking=false` | 8.4s | 490（**440**） | ❌ **无效，反而更多** |
| `thinking={type:"disabled"}` | 8.5s | 490（**440**） | ❌ **无效** |
| `reasoning_effort="low"` | 9.1s | 477（**440**） | ❌ **无效** |
| `thinking_budget=128` | **4.5s** | **178（128）** | ✅ **生效** |
| `enable_thinking=true` + `thinking_budget=128` | **4.2s** | **178（128）** | ✅ 生效 |

**结论：只有 `thinking_budget` 能压住 kimi 的思考量**。前三个参数不报错、静默失效，是本轮最容易踩的坑——如果不实测 token 数，会以为"关掉思考省了钱"，其实一个 token 都没省。

本轮统一用 `--thinking --thinking-budget 128`（低强度思考），与 qwen3.7-plus 那轮的配置对齐。

### 2.3 1M token 预算下的样本量设计

按「4 条件 × 每题 40 条 = 640 次请求」设计。dry-run 预估输入 ≈0.91M、输出 ≈0.04M；**实测总用量 960,591 tokens**，在 1M 预算内（比 qwen3.7-plus 那轮的 1,002,240 还省 4%——kimi 的输出更短）。

---

## 3. 结果

### 3.1 按论文 Table 2 的格式汇总

| Problem | Data Size | Grading Method | Accuracy |
|---|---|---|---|
| **P1** | 1623 | kimi-k2.7-code（零样本，无评分标准） | 72.5% |
| | | kimi-k2.7-code（+ 评分标准） | 78.2% |
| | | kimi-k2.7-code（+ 4 个随机示例） | 81.8% |
| | | **kimi-k2.7-code（+ 4 个 RAG 示例）** | **87.5%** |
| **P2** | 1288 | kimi-k2.7-code（零样本，无评分标准） | 70.7% |
| | | kimi-k2.7-code（+ 评分标准） | 72.1% |
| | | kimi-k2.7-code（+ 4 个随机示例） | 86.1% |
| | | **kimi-k2.7-code（+ 4 个 RAG 示例）** | **89.3%** |
| **P3** | 342 | kimi-k2.7-code（零样本，无评分标准） | 76.1% |
| | | kimi-k2.7-code（+ 评分标准） | 76.4% |
| | | kimi-k2.7-code（+ 4 个随机示例） | 85.4% |
| | | **kimi-k2.7-code（+ 4 个 RAG 示例）** | **88.2%** |
| **P4** | 333 | kimi-k2.7-code（零样本，无评分标准） | 79.6% |
| | | kimi-k2.7-code（+ 评分标准） | 80.0% |
| | | kimi-k2.7-code（+ 4 个随机示例） | 87.1% |
| | | **kimi-k2.7-code（+ 4 个 RAG 示例）** | **89.6%** |
| **加权平均** | 3586 | kimi-k2.7-code（零样本，无评分标准） | 72.9% |
| | | kimi-k2.7-code（+ 评分标准） | 76.0% |
| | | kimi-k2.7-code（+ 4 个随机示例） | 84.2% |
| | | **kimi-k2.7-code（+ 4 个 RAG 示例）** | **88.4%** |

> **口径说明**：`Data Size` 用论文原值（该题完整数据规模）；Accuracy 测在每题**前 40 条测试样本**上（P1–P4 各 40 条），加权平均按论文口径（题数据量加权）。
> 这里没有 GPT-3 / Llemma 那些模型——**kimi 替代的是"评分方法"这一列**。

**插进论文原表一起看**：

| Grading Method | P1 | P2 | P3 | P4 | 加权平均 |
|---|---|---|---|---|---|
| MathBERT & linear | 83.6% | 83.7% | 85.7% | 86.3% | 84.1% |
| GPT-3 & linear | 87.9% | 88.3% | 89.2% | 85.4% | 87.9% |
| Llemma7b & linear | 90.7% | 90.9% | 85.7% | 89.9% | **90.2%** |
| Llemma34b & linear | 90.2% | 90.7% | 86.6% | 90.2% | 90.0% |
| Human Graders | 84.1% | 91.3% | 85.1% | 85.8% | 86.6% |
| *本机复现 MathBERT* | *86.8%* | *87.7%* | *90.1%* | *90.0%* | *87.7%* |
| *DeepSeek flash（RAG）* | *87.7%* | *88.5%* | *90.4%* | *90.3%* | *88.5%* |
| *qwen3.7-plus（RAG）* | *88.6%* | *91.8%* | *92.5%* | *90.4%* | ***90.3%*** |
| **kimi-k2.7-code（RAG）** | **87.5%** | **89.3%** | **88.2%** | **89.6%** | **88.4%** |

kimi 的 88.4% **超过论文里的 GPT-3（87.9%）和人类评分员（86.6%）**，与 DeepSeek（88.5%）基本持平，但**低于 Llemma7b（90.2%）和 qwen3.7-plus（90.3%）**。

### 3.2 四个条件的总体表现

| 条件 | 加权平均准确率 | macro-F1 | 多数类基线 | 超出基线 |
|---|---|---|---|---|
| A　无 rubric | 0.729 | 0.689 | 0.644 | +0.085 |
| B　+ rubric | 0.760 | 0.705 | 0.644 | +0.117 |
| C　+ 随机示例 | 0.842 | 0.824 | 0.644 | +0.198 |
| **D　+ RAG 示例** | **0.884** | **0.870** | 0.644 | **+0.240** |

**四个条件的排序 A<B<C<D 在四道题上全部成立**（P1/P2/P3/P4 无一例外），不是噪声。

不过要留意一个细节：**C→D 的跨度是不均匀的**。P1 涨 5.7 分、P2 涨 3.2 分、P3 涨 2.8 分、P4 涨 2.5 分——**题目越难（P1 最难，1623 条样本分布最杂），RAG 检索的收益越大**。

### 3.3 与硕士论文结论的对照

| 硕士论文第二部分的结论 | 本次实测（加权平均口径） | 是否复现 |
|---|---|---|
| 生成式 LLM 流水线性能超过自建模型 | D 0.884 vs MathBERT 0.877 | ✅ 基本持平、略优 |
| **加入评分标准（rubric）能提升准确率** | 0.729 → 0.760（**+3.1 个百分点**） | ✅ 复现，但幅度明显小于 DeepSeek（+7.3） |
| **提供已批改示例能提升准确率** | 0.760 → 0.842（**+8.2**） | ✅ 复现，是 kimi 单项收益最大的 |
| **RAG 选示例优于随机选** | 0.842 → 0.884（**+4.2**） | ✅ 复现，且是四模型中幅度最大的 |

> 💡 **与 DeepSeek 那轮的关键差异**：DeepSeek 的收益主要在"给评分标准"（+7.3），
> 而 kimi 的收益主要在"给示例"（+8.2）和"RAG 而非随机"（+4.2）。
> 这说明**同样的提示词工程手段，对不同模型的杠杆率完全不同**——不能照搬调优顺序。

### 3.4 五个方法在同一批 1,120 个判定上的对比

（同一批 160 个测试样本 × 7 个评分点；真值正类比例 0.446）

| 方法 | acc | 精确率 | 召回率 | macro-F1 | FP（冤枉） | FN（漏判） | 预测正类比例 |
|---|---|---|---|---|---|---|---|
| **MathBERT + 线性头** | 0.871 | 0.839 | 0.878 | 0.870 | 84 | 61 | 0.466 ✓ |
| kimi-k2.7-code A | 0.747 | 0.943 | 0.461 | 0.715 | 14 | 269 | 0.218 |
| kimi-k2.7-code B | 0.767 | **0.984** | 0.485 | 0.738 | **4** | 257 | 0.220 |
| kimi-k2.7-code C | 0.851 | 0.964 | 0.691 | 0.842 | 13 | 154 | 0.320 |
| **kimi-k2.7-code D** | **0.887** | **0.951** | 0.786 | **0.883** | **20** | 107 | 0.368 |
| deepseek-flash D | 0.894 | 0.940 | 0.814 | 0.891 | 26 | 93 | 0.386 |
| qwen3.8-max B | 0.860 | 0.941 | 0.731 | 0.853 | 23 | 134 | 0.346 |
| **qwen3.7-plus D** | **0.908** | 0.913 | **0.878** | **0.907** | 42 | **61** | 0.429 |

> **口径**：这一批是"1,120 个判定合并计算"（微观平均），与 §3.1 的"题数据量加权"略有差异（kimi D 在这里是 88.7%，加权口径 88.4%）。两者都对，只是权重不同。

**一句话读懂这张表**：

- **kimi 是全场最"严"的评分员**。它说"对"的时候有 **95.1%** 概率真的对（精确率全场第一），B 条件甚至达到 98.4%；但代价是**只敢把 36.8% 的判定标为"对"**，而真值其实是 44.6%。
- **它冤枉学生的次数极少**（D 条件 20 次，只有 MathBERT 的 1/4），但**漏放水的次数是 MathBERT 的 1.75 倍**（107 vs 61）。
- 对比 qwen3.7-plus D：acc 相同量级，但 qwen3.7-plus 的 FP/FN 更平衡（42/61），且它的 TP 是 438，kimi 只有 392 —— **kimi 少认出了 46 个学生确实做对的步骤**。

对教学产品而言这条很重要：把学生做对的步骤判成错的（漏判）**比**把错的判成对的更伤信心。论文 §8.2 里那条学生原声——"自动评分器给了我 75%，让我知道方向是对的，没有灰心"——说的正是这件事。**kimi 这个保守倾向如果直接上线，会系统性压低学生分数。**

### 3.5 模型的三档判定分布 vs 人工标注

| 来源 | 判 0（缺失/错） | 判 1（部分正确） | 判 2（完全正确） |
|---|---|---|---|
| **人工标注（真值）** | 54.3% | **7.4%** | **38.4%** |
| kimi-k2.7-code B | 55.7% | 22.3% | 22.0% |
| **kimi-k2.7-code D** | **51.2%** | **12.0%** | **36.8%** |
| qwen3.7-plus B | 53.1% | 10.7% | 36.2% |
| qwen3.7-plus D | 50.9% | **6.2%** | **42.9%** |
| deepseek-flash D | 57.0% | 19.2% | 23.8% |

**这是本轮最有意思的发现之一**：

- kimi 在 **B 条件**下严重偏离人类尺度——判"完全正确"只有 22.0%（人工 38.4%），判"部分正确"是人工的 **3 倍**。这就是它零样本/GPT 准确率偏低的原因：**它把很多"完全正确"降级成了"部分正确"，而折叠成二分类时（只有 2 才算对）这些全变成"判错"**。
- 但**给了 RAG 示例（D 条件）后，它的三档尺度几乎完全校准到人类水平**：36.8% vs 人工 38.4%，差 1.6 个百分点——**是四个 LLM 里三档尺度最接近人工的**。
- 相比之下 qwen3.7-plus D 判"完全正确"是 42.9%（比人工高 4.5 个百分点），略偏宽松。

> 🔍 **一个方法论提醒**：这条规律说明 few-shot 示例的**真正作用可能不是"教规则"，而是"校准刻度"**。
> kimi 的 B→C 一下子涨了 8.2 分，很可能主要来自"看到示例后才明白什么样的答案算 2 分"。
> 对你的项目意味着：**如果要做部分分或分档反馈，示例的价值会比 rubric 说明更大**。

### 3.6 四个模型的收益结构对比（同一子集）

| 增量 | kimi-k2.7-code | deepseek-flash | qwen3.7-plus |
|---|---|---|---|
| 零样本起点（A） | 0.747 | 0.722 | **0.844** |
| A→B　+评分标准 | +2.0 | +5.4 | +2.2 |
| B→C　+示例 | **+8.4** | **+10.3** | +3.4 |
| C→D　+RAG 而非随机 | **+3.6** | +1.5 | +0.8 |
| **A→D　总收益** | +13.9 | **+17.2** | +6.4 |

**两条规律值得记住**：

1. **零样本越强的模型，"喂材料"的总收益越小**：qwen3.7-plus 起点 0.844，总增益只有 +6.4；kimi 起点 0.747，总增益 +13.9；DeepSeek 起点 0.722，总增益 +17.2。
2. **kimi 对 RAG 的敏感度是其他模型的 2~4 倍**（+3.6 vs +1.5 / +0.8）。这是它唯一的"第一名"——如果你要用 kimi 系模型做批改，**RAG 示例库不是可选项，是必需项**。

---

## 4. 成本与用量

| 项目 | 数值 |
|---|---|
| 总请求数 | **640 次**（160 样本 × 4 条件） |
| 成功率 | **640 / 640，0 失败** |
| JSON 解析 | **640 / 640 走 JSON 模式，7 键齐全率 100%**（零正则兜底） |
| 输入 tokens | **854,625** |
| 输出 tokens | **105,966**（其中思考 **78,606**，占 74%） |
| **合计 tokens** | **960,591** |
| 单条均耗 | 输入 1,335 / 输出 166 |
| 总耗时 | **2 分 44 秒**（并发 16） |
| 平均速率 | 约 0.27 秒/条 |
| 金额 | **[未收录]** 本工程价目表无 `kimi-k2.7-code`，故只报 token 用量、不计金额 |

**与 qwen3.7-plus 的用量对照**：

| 指标 | kimi-k2.7-code | qwen3.7-plus | 差 |
|---|---|---|---|
| 总 tokens | 960,591 | 1,002,240 | kimi 省 4.2% |
| 单条输入 | 1,335 | 1,377 | −3.0% |
| 单条输出 | 166 | 189 | −12.2% |
| 总耗时 | 2 分 44 秒 | 2 分 34 秒 | 基本持平 |

> **格式稳定性值得一提**：kimi-k2.7-code 是**代码专用模型**，640 次请求里 **JSON 模式 100% 命中、7 个键全部齐全、没有一次需要正则兜底**。
> 这比 DeepSeek 和 qwen 都干净（那两个虽然也基本全中，但零星出现过需要兜底的情况）。
> 结构化输出可靠性是这个模型在批改任务上唯一的"先天优势"。

---

## 5. 局限（诚实交代）

1. **`kimi-k2.7-code` 是代码专用模型，不是通用对话模型，也不是数学模型。** 拿它做数学证明批改属于**跨任务使用**。所以它的成绩**不能代表 kimi 通用模型的能力**——这个 key 上还有 `kimi-k3`、`kimi/kimi-k2.8-preview` 可用，如果你想看 kimi 系真实水平，应该再跑那几个。
2. **每题只评估前 40 条测试样本**（不是全部 540 条），这是为了与 qwen3.7-plus 那轮保持"同一批样本"以便横向对比。绝对分数会与全量口径略有差异，但**方法间比较是公平的**。
3. **只跑了单次，未测运行间波动**。DeepSeek 那轮实测过 `temperature=0` 下仍有 1.6~2.0 个百分点的运行间波动；kimi 的波动未测。因此 kimi 与 DeepSeek 之间 **0.3 个百分点的差距（88.4% vs 88.5%）不可视为真实差异**。
4. **思考无法完全关闭**。`thinking_budget` 最低压到 128，实测思考 token 仍占输出总量的 74%。如果想进一步降本，只能换模型。
5. **未做 K 值敏感性分析**（`K=4` 固定）。示例数量对 kimi 的影响未知——鉴于它对示例最敏感（B→C 增益最大），**这个模型的 K 值曲线很可能是四个模型里最陡的**，值得单独测。
6. **`reasoning_effort` 等参数静默失效**——本报告已记录，但这也说明**服务端参数兼容性需要用 token 数逐个实测**，不能只看文档。

---

## 6. 复跑命令

```bash
cd "D:\Study\AI\WorkBuddy_Data\2026-09-16-10-21-15\outputs\repro-2406.10268"

# 成本预估（不花钱）
python step4_deepseek_grade.py --provider dashscope --model kimi-k2.7-code \
    --thinking --thinking-budget 128 --conditions A,B,C,D --k 4 --limit 40 --dry-run

# 全量跑（缓存会跳过已完成的 640 条，不会重复付费）
python step4_deepseek_grade.py --provider dashscope --model kimi-k2.7-code \
    --thinking --thinking-budget 128 --conditions A,B,C,D --k 4 --limit 40 \
    --concurrency 16 --out-dir ./results_kimi27

# 四个 LLM + MathBERT 在同一批样本上的对比（读缓存，不花钱）
python compare_llm_vs_mathbert.py --provider dashscope --model kimi-k2.7-code --limit 40 \
    --all-models "deepseek:deepseek-flash,dashscope:qwen3.8-max,dashscope:qwen3.7-plus" \
    --out results_kimi27/compare_same_subset.csv

# 测试（71 个用例）
python -m pytest -q
```

---

## 7. 对 ai-math-grader 项目的启示

### 7.1 结论一：强模型可以省掉 RAG，弱模型必须做

这是本轮最实用的一条。把四个模型的 C→D 增益并排看：

| 模型 | 零样本起点 | C→D 增益（RAG 的价值） |
|---|---|---|
| qwen3.7-plus | 0.844 | +0.8 |
| deepseek-flash | 0.722 | +1.5 |
| **kimi-k2.7-code** | 0.747 | **+3.6** |

**模型越弱、越依赖示例来"校准刻度"，RAG 的价值就越大。** 对你的选型决策意味着：

- 如果主力模型选 qwen3.7-plus 这类零样本强模型 → **RAG 那套检索工程可以先不做**，省下的开发量很可观；
- 如果为了省钱选便宜模型 → **RAG 必须做**，否则准确率会掉 3~4 个百分点。

### 7.2 结论二：示例的核心作用是"校准刻度"，不只是"教规则"

kimi 的数据给了很直接的证据：**B 条件下它判"完全正确"只有 22.0%（人工 38.4%），给示例后变成 36.8%，几乎精确对齐人工**。

对你的项目意味着：**如果你打算输出"部分正确"这种中间档或者分档反馈，那示例库的价值会远大于评分细则的文字说明**。光写规则不够，模型需要看到"什么叫 2 分"的实例。

### 7.3 结论三：kimi 这个"极严"倾向不能直接上线

kimi D 条件：FP 只有 20 次（全场最低），但 **FN 107 次**，比 MathBERT 多 75%，比 qwen3.7-plus 多 75%。**它少认出了 46 个学生确实做对的步骤。**

这正好印证了论文 §8.2 的学生反馈：**把学生做对的步骤判成错的，比把错的判成对的更伤信任**。如果要用 kimi 系模型，必须做保守度校准：

- 在提示词里明确"学生用不同写法但逻辑等价即算对"；
- 或者对"判错"的决定加一个复核阈值（例如要求模型给出判定理由，理由不充分就升档）。

### 7.4 结论四：结构化输出可靠性可以作为选型的加分项

kimi-k2.7-code 在 640 次请求里 **JSON 100% 命中、7 键齐全率 100%、零兜底**。对生产系统来说，"不会返回脏格式"本身就有价值——省掉一整层解析容错的复杂度。

不过要注意：这是**代码模型**的特性（训练数据里结构化内容多），换成通用模型未必保持。

### 7.5 结论五：参数兼容性必须实测，不能信文档

本轮实测出三种静默失效的参数组合（`enable_thinking=false` / `thinking={type:disabled}` / `reasoning_effort=low`）——**它们不报错，但完全不生效**。如果按文档写代码，会以为省了成本，实际一个 token 都没省。

**教训**：接入任何模型的"降本参数"后，**第一件事是量 token 数确认它真的生效了**。

---

## 附：与另外三份报告的关系

| 报告 | 对象 | 最佳条件 | 加权平均 |
|---|---|---|---|
| `DEEPSEEK_实验报告.md` | deepseek-flash | RAG 4-shot | 88.5% |
| `QWEN37PLUS_实验报告.md` | qwen3.7-plus | RAG 4-shot | **90.3%** |
| **`KIMI27CODE_实验报告.md`（本文）** | **kimi-k2.7-code** | **RAG 4-shot** | **88.4%** |
| `README.md` §9 | （汇总章节） | — | — |

> ⚠️ 那个把算法叫 `deepseek-flash` 的历史遗留问题：`step4` 脚本原为 DeepSeek 而写，所以输出文件名固定叫 `deepseek_by_rubric.csv` / `deepseek_summary.csv`。
> **它们现在只是文件名，与模型无关**——读 `results_kimi27/` 下的这两个文件时，内容是 kimi-k2.7-code 的。



---

# 附录D：QWEN37PLUS_实验报告.md（原文）

# qwen3.7-plus 生成式批改实验报告

> 在 arXiv:2406.10268 的同一个数据集、同一个测试集上，用 **qwen3.7-plus（低思考强度）** 直接批改学生证明，
> 与论文的「冻结嵌入 + 线性分类头」监督式基线、以及此前跑过的 DeepSeek / qwen3.8-max 做同口径对比。
>
> 复现时间：2026-09-16　|　模型：`qwen3.7-plus`（DashScope OpenAI 兼容端点）
> 生成配置：`enable_thinking = true`，`thinking_budget = 128`（低强度思考）
> Token 用量：**1,002,240**（输入 881,459 + 输出 120,781）　|　请求数：640　|　耗时：2 分 34 秒
>
> 格式参照 [`DEEPSEEK_实验报告.md`](./DEEPSEEK_实验报告.md)。

---

## 0. 一句话结论

**qwen3.7-plus 在低思考强度下拿到加权平均 90.3%，是本次所有方案里最好的结果** ——
超过论文的 Llemma34b（90.0%）、GPT-3（87.9%）、人类评分员（86.6%），
也超过本机复现的 MathBERT（87.7%）和另外两个 LLM。
而且它的**判定尺度校准得最好**（预测正类比例 42.9% vs 真值 44.6%），
不像 DeepSeek 那样严重偏保守（17.5% vs 44.6%）。

---

## 1. 为什么走生成式路线

沿用此前的结论：**DeepSeek 与 DashScope 都没有可用的 `/embeddings` 接口**，
无法替代 MathBERT 扮演"编码器"角色，所以只能是让 LLM 直接读题面 + 评分标准 + 学生证明后输出判定。

## 2. 实验设计

### 2.1 四个条件（与前两个模型完全一致）

| 条件 | 评分标准 | few-shot 示例 | 测的是什么 |
|---|---|---|---|
| **A** | ✗ | 无 | 模型自身的数学判断力 |
| **B** | ✓ | 无 | rubric 注入的收益 |
| **C** | ✓ | K=4，随机抽 | 示例的收益 |
| **D** | ✓ | K=4，RAG 检索 | 示例选择策略的收益（RAG vs 随机） |

### 2.2 思考强度的选择（本次的关键配置决策）

DashScope 上的 Qwen3 系列**默认开启思考模式**。对着真实批改提示词实测（单条）：

| 配置 | 单条输入 | 单条输出（含思考） | 单条耗时 | 是否可用 |
|---|---|---|---|---|
| 不传参数（默认） | 1,208 | 2,251（思考 2,191） | 30 秒 | ✅ 但太贵太慢 |
| **`thinking_budget = 128`** | 1,208 | **188**（思考 128） | **3.6 秒** | ✅ **本项目采用** |
| `thinking_budget = 512` | 1,208 | 572（思考 512） | 8.4 秒 | ✅ |
| `enable_thinking = false` | 1,210 | 65（无思考） | 2 秒 | ✅ |
| `reasoning_effort = "low"` | 1,208 | 2,352（思考 2,352） | 31 秒 | ❌ **被端点静默忽略** |

> ⚠️ **踩坑提醒**：`reasoning_effort=low` 在本端点**完全无效**（不报错，但和默认一模一样）。
> 想压低思考量必须用 `thinking_budget`（正整数，上限 262144；传 0 会报 400）。
> 这一点如果不知道，会以为"已经调低了"而实际仍按最高档在跑。

### 2.3 1M token 预算下的样本量设计

用 `thinking_budget=128` 时每条约 1,400 token（输入 1,208 + 输出 188）。要在 1M 内跑满四个条件：

```
每条件样本数 × 4 题 × 4 条件 × 1,400 token ≤ 1,000,000
→ 每条件样本数 ≤ 44
```

因此取 **每题 40 条测试样本**（P1/P2 各取测试集前 40 条，P3/P4 的测试集本身只有 52/50 条，
所以 40 条已覆盖其 77~80%）→ 共 160 个样本、1,120 个评分点判定、640 次请求。

**实测用量：输入 881,459 + 输出 120,781 = 1,002,240 token**，正好压在 1M 线上（超出 0.2%）。

> 📌 由于样本量与前两个模型不同，**跨模型的比较一律改用"每题 40 条"的同一批样本**重算
> （见 §3.3），而不是拿各自的原始数字直接比。DeepSeek 的缓存是完整的，所以它的子集结果不需要重新付费。

---

## 3. 结果

### 3.1 按论文 Table 2 的格式汇总

> `Data Size` 是该题完整数据集规模（与论文同一口径）；
> Accuracy 均测在 **每题 40 条**的测试子集上（P1 40、P2 40、P3 40、P4 40）。

| Problem | Data Size | Grading Method | Accuracy |
|---|---|---|---|
| **P1** | 1623 | qwen3.7-plus（zero-shot，无评分标准） | 78.6% |
| | | qwen3.7-plus（+ 评分标准） | 84.6% |
| | | qwen3.7-plus（+ 4 个随机示例） | 86.4% |
| | | **qwen3.7-plus（+ 4 个 RAG 示例）** | **88.6%** |
| **P2** | 1288 | qwen3.7-plus（zero-shot，无评分标准） | 88.6% |
| | | qwen3.7-plus（+ 评分标准） | 89.3% |
| | | qwen3.7-plus（+ 4 个随机示例） | 91.1% |
| | | **qwen3.7-plus（+ 4 个 RAG 示例）** | **91.8%** |
| **P3** | 342 | qwen3.7-plus（zero-shot，无评分标准） | 84.3% |
| | | qwen3.7-plus（+ 评分标准） | 85.4% |
| | | qwen3.7-plus（+ 4 个随机示例） | 92.1% |
| | | **qwen3.7-plus（+ 4 个 RAG 示例）** | **92.5%** |
| **P4** | 333 | qwen3.7-plus（zero-shot，无评分标准） | 86.1% |
| | | qwen3.7-plus（+ 评分标准） | 87.1% |
| | | qwen3.7-plus（+ 4 个随机示例） | 90.4% |
| | | **qwen3.7-plus（+ 4 个 RAG 示例）** | **90.4%** |
| **加权平均** | 3586 | qwen3.7-plus（zero-shot，无评分标准） | 83.4% |
| | | qwen3.7-plus（+ 评分标准） | 86.6% |
| | | qwen3.7-plus（+ 4 个随机示例） | 89.0% |
| | | **qwen3.7-plus（+ 4 个 RAG 示例）** | **90.3%** |

排序 **A < B < C < D 在四道题上全部成立**，与硕士论文的三条结论一致。

同时报告 macro-F1（论文未报告，在类别不平衡时更可信）：

| 条件 | 加权准确率 | 加权 macro-F1 | 多数类基线 | 超出基线 |
|---|---|---|---|---|
| A　无评分标准 | 83.4% | 81.9% | 64.4% | +19.0 |
| B　+ 评分标准 | 86.6% | 84.8% | 64.4% | +22.2 |
| C　+ 随机 4-shot | 89.0% | 88.7% | 64.4% | +24.6 |
| **D　+ RAG 4-shot** | **90.3%** | **89.6%** | 64.4% | **+25.9** |

### 3.2 按论文 Table 3 的格式（条件 D 分评分点）

| 题目 | R1 | R2 | R3 | R4 | R5 | R6 | R7 |
|---|---|---|---|---|---|---|---|
| P1 | 87.5 | 82.5 | 90.0 | 87.5 | 85.0 | 92.5 | **95.0** |
| P2 | 92.5 | 92.5 | 90.0 | 92.5 | 92.5 | 90.0 | 92.5 |
| P3 | 92.5 | 90.0 | 87.5 | 85.0 | 95.0 | 97.5 | **100.0** |
| P4 | 95.0 | 95.0 | 82.5 | 87.5 | 87.5 | 90.0 | 95.0 |

与论文 Table 3 的形状一致：**P1 整体最难**（P1-R2 "证明基础情形" 只有 82.5%），
而 R7（应用归纳假设）和 R6（分解归纳步骤）在多数题上最容易判对。

### 3.3 三个 LLM + 监督式基线，在同一批 1,120 个判定上对比

> 全部限制在"每题 40 条"的同一子集，唯一可比的口径。

| 方法 | acc | 精确率 | 召回率 | macro-F1 | 真值正类比例 | **预测正类比例** |
|---|---|---|---|---|---|---|
| MathBERT + 线性头 | 0.871 | 0.839 | 0.878 | 0.870 | 0.446 | 0.466 |
| qwen3.7-plus A | 0.844 | 0.905 | 0.725 | 0.837 | 0.446 | 0.357 |
| qwen3.7-plus B | 0.866 | 0.931 | 0.756 | 0.861 | 0.446 | 0.362 |
| qwen3.7-plus C | 0.900 | 0.907 | 0.864 | 0.898 | 0.446 | 0.424 |
| **qwen3.7-plus D** | **0.908** | 0.912 | 0.878 | **0.907** | 0.446 | **0.429** |
| deepseek-flash A | 0.722 | 0.980 | 0.385 | 0.676 | 0.446 | 0.175 |
| deepseek-flash B | 0.776 | 0.977 | 0.509 | 0.750 | 0.446 | 0.232 |
| deepseek-flash C | 0.879 | 0.948 | 0.770 | 0.874 | 0.446 | 0.362 |
| deepseek-flash D | 0.894 | 0.940 | 0.814 | 0.891 | 0.446 | 0.386 |
| qwen3.8-max A | 0.769 | 0.965 | 0.499 | 0.742 | 0.446 | 0.230 |
| qwen3.8-max B | 0.860 | 0.941 | 0.731 | 0.853 | 0.446 | 0.346 |
| *qwen3.8-max C* | *0.893* | — | — | *0.886* | *0.398* | *0.346* |（免费额度中断，仅 693 个判定）

**混淆矩阵（TN / FP / FN / TP）**：

| 方法 | TN | FP | FN | TP |
|---|---|---|---|---|
| MathBERT | 537 | 84 | 61 | 438 |
| **qwen3.7-plus D** | 579 | **42** | **61** | **438** |
| deepseek-flash D | 595 | 26 | 93 | 406 |
| qwen3.8-max B | 598 | 23 | 134 | 365 |

> 🎯 **最值得注意的一点**：qwen3.7-plus D 与 MathBERT 的 **TP 完全一样（438）、FN 完全一样（61）**，
> 但它的假阳性只有 MathBERT 的一半（42 vs 84）—— 也就是说，
> **它把"认定学生写对"这件事做得更准，同时没有因此漏掉该给的分。**
> 这是三个 LLM 里唯一一个"精确率和召回率同时追上/超过监督式基线"的配置。

### 3.4 三个模型的收益结构对比（同一子集）

| 从 → 到 | qwen3.7-plus | qwen3.8-max | deepseek-flash |
|---|---|---|---|
| A → B（加评分标准） | **+2.2** | +9.1 | +5.4 |
| B → C（加随机示例） | **+3.4** | +3.3 | +10.3 |
| C → D（随机换 RAG） | +0.8 | *（未跑）* | +1.5 |
| **合计 A → D** | **+6.4** | — | **+17.2** |

> 💡 **一个反直觉但很实用的规律：模型越强，"喂材料"的收益越小。**
> DeepSeek 靠 rubric + 示例能涨 17.2 个百分点；qwen3.7-plus 只涨 6.4 个 ——
> 因为它**零样本就已经接近天花板**（83.4%，而 DeepSeek 只有 72.2%）。
> 这对你做产品选型有直接意义：**如果模型本身够强，你不需要花大力气做 RAG 示例库；
> 如果用的是小模型，则必须把评分标准和示例喂足。**

---

## 4. 成本与用量

| 项目 | 数值 |
|---|---|
| 请求数 | 640（160 样本 × 4 条件），**0 失败** |
| 输入 tokens | 881,459 |
| 输出 tokens | 120,781（其中思考约 128/条） |
| **合计** | **1,002,240**（正好压在设计预算 1M 上） |
| 耗时 | 2 分 34 秒（并发 16，约 0.24 秒/条） |
| 单条成本 | 约 1,566 token |

对比：若用**模型默认思考强度**（单条约 3,459 token），同样的四个条件要 **2.2M token**，
是本次的 2.2 倍、耗时约 15 分钟。**`thinking_budget=128` 这一步压缩是本次能跑完的关键。**

---

## 5. 局限（诚实交代）

| 项 | 说明 |
|---|---|
| 样本量 | 每题仅 40 条（共 160），约为完整测试集的 30%。P1/P2 是子采样，P3/P4 覆盖其测试集的 77~80% |
| 单次运行 | 未做多种子重复。此前实测同类实验的运行间波动约 ±1.6~2.0 个百分点 |
| 思考强度只测了一档 | 用 `thinking_budget=128`；未对比 budget=512 / 关闭思考对准确率的影响 |
| K 固定为 4 | 未做 K 的敏感性分析 |
| 未做提示词调优 | 沿用 DeepSeek 实验的同一版提示词，未针对 qwen 调整 |
| 价格未知 | 该端点未收录 qwen3.7-plus 价目，只能报 token 用量，无法折算金额 |
| qwen3.8-max 不完整 | 其 C 条件因免费额度中断，只有 693/1,120 个判定，不参与结论 |

---

## 6. 复跑命令

```bash
# 成本预估（不花钱）
python step4_deepseek_grade.py --provider dashscope --model qwen3.7-plus \
    --thinking --thinking-budget 128 --conditions A,B,C,D --limit 40 --dry-run

# 全量跑（缓存会跳过已完成的 640 条，不会重复付费）
python step4_deepseek_grade.py --provider dashscope --model qwen3.7-plus \
    --thinking --thinking-budget 128 --conditions A,B,C,D --k 4 --limit 40 --concurrency 16 \
    --out-dir ./results_qwen37

# 三个模型 + MathBERT 在同一批样本上的对比（读缓存，不花钱）
python compare_llm_vs_mathbert.py --provider dashscope --model qwen3.7-plus --limit 40 \
    --all-models "deepseek:deepseek-flash,dashscope:qwen3.8-max" \
    --out results_qwen37/compare_same_subset.csv
```

**API key**：脚本自动按顺序查找 `--api-key` → 环境变量 `LLM_ENV_FILE` 指定的文件 → 项目默认配置。
适用模型名见 `/models` 端点（该网关同时暴露 qwen / glm / kimi / deepseek 等 252 个模型）。

---

## 7. 对 ai-math-grader 项目的启示

| 结论 | 怎么用 |
|---|---|
| **强模型 + 评分标准，就能达到 90% 的证明批改准确率** | 不必先自建模型。用 qwen3.7-plus 这类模型 + 一段写清楚的评分标准，配合已有缓存，几百次请求即可上线一个可用批改器 |
| **模型越强，RAG 示例的边际收益越小** | 若选强模型，**可以砍掉 RAG 示例库这块工程量**（qwen3.7-plus 从 C 到 D 只涨 0.8 分）；把精力放在评分标准本身和反馈话术上 |
| **思考强度可以换成本，且有一个性价比拐点** | `budget=128` 比默认省 55% token、快 8 倍，准确率仍拿到全场最高。**不需要开满思考** |
| **务必检查模型的判定尺度校准** | qwen3.7-plus 预测正类 42.9% ≈ 真值 44.6%（校准良好）；DeepSeek 只有 17.5%，会系统性冤枉学生。**选型时一定要看这一列，不能只看准确率** |
| **`reasoning_effort` 这类参数要先验证再信任** | 本端点静默忽略它。任何"降本参数"都要用实测 token 数验证，否则会以为省了钱其实没有 |

---

*本报告所有数字均为 2026-09-16 实测。原始响应全部缓存在 `llm_cache/dashscope_qwen3.7-plus_*.jsonl`，可离线复算。*



