# Project Memory

更新：2026-09-16。

## 当前论文定位

工作题目（待导师确认）：**《评分证据约束下的数学归纳法证明关键缺步诊断与分项评分研究》**。

论文研究真实学生数学归纳法自由文本作答。核心方法是“原文证据约束 + 步骤依赖 + 规则分项评分”；局部 Lean 验证只作为可选增量实验，不是课题成立条件。

## 核心研究问题

1. 原文证据约束能否减少关键内容未表达时的误给分？
2. 步骤依赖能否阻止前置步骤缺失或无效时后续评分项继续得分？
3. M1 与 M2 分别带来多少分项评分增益、关键缺步检出增益、合理省略误扣分和人工复核成本？
4. 可选 M3 局部 Lean 验证是否在同一解析输出上提供独立真实检错？

## 数据基础

首选 Student Proof by Induction Data Set。公开论文报告四道归纳法题、约 3586 份非空学生作答和七项 rubric。正式实验前必须重新下载并记录：来源、许可、版本、校验值、字段、清洗规则和实际可用数量。

七项 rubric 起点：

1. 识别基例；
2. 证明基例；
3. 陈述归纳假设；
4. 给出归纳假设的范围；
5. 陈述归纳步骤目标；
6. 将 `k+1` 情形分解到 `k` 情形；
7. 应用归纳假设。

原数据没有系统提供原文字符位置、步骤依赖、合理省略/关键缺步、首错/首缺步、复核需求和局部 Lean 标注，因此需要二次标注。先导建议 80–120 份；正式规模依据一致性、效应和资源再定，暂以 300–500 份作为规划范围而非统计保证。

## 输入与输出

输入：人工确认的题目结构、学生原始作答、冻结版本的题目 rubric 与允许省略政策。

输出：

- 带 Unicode codepoint 字符跨度的候选步骤；
- 步骤角色、声称断言、理由与引用；
- 可观察引用、题干/允许知识上下文、结构性或隐含有效依赖和 rubric 必需路径；
- 合理省略、部分完成、关键缺步、错误、依赖受阻、歧义或待复核状态；
- 七项分数、总分、证据、诊断和复核原因；
- 若启用 M3，则附加局部形式验证记录。

## 方法路线

```text
题目 + 原始作答 + 冻结 rubric
-> 候选 induction-stepir-v1
-> 结构、原文跨度与语义忠实性检查
-> 可观察引用与公式/推理结构线索
-> rubric 约束的省略/缺步判断
-> 样本级有效依赖、rubric 必需路径与 blocked_dependency 传播
-> 规则驱动七项评分和人工复核分流
-> 分项结果、总分、证据和诊断
```

模型只负责候选解析。系统推断、参考解步骤和验证展开不得写回学生证据层。依赖受阻不等于所有后续步骤都独立错误，评分规则必须避免重复扣分。

## 对照与评价

| 组 | 方法 | 目的 |
| --- | --- | --- |
| B0 | 原论文基线或可复现替代 | 对齐公开数据任务 |
| B1 | LLM 直接依据 rubric 评分 | 直接评分基线 |
| B2 | LLM + rubric + few-shot/RAG | 强提示基线 |
| M1 | 原文证据约束 + 规则评分 | 测量证据约束作用 |
| M2 | M1 + 步骤依赖 | 当前完整核心方法 |
| M3（可选） | M2 + 局部 Lean | 测量形式验证增量与代价 |

主要指标：七项 Macro-F1、总分 MAE/完全一致/±1、关键缺步误给分率、合理省略误扣分率、证据跨度质量、依赖质量、首错/首缺步定位、自动覆盖率、人工复核率、时间和费用。

## 当前实现状态

- 已完成当前课题的架构、评价、数据、表示、评分和模型接口设计文档。
- 尚未接入公开数据或完成二次标注。
- 尚未实现当前课题的数据加载、`induction-stepir-v1` 校验、依赖感知评分或真实模型独立测试。
- 现有工程组件必须逐项审计后才能复用；其既有题型输出不能作为当前课题实验结果。

## 当前风险

1. 只有四道题，题内表现不能代表跨题泛化。
2. 原始标签粒度与当前部分分口径可能不一致。
3. 合理省略具有教学判断性，标注一致性可能成为主要瓶颈。
4. 原文跨度正确不等于语义解析忠实。
5. 固定依赖模板可能误伤替代正确路径。
6. 方法贡献需要由新标注、可执行约束、依赖评分和严格消融共同支撑。
7. 可选自动形式化可能引入新误报并增加复核成本。

## 下一阶段

1. 获取并审计公开数据及 grading instructions。
2. 冻结第一版七项 rubric、允许省略、关键缺步、依赖和复核标注手册。
3. 完成 80–120 份双人先导标注并分析一致性。
4. 实现 B1/B2、M1、M2 的最小闭环和严格 test 隔离。
5. 完成对照、消融和错误分析后，再决定是否实施 M3。

## 文档入口

- [中期汇报](../中期汇报.md)
- [当前任务](tasks.md)
- [代码地图](code_map.md)
- [方法架构](method_architecture.md)
- [评价协议](evaluation_protocol.md)
- [数据设计](dataset_design.md)
- [StepIR](stepir_schema.md)
- [评分规则](scoring_rules.md)
- [模型接口](llm_stepir_io_contract.md)
- [方向决策](decisions/2026-09-15-induction-evidence-dependency-scope.md)
- [依赖语义决策](decisions/2026-09-16-observed-and-effective-dependency-separation.md)
