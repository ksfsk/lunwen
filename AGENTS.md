# AGENTS.md

## 项目定位

当前工作题目（待导师确认）：《评分证据约束下的数学归纳法证明关键缺步诊断与分项评分研究》。

研究对象是真实学生的数学归纳法自由文本作答。论文以可检验研究问题、方法贡献和独立实验为主，系统只作为方法验证载体。

核心研究问题：

- 原文证据约束能否减少学生未写出关键内容却被误给分的情况？
- 步骤依赖能否进一步阻止前置步骤缺失时后续评分项继续得分？
- 上述机制分别带来多少评分增益、合理省略误扣分和人工复核成本？
- 可选的局部 Lean 验证能否在相同解析结果上提供独立检错增益？

这些都是待实验验证的主张，不得写成已经实现或已证实的创新。

## 研究边界

- 首选公开数据为 Student Proof by Induction Data Set：四道题、约 3586 份非空英文作答和七项归纳法 rubric。正式使用前必须重新核验版本、许可、字段和实际数量。
- 七项 rubric 为：识别基例、证明基例、陈述归纳假设、给出假设范围、陈述归纳目标、分解 `k+1` 情形、应用归纳假设。
- 原始标签不足以直接评价证据和依赖，需要二次标注原文跨度、步骤、合理省略、关键缺步、依赖、首错/首缺步和复核状态。
- 主方法中模型只生成候选 StudentStepIR，不直接决定最终分数；LLM 直接评分与强 rubric 提示作为对照。
- 核心方法是“原文证据约束 + 步骤依赖 + 规则分项评分”。Lean 不是必要模块，只能作为可选 M3 消融。
- 不扩展到开放域证明理解、任意数学证明评分、通用自动定理证明或跨语言泛化。

## 方法架构

```text
结构化题目 + 学生原始作答 + 冻结 rubric
-> 候选 StudentStepIR（induction-stepir-v1）
-> Schema、原文跨度与语义忠实性检查
-> 学生声称依赖与有效评分依赖
-> 合理省略、部分完成、关键缺步与错误诊断
-> 规则驱动的七项分项评分与人工复核分流
-> 分项分数、总分、证据、诊断和复核原因
```

可选 M3 仅对已有明确学生证据且能够忠实编码的局部步骤进行 Lean 验证。Lean 通过不能为学生未表达的评分项创造分数；Lean 失败必须区分数学错误、形式化错误、不支持、超时和系统异常。

## 当前实现状态

- 六份核心设计文档已经切换到数学归纳法主线。
- 公开数据尚未接入，二次标注尚未开始。
- `induction-stepir-v1`、依赖感知评分器和当前课题的独立测试尚未实现。
- `proof_mvp` 中现有程序主要是早期工程代码，只能在逐项审计后复用 I/O、Schema 校验、批处理和结果记录框架；其题型规则和已有输出不是当前课题结果。

## 开发与评价原则

- 先完成数据审计和标注手册，再实现强基线与核心方法。
- gold、模型预测、接口示例和人工构造变体必须分开保存。
- few-shot、dev、test 严格分离；同一原作答及其变体、近重复和模板家族不得跨划分。
- 真实预测禁止缺失结果回退 gold；缺失、重复、额外 ID、解析失败和拒答都进入分母。
- 测试请求不得包含目标样本的 gold StepIR、分数、错误标签或标注说明。
- 双人标注至少覆盖预先确定的测试子集，并报告一致性、分歧和裁决。
- 主要对照为 B0 原论文基线或可复现替代、B1 LLM 直接评分、B2 强 rubric/few-shot 评分、M1 证据约束评分、M2 M1 + 依赖、可选 M3 M2 + Lean。
- 主要指标包括七项 Macro-F1、总分 MAE/完全一致/±1、关键缺步误给分率、合理省略误扣分率、证据跨度与依赖质量、首错/首缺步定位、自动覆盖率、人工复核率、时间和费用。
- 不把待复核视为零分，不通过剔除失败样本提高结果；比较风险—覆盖—复核成本。

## 文档与记录

继续工作前优先阅读：

- `memory/project.md`
- `memory/tasks.md`
- `memory/code_map.md`
- `memory/method_architecture.md`
- `memory/evaluation_protocol.md`
- `memory/decisions/2026-09-15-induction-evidence-dependency-scope.md`

涉及数据、表示、评分或接口时，再读 `dataset_design.md`、`stepir_schema.md`、`scoring_rules.md` 和 `llm_stepir_io_contract.md`。

研究边界或实验口径变化时新增 decision；仅在实际运行当前课题实验后创建 experiment 记录；阶段推进写入 `memory/research_log/`。不得用规划文档代替实验结果。

## 验证要求

仅修改文档时检查：课题名称、术语、链接、版本和“已实现/待实现”表述。

修改代码或数据时运行与变更对应的当前课题测试。若触及 `proof_mvp` 的既有公共组件，再运行：

```powershell
cd C:\Users\ksfsk\Desktop\lean4_proof_validation_research\proof_mvp
python scripts\run_batch.py
```

该命令只检查既有工程没有被破坏，不能作为当前研究结果。

## 论文表述

优先使用“证据约束”“依赖感知”“规则驱动”“关键缺步诊断”“人工复核”和“方法研究”。避免“系统理解任意证明”“有原文即数学正确”“Lean 直接评分”“编译通过就给分”或“组合模块本身就是创新”。
