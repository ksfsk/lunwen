# 《评分证据约束下的数学归纳法证明关键缺步诊断与分项评分研究》中期汇报

更新日期：2026-09-16  
文档状态：中期汇报内容稿已补齐；实验数值、结果图和案例输出均为理论模拟或人工构造，待正式实验后整体替换；课题名称待导师确认。

## 第一部分 研究背景

数学证明能力是数学学习中的重要能力。与只需给出最终答案的计算题不同，证明题的评价不仅要判断结论是否正确，还需要考察学生是否建立了必要前提、是否正确使用推理依据，以及各步骤之间能否构成完整的论证链。数学归纳法具有相对稳定的证明结构，通常包括基例、归纳假设、归纳步骤目标、`k+1` 情形分解和归纳假设应用等环节，因此适合作为研究自然语言证明自动评分的受限场景。但在真实学生作答中，这些环节可能出现合理简写、表述不完整、错误使用归纳假设或者关键步骤缺失，使自动评价不能只依靠关键词匹配或最终结论判断。

传统证明题评分主要依赖教师人工阅读。教师需要逐步判断学生写出了什么、哪些内容可以合理省略、错误从何处开始，以及后续步骤是否仍然具有得分依据。该过程具有较高的时间成本，也难以为大量学生提供及时、细粒度的反馈。已有研究指出，及时反馈有助于学生改进证明写作，因此，面向学生自由文本证明的自动评分与错误诊断具有明确的教育应用价值。

近年来，大语言模型和自然语言处理技术的发展，使自由文本证明自动评分成为可能。Zhao、Silva 和 Poulsen 基于四道数学归纳法题的真实学生作答，研究了利用大语言模型及其他机器学习方法进行自动评分和反馈的方法。其实验表明，模型可以在归纳法评分任务上取得一定效果；学生使用自动反馈后能够改进证明，但对自动评分系统的信任仍低于对人工评分者的信任。这说明归纳法自动评分具有现实可行性，同时也说明仅给出分数或反馈还不足以解决评分可信性问题[1]。

与此同时，自然语言数学证明的细粒度评价逐渐成为研究重点。ProofGrader 等工作开始使用专家评分数据、参考解和评分方案，对完整证明进行多档评分，说明证明评价正在从“正确或错误”的二分类判断发展到部分分和细粒度评价。然而，这类研究主要评价模型生成的竞赛证明，重点是提高总分与专家评分的一致性，尚未充分回答真实学生作答中的另一个关键问题：系统给出的某项分数，是否确实具有学生原文证据[2]。

在证明评分中，原文证据与逻辑正确性是两个不同问题。学生没有写出某个关键步骤，并不意味着系统不能根据标准证明将其补全；但系统能够补全该步骤，也不能说明学生已经完成了该评分要求。大语言模型具有较强的推理和补全能力，可能依据题目、参考答案或上下文生成一条完整合理的证明，从而把“系统推断出的内容”误认为“学生实际表达的内容”。如果评分系统没有明确的原文证据约束，就可能在关键步骤缺失时仍然给分，降低评分结果的可追溯性。

此外，数学归纳法各评分项之间具有明显的依赖关系。例如，应用归纳假设通常依赖于归纳假设已经被正确陈述并具有适当的适用范围；完成 `k+1` 情形的推导，也可能依赖于将其正确分解到 `k` 情形。已有研究已经表明，依赖图能够用于表示证明行之间的先后关系，或者保持自然语言证明在形式化过程中的结构忠实性。例如，Proof Blocks 通过教师预设的证明行依赖图实现结构化证明自动评分[3]；ProofFlow 则通过有向无环图保存自然语言证明步骤之间的逻辑依赖[4]。但这些工作分别面向预先给定的证明行和证明自动形式化，并未直接解决真实学生自由文本作答中关键缺步的依赖传播与部分评分问题。

基于上述背景，本研究将问题限定在真实学生数学归纳法作答场景，重点研究“学生实际写出了什么”以及“该评分内容依赖的前置步骤是否成立”。研究拟将学生作答解析为带原文位置的候选结构化步骤，通过原文证据约束限制无依据补步，再利用评分规范定义的步骤依赖判断关键缺步和依赖受阻，最终进行可追溯的七项分项评分。研究不仅评价总分和分项评分准确性，还重点考察关键缺步误给分率、合理省略误扣分率、首错或首缺步定位、自动覆盖率及人工复核成本。

由此，本课题的研究背景可以概括为以下逻辑链：

```text
真实学生归纳法作答需要及时、细粒度评价
                ↓
人工评分和反馈具有较高时间成本
                ↓
大语言模型使自由文本证明自动评分成为可能
                ↓
直接模型评分仍存在证据不可追溯和自动补步风险
                ↓
需要引入原文证据约束和步骤依赖
                ↓
研究关键缺步诊断与可解释分项评分方法
```

综上，现有研究已经证明数学归纳法自由文本自动评分具有可行性，但对于学生未明确写出的关键步骤，仍缺少同时约束原文证据和评分依赖的细粒度评价机制。因此，有必要研究面向真实学生作答的证据约束、关键缺步诊断与依赖感知分项评分方法。

## 第二部分 研究意义

### 2.1 理论意义

现有数学证明自动评分研究多以总分一致性、正确率或模型与人工评分的一致程度作为主要目标，但证明题评分不仅涉及结论是否正确，还涉及学生是否实际表达了必要推理，以及后续步骤是否建立在有效前提之上。本研究将“原文证据”“步骤依赖”和“数学有效性”区分为不同层次，有助于进一步明确自然语言证明自动评分的基本对象和判断边界。

首先，本研究强调“系统能够推导出某一步”与“学生已经写出某一步”之间的区别。通过要求评分项与学生原文片段建立可追溯关系，可以研究大语言模型是否会根据标准证明自动补全学生缺失的推理，并减少由这种补全倾向导致的关键缺步误给分。这有助于将证明评分从单纯的结果预测问题进一步转化为受学生作答证据约束的评价问题。

其次，本研究把证明步骤之间的依赖关系引入分项评分。数学归纳法中的基例、归纳假设、归纳目标、`k+1` 情形分解和归纳假设应用并不是彼此独立的评分点。如果必要前提尚未建立，后续步骤即使包含相似关键词，也不应被直接视为完整有效。研究进一步区分学生文本中的可观察引用、由公式或推理结构直接体现的有效依赖，以及评分规范要求的必要依赖路径。学生不必写出规则名称；只要书面推理实际、正确地体现了对题干条件、前序步骤或允许背景知识的使用，就可以正常评分。只有必须由系统补写未表达的关键连接时，才不能视为学生已经完成。

最后，本研究不只评价总体评分准确性，还同时考察关键缺步误给分率、合理省略误扣分率、自动覆盖率和人工复核成本。这种评价方式能够补充仅使用准确率、F1 或总分误差的传统评价，使证明自动评分研究更加关注系统在实际教学使用中的风险与代价。

### 2.2 方法意义

本研究拟建立一种面向学生数学归纳法作答的证据约束与依赖感知评分方法。方法首先将学生原始作答解析为带原文位置的候选 StudentStepIR，再依据冻结的评分规范判断各评分项是否具有充分证据，并利用步骤依赖检查前置条件是否成立，最终由规则层完成分项评分和人工复核分流。

该方法的意义不在于简单组合大语言模型、结构化表示和规则评分，而在于建立明确的职责边界：

- 大语言模型负责候选步骤解析，不直接决定最终分数；
- 原文证据约束判断学生是否表达了相应内容；
- 步骤依赖判断该内容是否建立在必要前提上；
- 规则层依据固定 rubric 计算分项分数；
- 证据不足、替代路径不明确或系统无法可靠判断时进入人工复核。

这种分层结构可以保存评分过程中的中间依据，使系统错误能够被定位为解析错误、证据错误、依赖错误或评分规则错误，为后续消融实验和误差分析提供基础。

### 2.3 数据与评价意义

现有 Student Proof by Induction Data Set 已提供真实学生数学归纳法作答及七项 rubric 标签，但尚不足以直接评价关键缺步、原文证据和步骤依赖。本研究拟对其中一部分作答进行二次标注，补充：

- 学生步骤及其原文字符位置；
- 合理省略、部分完成和关键缺步；
- 可观察引用、结构性有效依赖、允许背景知识与评分必需路径；
- 首错或首缺步位置；
- 歧义和人工复核状态。

这些标注能够形成适用于证据约束评分和依赖诊断的研究资源。即使最终方法提升有限，标注规范、人工一致性结果、成对错误案例和评价指标仍可为后续学生证明理解与自动评分研究提供参考。

### 2.4 实践意义

在教学应用中，本研究可以为教师提供比单一总分更细致的评分依据。系统输出可以说明某项得分对应学生原文中的哪一部分、缺失了哪个关键步骤、后续评分项为什么受到影响，以及哪些情况需要人工复核。这有助于降低教师查找错误位置和核对评分依据的工作量。

对于学生而言，基于证据和步骤依赖的反馈比笼统的“证明不完整”或“归纳步骤错误”更加具体。系统可以指出学生是否遗漏基例、是否没有明确归纳假设的范围、是否未将 `k+1` 情形连接到 `k` 情形，以及是否真正使用了归纳假设，从而为修改证明提供更明确的方向。

需要强调的是，本研究并不以完全替代教师评分为目标，而是研究如何在可靠证据范围内完成自动分项评分，并将无法可靠判断的答案交给人工复核。其实际价值主要体现在提高评分依据的透明度、辅助教师筛查高风险答案，以及为学生提供及时且可追溯的证明反馈。

综上，本研究的意义可以概括为：

> 在理论上，将证明评分从结果预测推进到原文证据和步骤依赖约束下的过程评价；在方法上，建立可追溯、可消融、可复核的分项评分机制；在数据上，补充关键缺步与依赖标注；在实践上，为教师评分和学生反馈提供透明的辅助依据。

## 第三部分 国内外研究现状

### 3.1 数学证明自动评分与教学反馈

自动评分技术已经广泛应用于选择题、计算题、程序设计题等具有明确答案或可执行判定条件的任务，但自然语言数学证明的评分仍然困难。证明作答不仅包含结论，还包含前提、变量范围、推理依据和步骤之间的逻辑关系。同一个正确证明可以使用不同措辞、步骤顺序和中间结论，而表面相似的答案也可能缺少决定性的推理连接，因此不能仅通过关键词、字符串相似度或最终答案完成可靠评分。

在数学证明教学中，一类代表性工作是 Proof Blocks。该工具不要求学生从零书写自由文本证明，而是让学生排列教师预先提供的证明行。教师为证明行定义依赖图，系统将依赖图的拓扑排序视为可接受解，从而实现自动评分和即时反馈[3]。后续研究进一步使用学生提交到正确拓扑序之间的编辑距离计算部分分，并通过最小顶点覆盖等方法提高评分效率[5]。

Proof Blocks说明步骤依赖图能够支持多种正确顺序和部分分计算，也说明结构化证明活动可以实现稳定自动评分。但是，该类方法的输入是教师预先写好的证明行，学生主要完成选择和排序，不需要系统从自由文本中判断学生实际表达了哪些步骤。因此，它尚不能直接解决本研究关注的原文证据定位、自由表达解析、合理省略和关键缺步诊断问题。

### 3.2 数学归纳法自由文本自动评分

Zhao、Silva和Poulsen开展的数学归纳法自动评分研究与本课题最为接近。该研究收集了四道数学归纳法题的真实学生自由文本作答，使用大语言模型和其他机器学习方法预测评分标签，并部署自动评分器开展学生实验。研究结果表明，自动反馈能够帮助学生改进证明，但学生对AI评分的信任仍低于对人工评分者的信任[1]。

该工作证明了以下两点：第一，数学归纳法具有相对稳定的证明结构，适合作为自由文本证明评分的受限研究场景；第二，大语言模型已经具备从学生自然语言作答中识别部分证明要素的能力。因此，本研究继续使用真实学生归纳法作答和七项rubric作为基础任务具有现实依据。

但是，原有任务主要提供七项评分标签，没有系统标注每项分数对应的原文跨度、步骤依赖、合理省略、关键缺步、首错位置和人工复核需求。模型预测某个评分项成立，并不能说明该判断来自学生实际写出的内容，也不能判断模型是否依据标准证明自动补全了缺失步骤。因此，现有归纳法自动评分研究为本课题提供了数据和任务基础，但没有直接解决证据约束和依赖感知评分问题。

### 3.3 细粒度自然语言证明评价

随着大语言模型生成数学证明能力的提高，研究重点开始从最终答案判断转向自然语言证明的细粒度评价。ProofGrader基于ProofBench开展研究，使用专家评分、参考解和评分方案对模型生成的竞赛证明进行0—7分评价。相关研究表明，强推理模型结合参考解、评分标准和集成策略，可以提高与专家评分的一致性[2]。

这类工作说明：相比只判断证明是否正确，引入评分方案、参考解和细粒度分档能够更好地评价证明质量，也为本课题设计B2强rubric基线和总分误差指标提供了依据。

但ProofGrader主要评价大语言模型生成的竞赛证明，研究目标是使自动评价结果接近专家总分。它并不以真实学生作答为主要对象，也不要求每个得分项必须定位到学生原文，更没有重点研究学生没有写出关键步骤时模型是否会自动补全。因此，“评分结果接近专家”与“每项得分具有学生证据”仍然是两个不同问题。

另一条相关路线是数学推理的验证器和过程监督。Cobbe等通过独立验证器对数学解答候选进行排序[7]；Uesato等比较了结果监督与过程监督，指出仅依据最终答案难以充分控制推理错误[8]；Lightman等进一步发布PRM800K并研究逐步过程反馈[9]；Math-Shepherd则探索用自动构造的步骤级监督训练过程奖励模型[10]。这些工作共同说明，对中间推理过程进行评价比只看最终答案更有机会定位错误。

但过程奖励模型主要判断模型生成解答中的步骤是否有助于得到正确结果，通常不承担教学rubric分项、学生原文归属和合理省略政策。它们可以为“逐步评价”提供方法依据，却不能直接回答“该步骤究竟是学生写出的，还是评分模型补出的”。因此，本研究不把过程奖励分数直接当作学生分数，而是将其与原文证据门、依赖规则和人工复核分开。

大语言模型作为评价器的研究也表明，强模型能够在部分开放式任务上接近人工偏好，但会受到位置、冗长、自我偏好和提示方式等因素影响[11]。G-Eval与Prometheus说明，评价步骤、评分rubric和参考材料能够提升细粒度评价能力[12,13]，同时也提示评价器输出需要固定标准、版本控制和人工校验。本研究据此设置B1/B2基线，但不假定强rubric提示本身能够保证证据忠实或消除自动补步。

### 3.4 自然语言证明形式化、忠实性与依赖表示

自然语言证明形式化研究尝试将非形式化数学语言转换为定理证明器可以检查的形式表达。已有研究探索了使用大语言模型将自然语言数学陈述翻译为Isabelle/HOL等形式语言并辅助自动定理证明[6]，ProofFlow则进一步使用Lean 4研究分步骤形式化和结构忠实性[4]。这类研究的优势是能够提供较严格的语法和逻辑检查，但其主要目标通常是得到可执行的形式证明，而不是评价学生在原始作答中实际完成了哪些评分要求。

ProofFlow进一步强调自然语言证明形式化过程中的结构忠实性。该方法先把自然语言证明表示为有向无环依赖图，再将各个步骤形式化为中间引理，从而保留原证明中的逻辑依赖。其评价同时考虑语法正确性、语义忠实性和结构忠实性[4]。这一思路说明，仅得到一个可验证结论并不足以证明形式化结果忠实于原始证明，显式保存步骤依赖具有重要作用。

但ProofFlow面向的是大学数学证明自动形式化，其输入通常是相对完整的证明，输出目标是结构忠实的形式代码；它不直接处理真实学生作答的七项rubric、合理省略、部分分和人工复核成本。本研究借鉴其“步骤化表示”和“显式依赖”思想，但将依赖用于评分项成立条件和关键缺步传播，而不是要求所有学生答案都转换为完整形式证明。

在自然数学语言资源方面，NaturalProofs汇集数学陈述及其自然语言证明，用于数学引用检索与生成研究[14]；ProofNet将自然语言定理、自然语言证明与Lean形式陈述配对，用于自动形式化和形式证明研究[15]。这些资源证明了自然语言证明可以被结构化、检索或连接到形式系统，但它们主要由教材或正式数学文本构成，不含本研究需要的七项学生评分、关键缺步、合理省略和人工复核标签。因此，它们更适合作为表示方法和相关任务参考，不能替代真实学生归纳法作答及其二次标注。

### 3.5 现有研究对比

| 研究方向或代表工作 | 主要输入 | 真实学生自由文本 | 原文证据定位 | 合理省略/关键缺步 | 步骤依赖 | 分项评分与复核 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Proof Blocks[3,5] | 教师预设证明行 | 否 | 不适用 | 部分支持缺行，不处理自由表达 | 是 | 支持部分分，不强调人工复核 |
| 归纳法自动评分[1] | 学生归纳法自由文本 | 是 | 未系统标注 | 未形成独立诊断任务 | 否/有限 | 七项评分与反馈 |
| ProofGrader[2] | 模型生成的竞赛证明 | 否 | 参考解与评分方案，不要求学生跨度 | 以总分档位为主 | 弱 | 细粒度总分评价 |
| 自然语言证明形式化[6] | 数学陈述或证明 | 通常否 | 关注翻译正确性 | 非主要目标 | 部分 | 不面向教学rubric评分 |
| ProofFlow[4] | 较完整自然语言证明 | 否 | 强调语义忠实 | 非主要目标 | 是 | 不面向七项学生评分 |
| 本研究 | 学生归纳法自由文本 | 是 | 是 | 是 | 是 | 七项评分、诊断与复核分流 |

### 3.6 研究现状总结

现有研究已经分别证明了自由文本归纳法自动评分、细粒度证明评价、依赖图评分和自然语言证明结构化表示的可行性，但尚未形成一个直接面向真实学生归纳法作答、同时处理原文证据、合理省略、关键缺步、评分依赖和人工复核成本的统一评价方法。

因此，本研究不是简单地把已有模块组合在一起，而是围绕一个尚未被充分回答的评分问题展开：当学生没有明确写出关键内容时，系统如何避免利用自身推理能力替学生补步；当学生采用合理压缩表达时，又如何避免机械地判定为缺步；当前置步骤没有建立时，后续评分项应当如何处理。上述问题构成下一部分的问题分析。

## 第四部分 问题分析

### 4.1 问题一：模型推理补全与学生作答证据混淆

大语言模型能够依据题目、标准证明和上下文推导出学生没有写出的中间步骤。这种能力有助于生成完整证明，但在评分场景中可能造成对象混淆：系统能够推出某一步，并不代表学生已经完成该步骤。

例如，学生可能直接写出最终的 `P(k+1)` 结论，却没有说明如何将 `k+1` 情形分解到 `k` 情形，也没有展示归纳假设的实际应用。直接评分模型可能根据标准证明推断出缺失连接，并为R6或R7给分。其结果虽然在数学上可以被补全，却无法追溯到学生原文，形成关键缺步误给分。

因此，需要解决的第一个问题是：

> 如何将“学生明确表达的内容”与“系统根据数学知识推断出的内容”分开，使每个自动得分项都具有可定位、语义忠实的学生原文证据？

该问题后续主要由带原文跨度的StudentStepIR、Schema检查、原文跨度对齐和语义忠实性检查解决，并通过证据跨度质量和关键缺步误给分率评价。

### 4.2 问题二：合理省略与关键缺步难以区分

数学证明允许适当压缩。学生不需要展开每一步常规算术或重复说明已经明确的内容，因此“没有逐字写出”不能直接等同于“没有完成”。另一方面，归纳假设的范围、`k+1` 情形与 `k` 情形之间的联系以及归纳假设的实际应用属于证明成立的关键结构，不能由系统凭空补足。

如果评分规则过松，关键缺步会被当作合理简写；如果评分规则过严，正常的数学表达压缩又会被误扣分。这一矛盾直接影响证据约束方法是否具有实际可用性。

因此，需要解决的第二个问题是：

> 如何依据题目专用rubric和冻结的省略政策，区分允许压缩的微步骤、只完成部分要求的表达、不可省略的关键连接以及无法可靠裁定的边界样本？

该问题后续主要由Evidence–Rubric覆盖矩阵、候选缺口提取和受限省略政策决策树解决，并通过合理省略误扣分率、关键缺步检出率和人工复核率评价。

### 4.3 问题三：前置步骤失效后后续评分项仍可能得分

七项归纳法rubric之间不是相互独立的标签。例如，R7“应用归纳假设”通常依赖R3“陈述归纳假设”和R4“给出假设范围”已经成立，也依赖当前 `k+1` 情形能够合法地连接到归纳假设适用对象。若只逐项进行关键词或语义匹配，即使上游假设不存在或范围错误，系统仍可能因为答案出现“由归纳假设”而给出R7分数。

同时，一个根错误可能影响多个后续步骤。如果系统把每个受影响项目都诊断为新的独立错误，就会重复归因；但如果机械地阻断所有后续项目，又可能错误否定学生采用的替代正确路径。

因此，需要解决的第三个问题是：

> 如何从学生的显式引用、公式结构和允许隐含知识中识别实际体现的有效依赖，并结合rubric要求的必要依赖路径，在前置条件失效时传播受阻状态、定位首个根缺步，同时保留合法替代路径并避免重复扣分？

该问题后续主要由分层依赖表示、AND/OR评分路径、状态传播和根因归并解决，并通过有效依赖识别质量、首错/首缺步定位准确率和依赖失效误给分率评价。

### 4.4 问题四：降低误给分与维持自动覆盖率之间存在冲突

证据门和依赖规则越严格，系统越不容易在缺少依据时给分，但也越可能将非标准表达、替代路径和边界样本转入人工复核。如果方法只通过大量拒答或复核来降低误给分，即使风险指标改善，也不能说明自动评分能力真正提高。

因此，需要解决的第四个问题是：

> 如何在降低关键缺步误给分的同时，控制合理省略误扣分和人工复核成本，并在相同覆盖率或相同复核预算下公平比较不同方法？

该问题后续主要由规则分项评分、状态优先级、置信与复核分流以及风险—覆盖曲线解决。评价时同时报告自动覆盖率、人工复核率、总分误差、时间和费用，不能将待复核样本当作零分，也不能从测试结果中剔除。

### 4.5 问题、方法与评价指标的对应关系

| 问题 | 主要解决方法 | 核心输出 | 主要评价指标 |
| --- | --- | --- | --- |
| P1 无依据自动补步 | StudentStepIR、原文跨度对齐、语义忠实性检查 | EvidenceItem、证据状态 | Span-F1、证据忠实率、关键缺步误给分率 |
| P2 省略与缺步混淆 | 覆盖矩阵、候选缺口、冻结省略政策决策树 | `omitted_allowed`、`missing_critical`等诊断 | 合理省略误扣分率、关键缺步检出率、复核率 |
| P3 前置条件失效后继续得分 | 可观察引用、结构性/隐含有效依赖、AND/OR路径和状态传播 | `effective_step_dependencies`、`blocked_dependency`、根缺步与受阻原因 | 有效依赖F1、首错定位、依赖失效误给分率 |
| P4 风险与覆盖冲突 | 规则评分、状态优先级、人工复核分流 | 七项分数、总分、复核原因 | Macro-F1、MAE、覆盖率、复核率、时间与费用 |

上述四个问题不是彼此独立的四项任务，而是形成连续链条：首先确定学生实际写出的证据，再判断未写内容是否属于合理省略，然后检查已有步骤是否满足必要依赖，最后在风险和覆盖率之间形成可解释的分项评分结果。

## 第五部分 研究内容与总体框架

### 5.1 研究目标

本研究面向真实学生数学归纳法自由文本作答，目标不是生成一篇完整标准证明，也不是判断系统能否把学生答案补全，而是研究在学生原文证据范围内如何完成关键缺步诊断与七项分项评分。具体目标包括：

1. 设计带原文字符跨度的StudentStepIR，使候选证明步骤能够追溯到学生实际作答；
2. 建立原文证据约束，减少模型依据题目或标准证明自动补全缺失步骤后误给分；
3. 建立合理省略与关键缺步诊断机制，区分允许压缩的微步骤、部分完成、明确错误和不可省略的关键连接；
4. 建立步骤依赖与状态传播机制，在必要前置条件失效时阻止后续评分项被直接判为完整成立；
5. 建立规则驱动的七项分项评分和人工复核分流方法，同时评价评分增益、误判、自动覆盖率和复核成本。

### 5.2 总体研究框架

本研究提出“证据约束＋缺步诊断＋依赖感知＋规则评分”的总体框架。大语言模型只负责从学生原始作答中生成候选StudentStepIR，不直接给出最终分数；后续模块分别完成Schema与原文证据检查、合理省略与关键缺步判断、评分依赖检查以及规则分项评分。

![图5-1 证据约束与依赖感知的数学归纳法证明分项评分总体架构](assets/midterm/paper_module_dataflow/core_method_architecture_reference_style_v3.png)

**图5-1 证据约束与依赖感知的数学归纳法证明分项评分总体架构**

总体数据流为：

```text
结构化题目 + 学生原始作答 + 冻结Rubric
                ↓
候选StudentStepIR解析
                ↓
Schema、原文跨度与语义忠实性检查
                ↓
Evidence–Rubric覆盖与关键缺步诊断
                ↓
可观察依赖线索、样本级有效依赖与Rubric路径检查
                ↓
规则驱动的七项分项评分与人工复核分流
                ↓
分项分数 + 总分 + 原文证据 + 诊断状态 + 复核原因
```

该框架将“学生写了什么”“学生写出的内容是否有效”“前置条件是否成立”和“最终如何计分”分成不同层次，避免由同一个模型同时完成解析、推理补全和评分而造成职责混淆。

### 5.3 主要研究内容

围绕总体框架，本研究包含以下五项核心内容和一项数据基础工作：

1. **数据审计与二次标注**：核验公开数据版本，在原七项标签基础上补充原文跨度、步骤、合理省略、关键缺步、依赖、首错和复核状态；
2. **StudentStepIR解析**：将学生自由文本转换为带原文位置、步骤角色、断言、理由、引用和作用范围的候选结构；
3. **证据约束与缺步诊断**：检查结构化步骤是否具有忠实原文证据，并区分合理省略、部分完成、关键缺步、明确错误和歧义；
4. **步骤依赖与状态传播**：区分可观察引用、由公式结构或允许知识体现的样本级有效依赖，以及Rubric必要路径，检查AND/OR评分路径并定位根缺步；
5. **规则分项评分与人工复核**：依据冻结规则生成七项分数、总分、证据和诊断，对无法可靠自动决定的项目进行复核分流；
6. **对比实验与误差分析**：比较B0、B1、B2、M1和M2，分析各模块带来的增益、误判和人工成本。

### 5.4 方法贡献与预期创新点

本研究的预期贡献不在于简单叠加大语言模型、规则评分和依赖图，而在于围绕学生作答评分建立可检验的方法边界：

1. **评分证据约束**：将可定位、语义忠实的学生原文证据设为自动得分的前置条件，显式分离学生表达与系统推断；
2. **省略—缺步联合诊断**：使用Evidence–Rubric覆盖状态和冻结省略政策区分合理压缩与关键连接缺失；
3. **依赖感知分项评分**：从显式引用、公式结构和允许隐含知识中识别样本级有效依赖，再使用Rubric定义的AND/OR必要路径传播受阻状态并定位根缺步，避免前置条件失效后继续给分，同时不把“未写规则名称”误判为依赖缺失；
4. **风险—覆盖—复核联合评价**：除Macro-F1和总分误差外，同时报告关键缺步误给分、合理省略误扣分、自动覆盖率和人工复核成本。

上述内容均为拟提出并通过实验检验的方法贡献，不能在正式实验完成前写成已经证实的效果。

### 5.5 问题与核心模块的对应关系

| 科研问题 | 核心模块 | 主要方法 | 主要输出 |
| --- | --- | --- | --- |
| P1 模型补步与学生证据混淆 | StudentStepIR解析、原文证据约束 | 结构化解析、Schema门、跨度门、语义忠实门 | StudentStepIR、EvidenceAssessment |
| P2 合理省略与关键缺步混淆 | 合理省略与关键缺步诊断 | 覆盖矩阵、缺口候选、冻结政策决策树 | GapDiagnosis、ReviewCandidate |
| P3 前置步骤失效后继续得分 | 步骤依赖与状态传播 | 分层依赖表示、AND/OR路径、根因传播 | DependencyAssessment、首错/首缺步 |
| P4 风险、覆盖与复核成本冲突 | 规则评分与人工复核 | 状态优先级、规则计分、复核路由 | 七项分数、总分、证据包、复核原因 |

## 第六部分 具体方法与模块设计

本部分按照“解决问题—输入—处理算法—输出—评价指标”的统一形式说明各模块。立体数据块表示输入、中间表示或输出集合，平面框表示解析、检查、传播和评分操作。各模块共同组成图5-1所示的总体框架，但在实验中分别进行消融，以测量每个模块的独立作用。

### 6.1 数据输入与二次标注基础

本研究拟以 Student Proof by Induction Data Set 为数据基础。该数据集包含四道数学归纳法题的真实学生自由文本作答，并提供七个基本评分项：

1. 识别基例；
2. 证明基例；
3. 陈述归纳假设；
4. 给出归纳假设的适用范围；
5. 陈述归纳步骤目标；
6. 将 `k+1` 情形分解到 `k` 情形；
7. 应用归纳假设。

首先，对公开数据的来源、许可、版本、题目、字段、缺失值、重复项和原始评分标签进行审计。在此基础上，针对原有标签不足以描述学生具体推理过程的问题，对部分作答进行二次标注，补充：

- 学生实际表达的证明步骤；
- 步骤对应的原文字符位置；
- 合理省略、部分完成和关键缺步；
- 步骤之间的依赖关系；
- 首个错误或首个关键缺步；
- 歧义和人工复核状态。

二次标注拟采用双人独立标注和分歧裁决机制，并评价各类标签的人工一致性。若合理省略、关键缺步或依赖标签的一致性不足，则优先修订标注规范，而不是直接进入模型实验。

### 6.2 StudentStepIR解析模块

该模块解决P1中的第一层问题：把学生自由文本转换为结构化候选步骤，同时保留原文边界，不让模型直接输出分数或缺步结论。

![图6-1 StudentStepIR解析模块](assets/midterm/paper_module_dataflow/module_stepir_parser_reference_style_v1.png)

**图6-1 StudentStepIR解析模块**

| 要素 | 具体内容 |
| --- | --- |
| 输入 | `ProblemSpec`、`AnswerRecord`、`ParserContext`、冻结的解析Schema |
| 核心处理 | 构造白名单请求；切分原始作答；抽取原文跨度、断言、理由、步骤角色、引用和作用域；合并候选字段；执行JSON、Schema、ID、跨度和覆盖检查 |
| 中间表示 | `source_spans`、`raw_text`、`claim`、`reason`、`role`、`rule`、`premise_refs`、`step_refs`、`scope` |
| 输出 | 候选`StudentStepIR`、`RawResponseRecord`、规范化状态和`FailureRecord` |
| 评价指标 | 解析成功率、Schema通过率、步骤边界F1、角色Macro-F1、引用准确率、模型补步率、拒绝与复核比例 |

模块采用“受限生成＋确定性校验”的组合方式。大语言模型只能产生Schema允许的候选字段；评分状态、缺步标签、依赖结论和最终分数均不在模型输出白名单中。解析失败、缺失字段或越界跨度进入失败记录，不能回退到Gold结果。

为保存学生实际写出的证明内容，本研究拟设计面向数学归纳法作答的结构化中间表示 `induction-stepir-v1`。每个候选证明步骤主要记录：

- 步骤编号及其证明角色；
- 学生原文片段和字符位置；
- 学生声称的数学断言；
- 学生给出的理由；
- 对题目条件或前序步骤的引用；
- 局部假设和作用范围；
- 无法解析或存在歧义的文本。

大语言模型负责将学生原始作答解析为候选 StudentStepIR，但不直接决定分数。系统随后检查结构是否合法、原文位置是否越界、步骤是否具有文本依据、引用对象是否存在，以及结构化断言是否忠实保留学生原文中的错误、缺失和不确定性。

该部分重点研究两个问题：

1. 如何减少模型在解析过程中自动补入学生没有写出的步骤；
2. 如何区分结构合法、原文对齐和语义忠实三个不同层次。

系统推断出的中间步骤、参考答案中的步骤和可选验证展开均与 StudentStepIR 分开保存，不得写回学生证据层。

**算法1：受原文证据约束的 StudentStepIR 候选解析**

```text
输入：题目 P，学生原始作答 A，解析 Schema S，冻结解析配置 C
输出：候选 StudentStepIR I，证据检查 E，失败记录 F

1. 根据 P、A、S 和 C 构造白名单请求；禁止放入目标样本的 gold 标签、分数和诊断。
2. 调用解析模型得到候选 JSON；若拒答、超时或 JSON 无法解析，则记录 F 并转 needs_review。
3. 对每个候选步骤 s：
   3.1 检查 step_id、role、claim、reason、scope 和引用字段是否满足 S；
   3.2 检查 source_span=[start,end) 是否位于 A 的合法字符范围；
   3.3 重新切片 t=A[start:end]，要求 t 与 raw_text 一致；
   3.4 检查 claim/reason 是否忠实保留 t 中的变量、常数、方向、错误和不确定性；
   3.5 若步骤无原文支持或被模型改写修正，则保留审计记录，但不进入合格证据集合。
4. 检查引用对象存在性、作用域、重复 ID、跨步覆盖和未解析文本。
5. 输出候选 I、逐步证据状态 E 及失败记录 F；本算法不输出分数。
```

### 6.3 原文证据约束模块

该模块继续解决P1：判断StudentStepIR中的候选步骤是否真的具有学生原文依据，以及结构化断言是否忠实保留学生表达。证据检查被拆分为三个连续门和一个Rubric对齐操作。

![图6-2 原文证据约束模块](assets/midterm/paper_module_dataflow/module_evidence_constraint_reference_style_v1.png)

**图6-2 原文证据约束模块**

| 要素 | 具体内容 |
| --- | --- |
| 输入 | `StudentStepIR.step[]`、未经改写的`answer_text`、`RubricSpec` |
| G1结构门 | 检查必需字段、步骤ID、引用ID、作用域和Schema约束 |
| G2跨度门 | 按Unicode codepoint左闭右开区间切片，要求`answer_text[start:end]`与步骤原文一致 |
| G3忠实门 | 检查结构化claim是否修正常数、变量、方向、理由或补入原文不存在的步骤 |
| Rubric对齐 | 判断合格步骤能够支持哪些Rubric角色和内容条件，不把关键词出现直接视为评分项成立 |
| 输出 | `EvidenceItem[]`、`EvidenceAssessment`、拒绝原因和复核原因 |
| 评价指标 | Span-F1、精确跨度率、语义忠实准确率、错误证据接受率、合格证据拒绝率、人工复核率 |

证据门只回答“学生是否表达了相应内容”，不直接回答该内容在数学上是否正确。通过证据门的错误推理仍可在后续被标记为`invalid`；没有原文证据的系统推导则不能成为评分依据。

### 6.4 合理省略与关键缺步诊断模块

该模块解决P2。系统先构造Evidence–Rubric覆盖矩阵，再提取未覆盖、部分覆盖和歧义覆盖的要求，最后使用冻结省略政策判断缺失内容是允许压缩的微步骤，还是不可省略的关键连接。

![图6-3 合理省略与关键缺步诊断模块](assets/midterm/paper_module_dataflow/module_gap_diagnosis_matrix_tree_v2.png)

**图6-3 合理省略与关键缺步诊断模块**

| 要素 | 具体内容 |
| --- | --- |
| 输入 | `EvidenceItem[]`、`EvidenceAssessment`、`RubricSpec`、`OmissionPolicy[]` |
| 覆盖矩阵 | 建立证据行与R1—R7要求列的覆盖关系，记录已覆盖、部分覆盖、未覆盖和歧义状态 |
| 缺口提取 | 对必需要求与合格证据做差分，生成缺角色、缺内容或缺推理连接的`GapCandidate` |
| 政策决策 | 仅在存在冻结`policy_rule_id`，且前后锚点、必要前提、作用域和有限展开条件满足时允许省略 |
| 诊断输出 | `established`、`partial`、`omitted_allowed`、`missing_critical`、`invalid`或`needs_review` |
| 最终输出 | `GapDiagnosis[]`、原因码、`policy_rule_id`和`ReviewCandidate[]` |
| 评价指标 | 关键缺步Precision/Recall/F1、误给分率、合理省略误扣分率、诊断Macro-F1、复核率 |

省略政策在测试集冻结前确定。只有“显然”“同理”等词语而没有充分前后锚点时不能自动判为合理省略；从`P(k)`直接跳到`P(k+1)`且没有建立二者联系时，不能由系统补成R6或R7。

学生证明不需要逐字展开所有推理，但每项允许省略规则都必须预先限定题目、原文锚点、可用前提、作用域和展开上限。系统能根据标准证明补全内容，不等于该缺失属于合理省略。完整诊断状态和计分含义统一列于第6.7节；本模块重点产生局部的省略、缺步和错误判断，`blocked_dependency`由后续依赖模块产生。

**算法2：Rubric 覆盖、合理省略与关键缺步诊断**

```text
输入：合格证据集合 E，评分规范 R，冻结省略政策 O
输出：逐项诊断 G，人工复核候选 Q

1. 对每个评分项 r∈{R1,…,R7} 建立“角色—内容—理由—作用域”要求集合 Req(r)。
2. 将 E 中每条证据与 Req(r) 对齐，形成覆盖矩阵 M：已覆盖、部分覆盖、未覆盖或歧义。
3. 若 M 显示证据完整且局部内容有效，则暂记 established。
4. 若存在证据但只满足部分要求，则记 partial，并保存缺失子条件。
5. 若没有合格证据：
   5.1 查找与 r 匹配的冻结 policy_rule_id；
   5.2 仅当题目范围、前后锚点、必要前提、作用域和展开上限均满足时记 omitted_allowed；
   5.3 若 r 属于不可省略关键连接，则记 missing_critical；
   5.4 若无法确认是否属于替代路径或合理压缩，则记 needs_review 并加入 Q。
6. 若学生明确写出错误断言、错误理由或非法作用域，则记 invalid，而不是 missing_critical。
7. 输出 G、原因码、证据跨度、policy_rule_id 和 Q；不得生成学生未写出的证明步骤。
```

### 6.5 步骤依赖与状态传播模块

该模块解决P3，不把“学生是否说出依赖名称”当作得分门槛，而是区分三个层次：StudentStepIR记录学生文本中的明确引用或清晰指代；样本级有效依赖记录书面公式或推理实际体现的题干前提、前序步骤和允许背景知识；Rubric必要路径记录评分项成立所要求的AND/OR条件。

![图6-4 步骤依赖与状态传播模块](assets/midterm/paper_module_dataflow/module_dependency_propagation_reference_style_v1.png)

**图6-4 步骤依赖与状态传播模块**

| 要素 | 具体内容 |
| --- | --- |
| 输入 | 合格StudentStepIR步骤、EvidenceAssessment、GapDiagnosis、`required_dependency_paths` |
| 可观察引用 | 根据`premise_refs`和`step_refs`保存明确引用或清晰指代，检查不存在引用、前向引用、循环和作用域越界；字段为空不自动扣分 |
| 可用上下文 | 分开保存题干`premise_catalog`、冻结`allowed_knowledge_catalog`和已建立且作用域合法的学生步骤 |
| 样本级有效依赖 | 从`explicit_reference`、`implicit_structural`和`implicit_allowed_knowledge`三类证据中识别实际使用，并记录依赖来源、目标和状态 |
| Rubric必要路径 | 以稳定`path_id`表示Rubric项之间的AND/OR必要路径和替代正确路径 |
| 路径求值 | AND路径要求所有必要节点成立；OR路径至少一条替代路径成立；歧义路径转复核 |
| 状态传播 | 根错误或根缺步向后传播`blocked_dependency`，同时保留后续项目自身的局部错误 |
| 根因归并 | 区分独立错误和继承受阻，输出首错/首缺步并避免重复扣分 |
| 输出 | `effective_step_dependencies`、`rubric_dependency_assessments`、受阻路径和根因链 |
| 评价指标 | 依赖边Precision/Recall/F1、路径判定准确率、首错/首缺步定位、依赖失效误给分率、替代路径误拒率 |

依赖图本身不创造新的学生证据。学生无需明确写出“根据归纳假设”或基础规则名称；若公式或推理结构直接、忠实地体现了合法使用，或者冻结政策允许隐含使用相应背景知识，则有效依赖可以成立。反之，即使Rubric图表明标准证明可以补出某一步，只要学生原文没有展示该关键连接，就仍应由缺步模块处理。

依赖传播的关键不是再次判断局部文本，而是检查其前置条件：必要前提缺失、错误或超出作用域时，将局部内容与`blocked_dependency`分开保存；同一根错误只归因一次；已登记的替代正确路径按OR规则求值。无法由冻结规则可靠覆盖的替代路径进入人工复核，而不是强制判错。七项rubric的典型依赖骨架统一列于第6.8节。

**算法3：AND/OR 依赖路径求值与根因传播**

```text
输入：局部诊断 G，可观察引用 C，可用上下文 A，冻结必要依赖路径集合 Dr
输出：依赖判定 D，根错误/根缺步 Root，受阻原因链 B

1. 对 C 检查不存在引用、前向引用、循环和作用域越界；异常项记录原因但不改写学生原文。
2. 结合学生公式/推理结构与 A，从显式引用、结构直接对齐和允许隐含知识中构建样本级有效依赖；C 为空本身不触发扣分。
3. 按证明顺序对每个评分项 r 先读取其局部状态 local(r)，再读取 Dr 中的 required_dependency_paths(r)。
4. 对每条 AND 路径：仅当所有必要节点已建立且作用域合法时，路径成立。
5. 对每组 OR 路径：至少一条已登记替代路径成立即可；未知替代路径转 needs_review。
6. 若 local(r) 合理但所有必要路径均失败，则依赖状态记 blocked_dependency。
6. 若 r 自身已 invalid，同时又受上游阻断，则主状态保留 invalid，另存 blocked 原因。
7. 从首个 invalid 或 missing_critical 节点向后传播受阻状态；同一根因只计一次，不重复扣分。
8. 不回溯抹除已独立建立的基例、归纳假设或局部代数步骤。
9. 输出 D、Root 和 B。
```

### 6.6 规则分项评分与人工复核模块

该模块解决P4，将证据、缺步、局部有效性和依赖状态组合为七项评分结果。最终分数由版本化规则生成，而不是由解析模型直接决定。

![图6-5 规则分项评分与人工复核模块](assets/midterm/paper_module_dataflow/module_scoring_review_reference_style_v1.png)

**图6-5 规则分项评分与人工复核模块**

| 要素 | 具体内容 |
| --- | --- |
| 输入 | `RubricSpec`、EvidenceAssessment、GapDiagnosis、DependencyAssessment和局部有效性状态 |
| 多维状态 | 分别保存`evidence_status`、`omission_status`、`local_validity_status`、`dependency_status`和`score_status` |
| 主状态派生 | 按冻结优先级生成`needs_review`、`invalid`、`omitted_allowed`、`missing_critical`、`partial`、`blocked_dependency`或`established` |
| 规则计分 | 为R1—R7逐项应用满分、部分分、扣分或延迟复核规则，再聚合总分 |
| 复核路由 | 将证据歧义、替代路径未知、解析失败和不支持情况送入人工复核，不把待复核当作零分 |
| 输出 | 七项分数、总分、证据包、诊断状态、根因、`review_required`和原因码 |
| 评价指标 | 七项Macro-F1、总分MAE、完全一致率、±1一致率、自动覆盖率、人工复核率、时间和费用 |

评分结果需要同时给出分数和证据链。若同一项目既有独立错误又受上游阻断，主状态优先保留自身`invalid`，依赖受阻作为次级原因保存；若系统无法可靠裁定，则保留已确认的分项并将其余部分转人工复核。

每个评分结果必须关联rubric及其版本、学生原文证据、StudentStepIR步骤、证据门结果、依赖路径、诊断状态、分值和复核原因。M1用于检验证据约束能否减少无依据补步；M2在相同M1输入上增加依赖传播，检验其能否进一步减少前置条件失效后的误给分。证据不足、替代路径未知或系统异常时输出`needs_review`，并保留已经可靠确定的分项。

**算法4：规则驱动的七项分项评分与复核分流**

```text
输入：评分规范 R，证据状态 E，缺步诊断 G，依赖判定 D，复核策略 H
输出：R1—R7分项分数 S，总分 T，证据包 P，复核任务 Q

1. 对每个评分项 r 同时读取 evidence、omission、local_validity 和 dependency 四类状态。
2. 按冻结优先级派生主状态：
   needs_review > invalid > 无证据时的 omitted_allowed/missing_critical
   > partial > blocked_dependency > established。
3. 根据 R 的版本化计分规则映射分值：
   established 取满分；partial 取允许的部分分；omitted_allowed 按政策计分；
   missing_critical 不授予对应关键步骤分；invalid 按错误类型计零分或部分分；
   blocked_dependency 不把局部相关文本提升为完整得分。
4. 若主状态为 needs_review，则保留已确认项目，不把未决项目记零；生成 Q 并停止自动汇总完整总分。
5. 对可自动完成的样本聚合 T，并附上每项原文跨度、步骤 ID、路径 ID、原因码和规则版本。
6. 校验分项数量、分值范围和总分一致性；失败时记录系统异常并转 Q。
7. 输出 S、T、P 和 Q。
```

### 6.7 诊断状态、计分含义与优先级

诊断状态不是单一分类器直接输出的标签，而是由证据、省略、局部有效性和依赖四类判断共同派生。各状态的使用边界如下。

| 主状态 | 触发条件 | 自动计分含义 | 必须保留的依据 |
| --- | --- | --- | --- |
| `established` | 存在合格原文证据，局部内容有效，且至少一条必要依赖路径成立 | 按冻结rubric授予该项满分 | 原文跨度、步骤ID、成立路径ID |
| `partial` | 已表达评分项的实质内容，但只满足部分子条件 | 仅在rubric预先允许时给部分分 | 已覆盖与未覆盖的子条件 |
| `omitted_allowed` | 无独立步骤，但满足冻结省略政策的题目范围、锚点、前提与作用域 | 按政策计分，但不得伪造学生步骤 | `policy_rule_id`、前后锚点和适用范围 |
| `missing_critical` | 不可省略的必要内容没有合格学生证据 | 不授予对应关键步骤分，并记录根缺步 | 缺失要求、受影响评分项和证据搜索结果 |
| `invalid` | 学生明确表达了错误断言、理由、变量范围或应用方式 | 按rubric计零分或允许的部分分；可成为首错 | 错误原文、错误类型和局部判断 |
| `blocked_dependency` | 当前局部表达可能合理，但所有必要前置路径均未建立 | 不将该项判为完整成立；保留其局部内容状态 | 失败路径、上游根因和传播链 |
| `needs_review` | 解析失败、证据歧义、未知替代路径、规则不支持或系统异常 | 不视为零分；保留已确认分项并进入人工复核 | 未决字段、复核原因和已有确定结果 |

主状态按下表顺序派生，先满足者优先。若一个评分项同时存在自身错误和依赖受阻，主状态保留`invalid`，依赖失败作为次级原因单独保存，以免丢失根因信息。

| 优先级 | 判定问题 | 主状态 |
| ---: | --- | --- |
| 1 | 系统能否在当前信息和规则范围内可靠裁定？ | 否：`needs_review` |
| 2 | 学生是否明确写出了数学、语义或作用域错误？ | 是：`invalid` |
| 3 | 是否缺少合格原文证据？ | 是：满足政策则`omitted_allowed`，否则`missing_critical` |
| 4 | 是否只完成了评分项的一部分？ | 是：`partial` |
| 5 | 局部内容合理但必要依赖路径是否全部失败？ | 是：`blocked_dependency` |
| 6 | 证据、局部有效性和必要依赖是否均成立？ | 是：`established` |

### 6.8 七项rubric的典型依赖规则

下表给出跨题目共享的典型依赖骨架。正式实验时仍需为四道题分别冻结`path_id`、题目条件、可接受替代路径和作用域；表中规则不能脱离具体题目机械使用。

| 评分项 | 局部证据要求 | 典型必要依赖 | 主要阻断条件 | 允许的替代处理 |
| --- | --- | --- | --- | --- |
| R1 识别基例 | 明确给出起始值或对应基例 | 题目定义域和归纳起点 | 起始值与题目不一致 | 基例计算中已无歧义地体现起始值，可作为等价路径 |
| R2 证明基例 | 展示基例成立的代入、计算或论证 | R1，或同一句中明确完成“识别+证明” | 只写“显然”且无可核对内容；代入对象错误 | 合并表达可同时支持R1和R2，但必须共享真实原文证据 |
| R3 陈述归纳假设 | 对某个合法的`k`陈述待证命题 | 题目命题模板与归纳变量 | 假设了错误命题或错误指标 | 等价变量命名允许，但不得由系统改正学生公式 |
| R4 给出假设范围 | 明确`k`的范围、起点或适用条件 | R3与题目定义域 | 范围缺失、与题目冲突或作用域越界 | 若范围由紧邻且无歧义的量化表达给出，可登记共享证据 |
| R5 陈述归纳目标 | 明确要证明`P(k+1)`或等价下一步目标 | 通常依赖R3、R4；也可由清晰的过渡结构建立 | 目标仍是`P(k)`、变量切换不明 | 直接进入`k+1`推导且目标无歧义时，可登记替代路径 |
| R6 分解`k+1`情形 | 把`k+1`对象连接到`k`对象或题目递推结构 | R5与题目定义/递推关系 | 直接跳到结论，未建立连接；分解公式错误 | 经等价恒等变换建立同一连接时，可走已批准OR路径 |
| R7 应用归纳假设 | 在合法对象和范围上实际使用R3中的假设 | 通常为R3 AND R4 AND R6 | 只有“由归纳假设”字样；使用对象与假设不匹配 | 若另一条已登记推导路径确实使用同等前提，可满足OR路径 |

依赖路径求值遵循三条统一规则：AND路径要求所有必要节点均成立；OR路径只需一条已批准路径成立；未登记但可能正确的替代路径进入人工复核。依赖图只限制评分项能否成立，不会为缺失的学生证据创造分数，也不会因为后续受阻而抹除前面已经独立成立的项目。

### 6.9 模块组合与方法边界

五个核心模块按固定顺序连接：StudentStepIR解析提供候选步骤，证据约束模块筛除无原文依据或不忠实的候选，缺步诊断模块判断rubric覆盖和省略政策，依赖模块检查必要前置条件，评分模块最终生成分数与复核原因。M1包含解析、证据约束、缺步诊断和规则评分；M2在完全相同的M1输入和规则基础上增加依赖检查与状态传播。

方法边界如下：

- 模型不得直接输出最终分数、关键缺步或依赖成立结论；
- 系统生成的标准证明、搜索步骤或形式化展开不得写回StudentStepIR；
- `omitted_allowed`是冻结评分政策，不是系统替学生补造的步骤；
- 依赖状态不能替代原文证据，也不能把一个根错误重复记为多个独立错误；
- `needs_review`表示自动系统无法可靠裁定，不等于学生得零分；
- Lean不属于五个核心模块，只在全部核心实验完成后作为可选扩展。

## 第七部分 对比实验与阶段性预期结果

### 7.1 实验设计与阶段性准备

现阶段工作以研究问题收敛、方法架构设计和实验方案准备为主，已经完成以下内容：

1. 将研究对象限定为真实学生数学归纳法自由文本作答，确定以七项归纳法 rubric 的分项评分、关键缺步诊断和人工复核分流为主要任务；
2. 明确核心研究问题，即原文证据约束能否减少关键缺步误给分，步骤依赖能否进一步减少前置条件缺失时的后续误给分，以及上述机制会引入多少合理省略误扣分和人工复核成本；
3. 完成 `induction-stepir-v1` 的概念设计，明确学生原文、字符跨度、步骤角色、断言、理由、引用和作用范围等主要字段；
4. 完成证据约束、合理省略与关键缺步诊断、步骤依赖传播、规则分项评分和人工复核分流的模块设计；
5. 完成 B0、B1、B2、M1 和 M2 的对照实验框架，以及分项评分、关键缺步、合理省略、依赖质量、自动覆盖率、人工复核率和成本指标的评价设计；
6. 完成总体方法架构图以及 StudentStepIR 解析、原文证据约束、合理省略与关键缺步诊断、步骤依赖传播、规则分项评分与人工复核等核心模块图；
7. 形成数据设计、StepIR Schema、评分规则、模型输入输出约束和评价协议的第一版文档，为后续数据标注和程序实现提供统一口径。

上述成果属于方法与实验设计层面的阶段性成果，用于说明研究问题已经形成可执行的技术路径；其作用不是替代正式实验，也不代表相关方法已经取得确定增益。

### 7.2 预期实证现象与研究假设

根据当前理论分析，后续实验主要验证以下预期。所有表述均为待检验研究假设，具体结论以冻结测试集上的正式结果为准。

#### 7.2.1 直接评分基线的预期表现

B1 和 B2 预计能够识别多数结构清楚、步骤完整的学生作答，并在总分和七项 rubric 标签上形成具有一定竞争力的结果。强 rubric 提示、few-shot 示例或检索材料预计能够进一步提高常见表达的识别能力。

但直接评分模型可能依据题目、标准证明和上下文自动补全学生没有写出的推理。当答案给出正确结论，却没有明确建立 `k+1` 情形与 `k` 情形的联系，或者没有实际应用归纳假设时，直接评分方法仍可能授予 R6 或 R7 分数。因此，B1/B2 的主要风险预计不是完全无法评分，而是在“看起来可以补全”的答案上产生无原文依据的误给分。

#### 7.2.2 M1证据约束的预期增益

M1 要求每个可得分判断关联学生原文跨度和对应 EvidenceItem，预计能够减少以下问题：

- 学生未写出关键步骤，但模型根据标准证明自动补全后给分；
- 只出现相关关键词，却没有形成相应数学断言或推理连接；
- 结构化解析修正学生原文中的错误常数、变量或推导方向；
- 评分理由无法定位到学生原始作答。

因此，M1 预期首先降低关键缺步误给分率并提高评分证据的可追溯性。与此同时，严格证据门也可能将合理压缩、跨句表达或非标准措辞错误识别为证据不足，导致合理省略误扣分或人工复核率上升。实验需要同时报告收益和代价，不能只报告评分准确率的变化。

#### 7.2.3 M2步骤依赖的预期增益

M2 在 M1 基础上加入 rubric 所规定的有效依赖路径，预计主要改善“当前评分项表面相关，但必要前置条件没有建立”的情况。例如，学生可能写出“由归纳假设可得”，但没有正确陈述归纳假设、适用范围不合法，或者当前表达无法实际引用该假设。仅靠局部关键词或单项证据可能仍然给分，而依赖检查应将其标记为 `blocked_dependency` 或相应复核状态。

M2 预期进一步降低依赖失效条件下的后续误给分，并帮助定位首个根错误或首个关键缺步，避免把同一前置错误重复计算为多个独立错误。其潜在代价是依赖规则过严时可能阻断替代正确路径，从而降低自动覆盖率并增加人工复核。因此，实验需要单独分析标准路径、替代路径和依赖歧义样本。

#### 7.2.4 风险—覆盖—复核成本的预期关系

本研究预计不同方法之间存在如下权衡：

```text
直接评分：自动覆盖率较高，但关键缺步误给分风险可能较高
        ↓ 加入原文证据约束
M1：关键缺步误给分预计下降，但合理省略误扣分和复核率可能上升
        ↓ 加入步骤依赖
M2：依赖失效误给分预计进一步下降，但替代路径和规则边界可能增加复核成本
```

因此，本研究不预设 M2 在所有总体指标上必然优于 M1，而是比较各方法在相同自动覆盖率或相同人工复核预算下的评分风险。若 M2 只通过大量转人工复核降低误给分，则应如实报告其覆盖代价，不能将其直接解释为自动评分能力提升。

### 7.3 对比方法

| 组别 | 预期优势 | 预期主要问题 | 重点验证指标 |
| --- | --- | --- | --- |
| B0 | 提供与原归纳法自动评分研究可比较的基础结果 | 方法能力和复现条件可能有限 | 七项 Macro-F1、总分误差 |
| B1 | 实现简单，自动覆盖率高，可直接生成分数和反馈 | 容易依据标准证明补全学生缺失步骤 | 关键缺步误给分率、证据可追溯率 |
| B2 | Rubric、示例和检索信息有助于识别常见表达 | 强提示不一定消除无依据补步 | Macro-F1、总分误差、关键缺步误给分率 |
| M1 | 分数必须关联原文证据，预计减少无依据给分 | 可能误伤合理省略并提高复核率 | 关键缺步误给分率、合理省略误扣分率、覆盖率 |
| M2 | 检查前置条件并定位根缺步，预计减少依赖失效误给分 | 依赖规则可能过严，替代路径处理困难 | 依赖质量、首错/首缺步定位、覆盖率、复核率 |

### 7.4 实验设置与公平比较原则

正式实验拟使用相同的数据划分、相同冻结测试集和相同七项rubric比较B0、B1、B2、M1和M2。中期阶段为便于展示，先按照480份二次标注作答进行理论设计；实际样本数量以数据审计和标注质量为准。

| 设置项 | 计划设置 | 控制目的 |
| --- | --- | --- |
| 数据划分 | 模拟为240份开发/训练、90份验证、150份冻结测试 | 测试集不参与提示、规则、政策和阈值调整 |
| 防泄漏策略 | 同一原作答及其人工变体、近重复、模板家族和题目家族不跨划分 | 防止相似证明表达同时进入开发集和测试集 |
| Gold构建 | 测试集全部双人独立标注；开发集抽取不少于50份双标；分歧由第三方或讨论裁决 | 同时报告原始一致性和裁决后Gold，不以裁决掩盖定义歧义 |
| 模型与参数 | 正式运行前冻结模型版本、系统提示、温度、最大输出、few-shot样本和检索库 | 保证不同方法之间只改变预定模块 |
| B1/B2输入 | B1仅使用题目、作答和通用评分请求；B2增加冻结rubric、开发集few-shot或检索材料 | 区分直接评分与强rubric基线 |
| M1/M2输入 | M1与M2共享同一批候选StudentStepIR；M2仅增加依赖路径求值 | 使M1到M2的变化可归因于依赖机制 |
| 失败处理 | 拒答、超时、解析失败、缺失ID、重复ID和额外ID全部进入分母并单独统计 | 禁止用Gold回退或剔除失败样本美化结果 |
| 重复运行 | 含生成模型的方法至少独立运行3次；规则层固定输入时保持确定性 | 报告均值、标准差和运行波动 |
| 显著性与区间 | 对主要指标使用按作答配对的bootstrap置信区间；分项二分类差异可补充McNemar检验 | 避免把小幅随机波动解释为稳定增益 |
| 风险—覆盖比较 | 同时报原始阈值、相同覆盖率和相同人工复核预算下的结果 | 防止通过大量转人工复核制造低风险 |

为降低研究者自由度，题目专用省略政策、依赖路径、状态优先级、计分规则和复核阈值都在验证集上冻结，并保存版本号。正式结果至少报告总体、分题目、答案长度、关键缺步类型、替代路径和复核原因六个层面的分析。

### 7.5 评价指标与计算公式

设测试作答数为`N`，每份作答包含`J=7`个rubric项；真实标签为`yᵢⱼ`，预测标签为`ŷᵢⱼ`；人工总分与系统总分分别为`Tᵢ`和`T̂ᵢ`。主要指标定义如下。

#### 7.5.1 分项评分与总分指标

七项Macro-F1先分别计算每个rubric项的F1，再取宏平均：

$$
F1_j=\frac{2P_jR_j}{P_j+R_j},\qquad
\text{Macro-F1}=\frac{1}{7}\sum_{j=1}^{7}F1_j.
$$

总分误差与一致率只在系统能够自动形成完整总分的样本集合`A`上计算，并与自动覆盖率一起报告：

$$
\text{MAE}=\frac{1}{|A|}\sum_{i\in A}|\hat T_i-T_i|,
$$

$$
\text{Exact}=\frac{1}{|A|}\sum_{i\in A}\mathbf{1}(\hat T_i=T_i),\qquad
\text{Acc}_{\pm1}=\frac{1}{|A|}\sum_{i\in A}\mathbf{1}(|\hat T_i-T_i|\le1).
$$

#### 7.5.2 关键缺步与合理省略风险

设`K`为人工标注为`missing_critical`的rubric实例集合，`award(x)=1`表示系统仍为该关键评分项授予正分，则关键缺步误给分率为：

$$
\text{FalseAward}_{critical}=\frac{\sum_{x\in K}\mathbf{1}(award(x)=1)}{|K|}.
$$

设`O`为人工标注为`omitted_allowed`的实例集合，`deny(x)=1`表示系统因该省略而拒绝本应获得的分数，则合理省略误扣分率为：

$$
\text{FalsePenalty}_{omission}=\frac{\sum_{x\in O}\mathbf{1}(deny(x)=1)}{|O|}.
$$

上述两个指标需要同时报告，避免只降低误给分却显著增加误扣分。另报告关键缺步Precision、Recall、F1，以及首错/首缺步定位准确率：

$$
\text{FirstErrorAcc}=\frac{1}{N_e}\sum_{i=1}^{N_e}\mathbf{1}(\widehat{root}_i=root_i),
$$

其中`Nₑ`为存在人工首错或首缺步标注的作答数。

#### 7.5.3 证据跨度与依赖质量

对单个证据片段，将预测字符位置集合记为`Ŝ`，Gold字符位置集合记为`S`：

$$
P_{span}=\frac{|\hat S\cap S|}{|\hat S|},\qquad
R_{span}=\frac{|\hat S\cap S|}{|S|},\qquad
F1_{span}=\frac{2P_{span}R_{span}}{P_{span}+R_{span}}.
$$

依赖边或依赖路径同样以预测集合`Ê`与Gold集合`E`计算Precision、Recall和F1。可观察引用边只在人工可裁定子集评价；样本级有效依赖按`explicit_reference`、`implicit_structural`和`implicit_allowed_knowledge`分层报告。除边级F1外，单独报告必要路径判定准确率、替代路径误拒率和依赖失效误给分率，以防较高边级F1掩盖关键路径错误。

#### 7.5.4 覆盖率、复核率与成本

设`A`为自动完成完整评分的作答集合，`Q`为至少含一个未决评分项而进入人工复核的作答集合：

$$
\text{Coverage}=\frac{|A|}{N},\qquad
\text{ReviewRate}=\frac{|Q|}{N}.
$$

`needs_review`不记为零分，也不能从分母中删除。成本指标包括单份平均耗时、P95耗时、平均输入/输出token、单份模型费用和每100份作答预计人工复核分钟数。正式结论优先依据相同覆盖率或相同复核预算下的风险差异，而不是只比较某一固定阈值的点估计。

### 7.6 保守条件下的理论模拟实验数据

> **重要说明：本节全部数字均为中期汇报阶段依据理论预期构造的模拟数据，不是已经运行实验得到的真实结果，也不能作为论文最终结论。正式实验完成后，必须整体替换为冻结数据集上的实际结果、置信区间和运行记录。**

#### 7.6.1 模拟数据规模与划分

为使中期阶段的实验设计、表格和结果分析具有完整形式，模拟设定如下：

| 数据项目 | 模拟数量 | 说明 |
| --- | ---: | --- |
| 公开数据非空作答 | 3586份 | 仅采用公开论文所述规模作为模拟基数，后续以本地审计结果替换 |
| 二次标注作答 | 480份 | 四道题各120份 |
| 训练/规则开发集 | 240份 | 用于规则设计、提示设计和错误类型归纳 |
| 验证集 | 90份 | 用于阈值和复核策略选择 |
| 冻结测试集 | 150份 | 不参与提示、规则和阈值调整 |
| 测试集Rubric实例 | 1050项 | 150份作答 × 7项rubric |
| `missing_critical`实例 | 180项 | 用于计算关键缺步误给分率 |
| `omitted_allowed`实例 | 90项 | 用于计算合理省略误扣分率 |
| 依赖受阻实例 | 120项 | 用于评价依赖状态与根缺步定位 |
| 双人独立标注作答 | 200份 | 包含全部测试集和部分开发集 |

模拟标注一致性结果如下：

| 标注维度 | 模拟一致性指标 | 模拟解释 |
| --- | ---: | --- |
| 七项Rubric标签 | Cohen's κ = 0.82 | 基本评分项达到较好一致性 |
| 诊断主状态 | Cohen's κ = 0.74 | 多状态诊断仍存在边界分歧 |
| 合理省略/关键缺步 | Cohen's κ = 0.68 | 教学判断差异较明显，需要裁决 |
| 原文证据跨度 | Span-F1 = 0.85 | 多数证据可定位，跨句证据仍较困难 |
| 依赖边 | Pairwise-F1 = 0.73 | 核心依赖可识别，替代路径分歧较多 |
| 是否需要人工复核 | Cohen's κ = 0.72 | 复核边界基本可用但尚不稳定 |

#### 7.6.2 七项Rubric分项结果

表中数值为各评分项的模拟 F1。结果设置体现：简单、显式的基例识别相对容易；归纳假设范围、`k+1` 分解和归纳假设应用更依赖证据与步骤关系，因此更能体现 M1/M2 的作用。

| Rubric项 | B0 | B1 | B2 | M1 | M2 |
| --- | ---: | ---: | ---: | ---: | ---: |
| R1 识别基例 | 0.780 | 0.820 | 0.840 | 0.850 | **0.860** |
| R2 证明基例 | 0.690 | 0.730 | 0.750 | 0.770 | **0.780** |
| R3 陈述归纳假设 | 0.680 | 0.740 | 0.770 | 0.790 | **0.800** |
| R4 给出假设范围 | 0.580 | 0.620 | 0.660 | 0.690 | **0.710** |
| R5 陈述归纳目标 | 0.650 | 0.690 | 0.720 | 0.730 | **0.750** |
| R6 分解 `k+1` 情形 | 0.550 | 0.610 | 0.650 | 0.700 | **0.730** |
| R7 应用归纳假设 | 0.490 | 0.560 | 0.600 | 0.650 | **0.680** |
| **Macro-F1** | **0.631** | **0.681** | **0.713** | **0.740** | **0.759** |

在该保守模拟结果中，M1相比强提示基线B2的Macro-F1提高2.7个百分点；M2在M1基础上进一步提高1.9个百分点。增益仍主要来自R4、R6和R7，但整体提升有限，为后续真实实验和方法优化保留空间。

#### 7.6.3 总分、关键缺步与合理省略结果

| 指标 | B0 | B1 | B2 | M1 | M2 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 七项 Macro-F1 | 0.631 | 0.681 | 0.713 | 0.740 | **0.759** |
| 总分 MAE* | 1.48 | 1.31 | 1.18 | 1.07 | **0.98** |
| 总分完全一致率* | 28.7% | 34.7% | 40.0% | 45.3% | **49.3%** |
| 总分 ±1 一致率* | 64.0% | 70.7% | 76.0% | 80.0% | **83.3%** |
| 关键缺步误给分率 | 42.2% | 37.8% | 31.7% | 23.9% | **19.4%** |
| 合理省略误扣分率 | 18.9% | 15.6% | **13.3%** | 17.8% | 15.6% |
| 首错/首缺步定位准确率 | 34.0% | 40.0% | 46.0% | 55.0% | **62.0%** |
| 证据跨度F1 | 不适用 | 不适用 | 不适用 | **0.820** | **0.820** |
| 依赖识别F1 | 不适用 | 不适用 | 不适用 | 不适用 | **0.740** |

`*` 总分 MAE 和一致率按系统能够自动形成完整总分的样本计算，同时必须结合下一表的自动覆盖率阅读，不能脱离覆盖率单独比较。

按照180个`missing_critical`测试实例计算，模拟误给分数量分别为：B0 76项、B1 68项、B2 57项、M1 43项、M2 35项。M1相比B2少误给14项，误给分率下降7.8个百分点；M2相比M1再少误给8项，下降4.5个百分点。

按照90个`omitted_allowed`实例计算，模拟误扣数量分别为：B0 17项、B1 14项、B2 12项、M1 16项、M2 14项。该结果刻意保留证据约束的副作用：M1虽然减少关键缺步误给分，但严格证据门使合理省略误扣分率比B2增加4.5个百分点；M2通过省略政策和替代路径规则将该指标降低至15.6%，但仍未优于B2。

#### 7.6.4 自动覆盖率、人工复核与运行成本

| 指标 | B0 | B1 | B2 | M1 | M2 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 自动完成样本数 | 150 | 148 | 145 | 132 | 123 |
| 自动覆盖率 | 100.0% | 98.7% | 96.7% | 88.0% | 82.0% |
| 人工复核样本数 | 0 | 2 | 5 | 18 | 27 |
| 人工复核率 | 0.0% | 1.3% | 3.3% | 12.0% | 18.0% |
| 单份平均耗时 | 0.4秒 | 1.8秒 | 2.7秒 | 2.3秒 | 2.7秒 |
| 单份平均模型费用 | 约$0 | $0.0038 | $0.0076 | $0.0052 | $0.0058 |

费用数据按统一模型价格和模拟 token 使用量估算，只用于中期阶段说明成本记录方式，后续必须以真实模型、提示长度、缓存策略和API价格重新计算。

模拟结果表明，M1/M2并不是在所有指标上无代价地优于基线。M2获得相对较低的关键缺步误给分率和较高的分项F1，但关键缺步误给分率仍为19.4%，自动覆盖率从B2的96.7%下降到82.0%，人工复核率增加到18.0%。这说明当前理论方案仍有较大优化空间，其阶段性价值只能表述为“在增加复核成本的条件下降低部分高风险误给分”。

#### 7.6.5 模拟消融结果

| 方法变体 | Macro-F1 | 关键缺步误给分率 | 合理省略误扣分率 | 自动覆盖率 |
| --- | ---: | ---: | ---: | ---: |
| B2 强Rubric提示 | 0.713 | 31.7% | **13.3%** | **96.7%** |
| M1 去掉语义忠实性检查 | 0.722 | 28.3% | 15.6% | 91.3% |
| M1 完整证据门 | 0.740 | 23.9% | 17.8% | 88.0% |
| M2 去掉替代路径 | 0.748 | 20.6% | 22.2% | 76.7% |
| M2 完整方法 | **0.759** | **19.4%** | 15.6% | 82.0% |

模拟消融结果用于体现各部分的独立作用：语义忠实性检查主要减少模型修正或补全学生原文造成的误给分；步骤依赖进一步降低前置条件失效时的误给分；替代路径规则主要缓解依赖机制过严造成的合理答案误判，并恢复部分自动覆盖率。

#### 7.6.6 模拟结果图

下列图表由第7.6.2—7.6.4节的模拟数据生成，只用于中期阶段展示预期比较方式，正式实验后必须整体替换。

![图7-1 不同方法的评分质量与关键缺步风险（模拟）](assets/midterm/experiment_results/simulated_main_results.png)

**图7-1 不同方法的评分质量与关键缺步风险（模拟）**

![图7-2 七项Rubric分项F1对比（模拟）](assets/midterm/experiment_results/simulated_rubric_f1_profile.png)

**图7-2 七项Rubric分项F1对比（模拟）**

![图7-3 关键缺步风险—自动覆盖率关系（模拟）](assets/midterm/experiment_results/simulated_risk_coverage.png)

**图7-3 关键缺步风险—自动覆盖率关系（模拟）**

图7-1展示总体评分质量与关键缺步风险并非同一指标；图7-2展示M1/M2的预期增益主要集中在R4、R6和R7；图7-3则明确表示M1/M2降低风险的同时牺牲了自动覆盖率，因此不能只依据误给分率判断方法优劣。

#### 7.6.7 模拟结果下可以形成的阶段性判断

如果正式实验获得与上述模拟数据接近的结果，则可以形成以下结论：

1. 强提示能够提高一般评分准确性，但不能充分消除关键缺步情况下的无依据给分；
2. 原文证据约束对关键缺步误给分产生一定抑制作用，但提升幅度有限，同时带来合理省略误扣分和人工复核率上升；
3. 步骤依赖在证据约束基础上进一步减少部分前置条件失效误给分，但依赖识别和替代路径处理仍是主要误差来源；
4. 替代路径与省略政策是控制过度阻断的重要组成部分，缺少该机制时虽然误给分可能降低，但误扣分和复核成本明显增加；
5. M2的阶段性预期不是实现高精度完全自动化，而是在约82%的自动覆盖率下，将关键缺步误给分率由B2的约32%降低到约19%，并为部分高风险样本提供明确的复核原因。

这些判断目前仍属于基于模拟数据形成的预期解释。正式论文只能保留被真实实验支持的结论。

### 7.7 构造学生作答案例与模块行为分析

> **案例说明：以下三份作答均为研究者构造的模拟样本，不来自公开数据集，也不是已经运行系统得到的真实输出。** 三个案例统一使用题目：用数学归纳法证明，对任意整数`n≥1`，`1+2+⋯+n=n(n+1)/2`。案例的作用是说明B2、M1和M2预期产生差异的位置；正式论文应替换或补充真实匿名作答及实际模型输出。

#### 7.7.1 案例A：结论正确但缺少关键连接

**构造作答A：**

> 当`n=1`时，左边等于1，右边也等于1，所以基例成立。假设对某个`k≥1`有`1+⋯+k=k(k+1)/2`。因此`1+⋯+(k+1)=(k+1)(k+2)/2`，命题对`k+1`成立。

该作答正确完成了基例和归纳假设，也写出了`k+1`情形的目标结果，但没有写出`S_{k+1}=S_k+(k+1)`，也没有展示如何实际使用归纳假设。系统可以从标准证明补出这两步，但这些补全不能作为学生的R6、R7证据。

| 判断主体 | R1—R5 | R6 分解`k+1`情形 | R7 应用归纳假设 | 预期解释 |
| --- | --- | --- | --- | --- |
| 构造Gold | 基本成立 | `missing_critical` | `missing_critical` | 两个关键连接均无原文证据 |
| B2强rubric直接评分 | 多数成立 | 可能误判成立 | 可能误判成立 | 模型依据标准证明自动补步 |
| M1证据约束 | 有证据项成立 | `missing_critical` | `missing_critical` | 证据门阻止无跨度的R6/R7得分 |
| M2证据＋依赖 | 与M1相同 | `missing_critical` | `missing_critical`，根因链指向R6/R7 | 本例主要体现证据门，依赖模块增益有限 |

#### 7.7.2 案例B：允许压缩常规代数化简

**构造作答B：**

> `n=1`时等式成立。假设对某个`k≥1`，`S_k=1+⋯+k=k(k+1)/2`。对`k+1`，有`S_{k+1}=S_k+(k+1)`；由归纳假设，这等于`k(k+1)/2+(k+1)`，化简即得`(k+1)(k+2)/2`。

该作答给出了分解式、归纳假设的具体替换以及化简前后的两个锚点，只省略了把`k(k+1)/2+(k+1)`因式分解为`(k+1)(k+2)/2`的常规中间算式。若题目专用省略政策允许这种有限代数压缩，就不应要求学生补写每一行计算。

| 判断主体 | R6 | R7 | 省略诊断 | 预期解释 |
| --- | --- | --- | --- | --- |
| 构造Gold | `established` | `established` | 代数微步骤为`omitted_allowed` | 前后锚点和必要前提充分 |
| B2强rubric直接评分 | 成立 | 成立 | 通常不单独说明 | 结果可能正确，但省略理由不可审计 |
| M1证据约束 | 成立 | 成立 | 若政策匹配则`omitted_allowed` | 不因缺少逐行化简而误扣分 |
| M2证据＋依赖 | 成立 | 成立 | `omitted_allowed` | R3、R4和R6路径均建立，不触发阻断 |

#### 7.7.3 案例C：局部出现“由归纳假设”，但前置假设未建立

**构造作答C：**

> `n=1`时，`1=1`，基例成立。令`k≥1`。对`k+1`，`S_{k+1}=S_k+(k+1)=k(k+1)/2+(k+1)=(k+1)(k+2)/2`，其中第二个等号由归纳假设得到。因此命题成立。

该作答写出了正确的分解和“由归纳假设”字样，但此前只写了`k`的范围，没有明确假设`S_k=k(k+1)/2`成立。局部证据匹配容易让逐项评分器把R7判为成立；依赖感知方法则应区分“提到了归纳假设”与“已建立可用的归纳假设”。

| 判断主体 | R3 陈述归纳假设 | R4 假设范围 | R6 | R7 | 预期解释 |
| --- | --- | --- | --- | --- | --- |
| 构造Gold | `missing_critical` | `partial`或随题目规则裁决 | `established` | `blocked_dependency` | R7的必要前提R3未建立 |
| B2强rubric直接评分 | 可能从后文反推成立 | 可能成立 | 成立 | 可能成立 | 容易把后文公式当作已陈述的归纳假设 |
| M1证据约束 | `missing_critical` | 仅按“`k≥1`”的真实跨度判断 | 成立 | 可能因局部文本判为成立或转复核 | M1能发现R3缺证据，但尚未强制传播 |
| M2证据＋依赖 | `missing_critical` | 保留局部状态 | 成立 | `blocked_dependency` | R7沿`R3 AND R4 AND R6`路径受阻，根缺步定位到R3 |

三个案例分别对应本研究希望区分的三种情况：A不能由系统替学生补造关键步骤；B不能把合理压缩机械判为缺步；C不能因为局部出现正确公式和关键词就忽略前置依赖。正式实验时，将从冻结测试集中选取同类真实案例，附上匿名原文、双人Gold、各方法原始输出、最终诊断和分歧原因。

### 7.8 结果更新与结论形成规则

实验完成后，本节按照以下原则更新：

- 用真实样本数量、数据划分、模型版本和运行日期整体替换第7.6节的模拟设定与模拟数值；
- 将“预期降低”“可能提高”等假设性表述替换为带具体数值和不确定性范围的观察结果；
- 分别报告成功案例、失败案例和系统拒答，不剔除解析失败、缺失输出或需要复核的样本；
- 若 M1 或 M2 未产生预期增益，应保留结果并分析原因，不反向修改研究假设；
- 只有正式实验支持的内容才能写入论文结论和创新效果，方法设计本身不等同于效果已经成立。

## 第八部分 后期工作计划

### 8.1 后期工作目标

后期工作的总体目标是将现阶段的方法设计转化为可复现的真实实验：首先固定公开数据、rubric和二次标注规则，然后完成B0/B1/B2基线以及M1/M2核心方法，在同一冻结测试集上比较评分增益、关键缺步风险、合理省略误扣分、自动覆盖率和人工复核成本，最后根据实际结果完成论文撰写。

后期工作遵循以下原则：

- 先固定数据和评价口径，再调整模型、规则和阈值；
- 先完成不含Lean的M1/M2核心实验，再决定是否开展可选M3；
- 每一阶段均保存输入版本、运行配置、原始输出、失败样本和分析结论；
- 不使用测试集修改提示、规则、省略政策、依赖路径或复核阈值；
- 模拟数据只用于中期展示结构，正式实验开始后全部替换。

### 8.2 分阶段实施计划

| 阶段 | 计划周期 | 主要任务 | 阶段交付物 |
| --- | --- | --- | --- |
| 第一阶段：数据审计 | 第1—2周 | 下载公开数据；核验版本、许可、题目、字段、空值、重复项和原始标签；生成稳定ID | 数据审计报告、只读原始数据层、版本与校验值记录 |
| 第二阶段：标注规范与先导标注 | 第3—6周 | 为四道题制定R1—R7证据条件、省略政策和依赖路径；完成双人先导标注与分歧裁决 | 标注手册、80—120份先导Gold、一致性报告 |
| 第三阶段：基线实现 | 第7—9周 | 复现B0或确定可复现替代；实现B1直接评分和B2强Rubric/Few-shot评分 | 基线代码、冻结提示、基线结果与失败分类 |
| 第四阶段：M1实现 | 第10—12周 | 实现StudentStepIR、Schema校验、跨度对齐、语义忠实性检查、证据门和规则评分 | M1程序、单元测试、证据与关键缺步初步结果 |
| 第五阶段：M2实现 | 第13—15周 | 实现可观察引用、题干/允许知识目录、结构性与隐含有效依赖、AND/OR路径、状态传播、替代路径和根因归并 | M2程序、依赖测试、M1/M2消融结果 |
| 第六阶段：正式实验 | 第16—18周 | 冻结测试集；运行B0/B1/B2/M1/M2；完成总体、分题目、风险—覆盖和成本评价 | 正式结果表、置信区间、运行日志、案例库 |
| 第七阶段：论文完善 | 第19—22周 | 完成错误分析、局限性分析、图表整理、正文撰写和多轮修改 | 论文定稿、答辩材料、可复现代码与文档 |

以上周期为相对安排，可根据学校提交时间调整，但阶段顺序原则上不改变。数据与标注尚未稳定时，不提前宣布模型效果；正式测试集一旦冻结，不再用于规则开发。

### 8.3 各阶段的具体工作

#### 8.3.1 数据审计与实验样本固定

首先完成Student Proof by Induction Data Set的本地下载和版本固定，核对公开论文所述规模与实际文件数量是否一致。对四道题分别检查作答字段、评分标签、评分说明、空值、重复作答和异常编码，并保存清洗前后的数量变化。处理后为每条作答生成稳定的 `answer_id` 和 `problem_version`，保证标注、预测和结果表能够相互追溯。

#### 8.3.2 标注手册与二次标注

在数据审计基础上，为四道题逐项制定R1—R7的证据要求、部分完成条件、不可省略内容、允许省略规则和必要依赖路径。先选择覆盖完整答案、合理压缩、关键缺步、明确错误、替代路径和歧义表达的样本开展先导标注。至少两名标注者独立标注测试子集，并记录分歧原因。若合理省略、关键缺步或依赖标签一致性不足，则先修订定义并重新标注受影响样本。

#### 8.3.3 基线与核心方法实现

基线阶段完成B0、B1和B2，并固定模型版本、温度、提示模板、few-shot样本和调用配置。核心方法阶段先实现M1，使每个评分项能够追溯到原文证据和规则版本；再在相同StudentStepIR输出上实现M2，确保M1/M2的差异只来自依赖机制。每个模块均建立单元测试和人工构造反例，重点测试跨度越界、引用不存在、系统补步、合理省略、关键连接缺失、依赖循环、替代路径和重复扣分。

#### 8.3.4 正式评价与论文更新

正式实验阶段在同一冻结测试集上运行所有方法，记录请求、原始响应、解析失败、拒答、重复ID、缺失ID、耗时和费用。除总体指标外，还按题目、答案长度、错误类型、替代路径和复核原因进行分层分析。实验完成后用真实结果替换第七部分的全部模拟数据，并根据实际证据修改结论；若结果不符合预期，则保留负面结果并分析其原因。

### 8.4 预期成果

后期预计形成以下成果：

1. 一份经过版本核验的数学归纳法学生作答数据清单；
2. 一套原文证据、合理省略、关键缺步和步骤依赖二次标注规范；
3. 一批双人标注并经过裁决的研究用Gold数据；
4. B0、B1、B2、M1和M2的可复现实验实现；
5. 关键缺步误给分、合理省略误扣分和风险—覆盖—复核成本的实验结果；
6. 典型成功案例、失败案例和系统边界分析；
7. 完整学位论文、中期与答辩材料，以及与论文结果对应的代码和配置记录。

### 8.5 主要风险与应对措施

| 风险 | 可能影响 | 应对措施 |
| --- | --- | --- |
| 合理省略标注一致性不足 | Gold不稳定，难以评价方法 | 增加正例、反例和边界案例；低一致性标签转复核或缩小定义范围 |
| StudentStepIR发生自动补步 | 证据层被系统推理污染 | 强制保存原文跨度；使用语义忠实性检查；禁止评分状态进入模型输出 |
| 依赖规则过严 | 替代正确路径被阻断，复核率升高 | 使用AND/OR路径；登记替代路径；无法覆盖时转人工复核 |
| 依赖规则过松 | 前置步骤错误后仍继续得分 | 建立成对反例；单独统计依赖失效误给分率 |
| 模型输出不稳定 | 结果难以复现 | 固定模型和参数；保存原始响应；必要时重复运行并报告波动 |
| 自动覆盖率过低 | 方法教学实用性不足 | 比较不同复核阈值；报告风险—覆盖曲线；不以大量拒答制造低错误率 |
| 四道题规模有限 | 泛化结论受限 | 明确研究边界；进行分题目验证；将跨题型泛化列为后续工作 |
| 正式结果低于模拟预期 | 中期结论无法直接保留 | 将模拟数据明确标为预期；按真实结果重写结论并分析失败原因 |

### 8.6 可选扩展安排

局部Lean验证不纳入后期工作的必做路径。只有当数据标注、M1、M2和正式评价均按计划完成后，才选择少量具有明确学生证据、能够忠实编码的R2、R6或R7步骤开展M3实验。该实验只评价新增真实检错、误报、覆盖率下降、耗时和复核成本，不允许Lean通过为学生未表达的评分项创造分数。

## 参考文献（持续补充）

[1] Zhao C, Silva M, Poulsen S. Autograding Mathematical Induction Proofs with Natural Language Processing[EB/OL]. arXiv:2406.10268, 2024, revised 2025. https://arxiv.org/abs/2406.10268.

[2] Ma W, Cojocaru A, Kolhe N, et al. Reliable Fine-Grained Evaluation of Natural Language Math Proofs[EB/OL]. arXiv:2510.13888, 2025, revised 2026. https://arxiv.org/abs/2510.13888.

[3] Poulsen S, Viswanathan M, Herman G L, et al. Proof Blocks: Autogradable Scaffolding Activities for Learning to Write Proofs[C]//Proceedings of the 27th ACM Conference on Innovation and Technology in Computer Science Education. 2022. https://doi.org/10.1145/3502718.3524774.

[4] Cabral R, Do T M, Yu X, et al. ProofFlow: A Dependency Graph Approach to Faithful Proof Autoformalization[EB/OL]. arXiv:2510.15981, 2025. https://arxiv.org/abs/2510.15981.

[5] Poulsen S, Kulkarni S, Herman G L, et al. Efficient Feedback and Partial Credit Grading for Proof Blocks Problems[EB/OL]. arXiv:2204.04196, 2022; later published in Artificial Intelligence in Education, 2023. https://arxiv.org/abs/2204.04196.

[6] Wu Y, Jiang A Q, Li W, et al. Autoformalization with Large Language Models[EB/OL]. arXiv:2205.12615, 2022. https://arxiv.org/abs/2205.12615.

[7] Cobbe K, Kosaraju V, Bavarian M, et al. Training Verifiers to Solve Math Word Problems[EB/OL]. arXiv:2110.14168, 2021. https://arxiv.org/abs/2110.14168.

[8] Uesato J, Kushman N, Kumar R, et al. Solving Math Word Problems with Process- and Outcome-Based Feedback[EB/OL]. arXiv:2211.14275, 2022. https://arxiv.org/abs/2211.14275.

[9] Lightman H, Kosaraju V, Burda Y, et al. Let's Verify Step by Step[EB/OL]. arXiv:2305.20050, 2023. https://arxiv.org/abs/2305.20050.

[10] Wang P, Li L, Shao Z, et al. Math-Shepherd: Verify and Reinforce LLMs Step-by-Step without Human Annotations[C]//Proceedings of the 62nd Annual Meeting of the Association for Computational Linguistics. 2024: 9426-9439. https://doi.org/10.18653/v1/2024.acl-long.510.

[11] Zheng L, Chiang W L, Sheng Y, et al. Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena[C]//Advances in Neural Information Processing Systems. 2023, 36. https://proceedings.neurips.cc/paper_files/paper/2023/hash/91f18a1287b398d378ef22505bf41832-Abstract-Datasets_and_Benchmarks.html.

[12] Liu Y, Iter D, Xu Y, et al. G-Eval: NLG Evaluation Using GPT-4 with Better Human Alignment[C]//Proceedings of the 2023 Conference on Empirical Methods in Natural Language Processing. 2023: 2511-2522. https://doi.org/10.18653/v1/2023.emnlp-main.153.

[13] Kim S, Shin J, Cho Y, et al. Prometheus: Inducing Fine-Grained Evaluation Capability in Language Models[C]//The Twelfth International Conference on Learning Representations. 2024. https://openreview.net/forum?id=8euJaTveKw.

[14] Welleck S, Liu J, Le Bras R, et al. NaturalProofs: Mathematical Theorem Proving in Natural Language[C]//Proceedings of the Neural Information Processing Systems Track on Datasets and Benchmarks. 2021, 1. https://arxiv.org/abs/2104.01112.

[15] Azerbayev Z, Piotrowski B, Schoelkopf H, et al. ProofNet: Autoformalizing and Formally Proving Undergraduate-Level Mathematics[EB/OL]. arXiv:2302.12433, 2023. https://arxiv.org/abs/2302.12433.
